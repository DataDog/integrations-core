Using the research and topology memories below, design the metric workload for `$integration`.

Research memory:

$research_technology_memory

Topology memory:

$design_lab_topology_memory

Lab output path: `$lab_path`

Produce a workload design that covers:

1. seed data and initial setup operations;
2. continuous operations for a long-running lab;
3. which metric, upstream signal, or future integration contract each operation exercises;
4. logs, health checks, or status commands that prove the workload is alive;
5. metrics or signals expected to stay zero;
6. metrics or signals that require manual validation;
7. load script language and asset-copying requirements.

Do not write files in this phase.
