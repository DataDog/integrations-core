Using the research memory below, design the E2E lab topology for `$integration`.

Research memory:

$research_technology_memory

Lab output path: `$lab_path`

Produce a topology design that covers:

1. selected deployment mode: Docker Compose, AWS-managed service, EC2 package install, or hybrid;
2. why the selected mode matches the official docs and lab goals;
3. service containers or cloud resources, dependencies, startup order, ports, and protocols;
4. authentication, seed data, and health checks;
5. network reachability from the Datadog Agent to every monitored endpoint;
6. Autodiscovery strategy when labels are useful;
7. auxiliary assets that must be copied to the remote Docker host;
8. assumptions or risks to carry into generation.

Do not write files in this phase.
