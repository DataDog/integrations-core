---
type: prompt
name: phase4_readme
---
# Task — Write the README

You are finishing the Datadog integration for **${integration}**. Everything else is already
built — the check, the spec, all endpoint mapping YAMLs, `metadata.csv`, and the test suite — and
a **scaffolded `README.md`** generated by `ddev create` already exists in the integration
directory. In this task you turn that scaffold into the finished, customer-facing README using
the complete README contract in your system instructions.

## Context from the build (what the check actually does)

Use this handoff to identify the integration directory and namespace, what the check does, and
any custom behavior that affects customer-facing documentation:

${build_integration_memory}

Read the scaffolded `README.md`, `metadata.csv`, `check.py`, and `assets/configuration/spec.yaml`.
Confirm customer-facing statements against those files, research the official product home page
when needed, and edit the scaffold in place according to the standing structure, sourcing,
content, and validation rules.

## Finish

Summarize the Overview you wrote, the official URL you used for `[1]` (and its source), how you
described the collected data, confirmation that the Service Checks section reads the fixed "does
not include any service checks" form with the `service_checks.json` line and link removed, and any
product-specific configuration guidance you added. A reviewer reads the final README against the
integration.
