Add rate limiting primitives for the async GitHub client and a two-tier RateLimiterFactory for the Dispatcher, including a header-driven BudgetGovernor that reacts to GitHub's reported remaining budget and secondary-limit responses with a configurable pacing cap.
