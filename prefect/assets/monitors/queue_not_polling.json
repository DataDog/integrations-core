{
  "created_at": "2026-09-23",
  "definition": {
    "message": "{{#is_alert}}\nNo worker has polled work queue {{work_queue_name.name}} in pool {{work_pool_name.name}} for at least 15 minutes (currently {{value}} seconds since the last poll). Scheduled flow runs on this queue will not start. Check that the workers serving this pool are running and can reach the Prefect API.\n{{/is_alert}}\n\n{{#is_recovery}}\nA worker is polling work queue {{work_queue_name.name}} in pool {{work_pool_name.name}} again, and its scheduled runs can start.\n{{/is_recovery}}",
    "name": "[Prefect] Work queue {{work_queue_name.name}} in pool {{work_pool_name.name}} is not being polled",
    "options": {
      "escalation_message": "",
      "include_tags": true,
      "locked": false,
      "new_host_delay": 300,
      "no_data_timeframe": null,
      "notify_audit": false,
      "notify_no_data": false,
      "renotify_interval": 0,
      "require_full_window": true,
      "thresholds": {
        "critical": 60,
        "critical_recovery": 30
      },
      "timeout_h": 0
    },
    "priority": null,
    "query": "min(last_15m):max:prefect.server.work_queue.last_polled_age_seconds{*} by {work_pool_name,work_queue_name} > 60",
    "tags": [
      "integration:prefect"
    ],
    "type": "query alert"
  },
  "description": "Tracks the time since a worker last polled each work queue. A queue that no worker polls cannot start any of its scheduled flow runs, so every deployment on that queue stops running even though no run fails.",
  "last_updated_at": "2026-09-23",
  "tags": [
    "integration:prefect"
  ],
  "title": "Work queue has not been polled by any worker",
  "version": 2
}
