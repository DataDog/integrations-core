version: '2'
services:
  gearman:
    image: ${DOCKER_IMAGE}:${DOCKER_TAG}
    ports:
      - 15440:4730
    entrypoint: ["/bin/sh"]
    command: ["-c", "gearmand > /var/log/gearmand.log 2>&1"]
    volumes:
      - ${DD_LOG_1}:/var/log/gearmand.log
