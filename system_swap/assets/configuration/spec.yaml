name: System Swap
fleet_configurable: true

files:
  - name: system_swap.yaml
    options:
      - template: init_config
        options:
          - template: init_config/default
      - template: instances
        options:
          - template: instances/default