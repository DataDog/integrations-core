_Generated 2026-04-30. 215 total: 81 generic / 33 custom / 101 impossible / 1 need review (⚠)._

### Generic auto-config possible

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Active Directory (`active_directory`) | — | other — Active Directory inherits the Windows perf_counters template (same shape as iis). In its default mode the agent runs on the domain controller itself and reads NTDS / Netlogon / DHCP Server / DFS Replicated Folders performance counters locally; the check actively detects which Windows services exist on the host (see ActiveDirectoryCheckV2.get_default_config) and enables only the relevant counter sets. There is no remote host/port in the default deployment, so discovery reduces to 'is the NTDS service installed on this Windows host?'. The optional pdh_legacy mode adds remote host + credentials but is hidden in the spec. | [spec](../active_directory/assets/configuration/spec.yaml), [check](../active_directory/datadog_checks/active_directory/check.py) |
| Aerospike (`aerospike`) | `openmetrics_endpoint` | openmetrics-port-scan — Modern Aerospike monitoring is OpenMetrics-based: the integration is a thin `OpenMetricsBaseCheckV2` subclass (`AerospikeCheckV2`) that scrapes the Aerospike Prometheus Exporter, which conventionally listens on port 9145 at `/metrics`. The README shows the standard Autodiscovery template `{"openmetrics_endpoint": "http://%%host%%:9145/metrics"}`. The legacy direct-info-protocol fields (`host`, `port` 3000, `tls_*`) are marked `legacy: true` / `hidden: true` and exist only for backwards compatibility. A generic OpenMetrics port probe at 9145 covers the default deployment. | [spec](../aerospike/assets/configuration/spec.yaml), [check](../aerospike/datadog_checks/aerospike/check.py), [readme](../aerospike/README.md), [upstream](https://github.com/aerospike/aerospike-prometheus-exporter) |
| Apache (`apache`) | `apache_status_url` | http-path-probe — Apache's mod_status exposes a single canonical machine-readable URL: `/server-status?auto`. The endpoint and its `?auto` query parameter are universal across deployments — a single HTTP probe is enough. | [spec](../apache/assets/configuration/spec.yaml), [auto_conf](../apache/datadog_checks/apache/data/auto_conf.yaml), [upstream](https://httpd.apache.org/docs/2.4/mod/mod_status.html) |
| Appgate SDP (`appgate_sdp`) | `openmetrics_endpoint` | openmetrics-port-scan — Appgate SDP's check is a minimal OpenMetricsBaseCheckV2 wrapper. The spec uses `instances/openmetrics` with a single required `openmetrics_endpoint` field defaulted to `http://localhost:5556/metrics`; no credentials, certificates, or tokens are required by the check. This matches pattern A (OpenMetrics on a known port). Discovery is a port-scan for `/metrics` on Appgate SDP appliance/controller/gateway hosts — port 5556 is the documented Prometheus exporter port for the appliance. | [spec](../appgate_sdp/assets/configuration/spec.yaml), [check](../appgate_sdp/datadog_checks/appgate_sdp/check.py), [readme](../appgate_sdp/README.md) |
| Argo Rollouts (`argo_rollouts`) | `openmetrics_endpoint` | openmetrics-port-scan — Argo Rollouts is a Kubernetes-native progressive delivery controller. The check is `OpenMetricsBaseCheckV2` and the spec uses `template: instances/openmetrics` with a single endpoint pointing to the controller's Prometheus metrics on the well-known port `8090/metrics` (documented in the README and upstream docs). No authentication is required; an OpenMetrics port-scan against `http://%%host%%:8090/metrics` on the `argo-rollouts` pod is sufficient. | [spec](../argo_rollouts/assets/configuration/spec.yaml), [check](../argo_rollouts/datadog_checks/argo_rollouts/check.py), [readme](../argo_rollouts/README.md), [upstream](https://argo-rollouts.readthedocs.io/en/stable/features/controller-metrics/) |
| Argo Workflows (`argo_workflows`) | `openmetrics_endpoint` | openmetrics-port-scan — Argo Workflows is a Kubernetes-native workflow engine. The check is `OpenMetricsBaseCheckV2` and the spec uses `template: instances/openmetrics` with a single endpoint pointing to the workflow controller's Prometheus metrics on the well-known port `9090/metrics` (documented in the README and upstream docs). No authentication is required by default; an OpenMetrics port-scan on the workflow-controller pod at `http://%%host%%:9090/metrics` is sufficient. | [spec](../argo_workflows/assets/configuration/spec.yaml), [check](../argo_workflows/datadog_checks/argo_workflows/check.py), [readme](../argo_workflows/README.md), [upstream](https://argo-workflows.readthedocs.io/en/latest/metrics/) |
| ArgoCD (`argocd`) | — | openmetrics-port-scan — ArgoCD ships several components, each exposing Prometheus metrics on a fixed well-known port: Application Controller (8082), ApplicationSet Controller (8080), API Server (8083), Repo Server (8084), Notifications Controller (9001) and Commit Server (8087), all at `/metrics`. The check is `OpenMetricsBaseCheckV2` and dispatches one scraper per declared endpoint; at least one endpoint must be set, but each is unauthenticated. Each component has a distinct AD identifier (e.g. `argocd-application-controller`, `argocd-server`, `argocd-repo-server`), so an openmetrics-port-scan keyed on the container name plus the component-specific port is sufficient. | [spec](../argocd/assets/configuration/spec.yaml), [check](../argocd/datadog_checks/argocd/check.py), [readme](../argocd/README.md), [upstream](https://argo-cd.readthedocs.io/en/stable/operator-manual/metrics/) |
| ASP.NET (`aspdotnet`) | — | other — ASP.NET is collected via Windows Performance Counters (PDH) on the local host where the Agent runs. The spec uses `template: instances/perf_counters` and the check inherits `PerfCountersBaseCheckWithLegacySupport`. There is no remote host/port in the default mode — auto-config reduces to detecting whether ASP.NET (the `ASP.NET` / `ASP.NET Apps v*` perf-counter object set) is present on the Windows host and enabling the check with default counters. The optional `pdh_legacy` template can target a remote machine via username/password but is hidden in the spec. | [spec](../aspdotnet/assets/configuration/spec.yaml), [check](../aspdotnet/datadog_checks/aspdotnet/check.py), [check](../aspdotnet/datadog_checks/aspdotnet/aspdotnet.py), [readme](../aspdotnet/README.md) |
| AWS Neuron (`aws_neuron`) | `openmetrics_endpoint` | openmetrics-port-scan — AWS Neuron monitoring relies on the `neuron-monitor` companion tool, which exposes Prometheus-formatted metrics on the well-known port `8000/metrics` on the EC2/Inferentia/Trainium instance. The check is `OpenMetricsBaseCheckV2` and the spec uses `template: instances/openmetrics` with a single endpoint. No authentication is required; an OpenMetrics port-scan against `http://%%host%%:8000/metrics` is sufficient where `neuron-monitor-prometheus` is running. | [spec](../aws_neuron/assets/configuration/spec.yaml), [check](../aws_neuron/datadog_checks/aws_neuron/check.py), [readme](../aws_neuron/README.md), [upstream](https://awsdocs-neuron.readthedocs-hosted.com/en/latest/tools/neuron-sys-tools/neuron-monitor-user-guide.html) |
| Azure IoT Edge (`azure_iot_edge`) | `edge_hub_prometheus_url`, `edge_agent_prometheus_url` | openmetrics-port-scan — Azure IoT Edge runtime exposes Prometheus metrics from two modules (edgeHub and edgeAgent), both on the well-known port 9600 at the canonical /metrics path. The check inherits OpenMetricsBaseCheck and the spec uses the openmetrics_legacy template; no credentials are required. Two endpoints are needed instead of one, but each is a stock OpenMetrics scrape on a fixed port, so a generic openmetrics-port-scan probe over port 9600 (run twice, once per module hostname/IP) is sufficient. Container Autodiscovery is the documented installation path - the README ships com.datadoghq.ad.* labels with both URLs hardcoded - which strongly indicates the deployment shape is autodiscoverable. | [spec](../azure_iot_edge/assets/configuration/spec.yaml), [check](../azure_iot_edge/datadog_checks/azure_iot_edge/check.py), [readme](../azure_iot_edge/README.md), [upstream](https://learn.microsoft.com/en-us/azure/iot-edge/how-to-access-built-in-metrics) |
| BentoML (`bentoml`) | `openmetrics_endpoint` | openmetrics-port-scan — BentoML serves Prometheus metrics on the model server's HTTP port (default 3000) at the canonical /metrics path. The spec uses template: instances/openmetrics with example openmetrics_endpoint http://localhost:3000/metrics, and the check class inherits OpenMetricsBaseCheckV2. No credentials are required. The check additionally probes a few model-server health endpoints under the same base URL, but those derive from the openmetrics_endpoint host:port - they are not separate inputs. Single canonical OpenMetrics port + path makes this a textbook generic openmetrics-port-scan candidate. | [spec](../bentoml/assets/configuration/spec.yaml), [check](../bentoml/datadog_checks/bentoml/check.py), [upstream](https://docs.bentoml.com/en/latest/build-with-bentoml/observability/metrics.html) |
| Boundary (`boundary`) | `health_endpoint`, `openmetrics_endpoint` | openmetrics-port-scan — HashiCorp Boundary's controller exposes Prometheus metrics on `/metrics` and a health endpoint on `/health`, both on the controller listener (default port 9203). The check inherits `OpenMetricsBaseCheckV2` and the only auxiliary call is an unauthenticated GET on `/health`. Both paths are universal and unauthenticated by default, so a generic OpenMetrics port-scan that targets 9203 covers it. Pattern A, with the health-endpoint requirement being a peer of the metrics URL on the same port. The Boundary controller's metrics endpoint is publicly documented as fixed at the controller API listener. | [spec](../boundary/assets/configuration/spec.yaml), [check](../boundary/datadog_checks/boundary/check.py), [upstream](https://developer.hashicorp.com/boundary/docs/operations/metrics) |
| Btrfs (`btrfs`) | — | other — The Btrfs check has no required configuration fields and no network endpoint. It enumerates `psutil.disk_partitions()` on the Agent host, filters those whose `fstype == 'btrfs'`, and issues `BTRFS_IOC_SPACE_INFO` / `BTRFS_IOC_FS_INFO` / `BTRFS_IOC_DEV_INFO` ioctls against each mountpoint via a directory file descriptor. There is no host/port/URL/credential to discover. Auto-config is therefore trivial: emit the integration with its default empty instance whenever the Agent host has at least one mounted btrfs filesystem (or even unconditionally — the check raises only if zero btrfs devices are present). Discovery is a host-local presence check ("is any filesystem of type btrfs mounted?"), analogous to pattern H/L but with no binary or network involved. | [spec](../btrfs/assets/configuration/spec.yaml), [check](../btrfs/datadog_checks/btrfs/btrfs.py), [readme](../btrfs/README.md) |
| Calico (`calico`) | `openmetrics_endpoint` | openmetrics-port-scan — Calico's check is a thin OpenMetricsBaseCheckV2 wrapper around Felix's Prometheus endpoint. The spec uses the standard `instances/openmetrics` template with a single required `openmetrics_endpoint` field; no credentials are involved. Felix exposes /metrics on a fixed default port (9091 per the README's minikube example, with felix-metrics-svc as the upstream-recommended Service). Discovery is a port-scan for an OpenMetrics endpoint on the well-known Felix metrics port. No auto_conf.yaml ships on disk, but the integration follows pattern A (OpenMetrics on a known port) and the user has nothing else to provide. | [spec](../calico/assets/configuration/spec.yaml), [check](../calico/datadog_checks/calico/check.py), [readme](../calico/README.md), [upstream](https://docs.tigera.io/calico/3.25/operations/monitor/monitor-component-metrics) |
| Celery (`celery`) | `openmetrics_endpoint` | openmetrics-port-scan — Despite the hint, this integration is not DogStatsD-based: it is a standard OpenMetricsBaseCheckV2 implementation that scrapes the Celery Flower Prometheus metrics endpoint. The spec inherits `template: instances/openmetrics` and overrides `openmetrics_endpoint.required: true`, with the canonical example `http://localhost:5555/metrics`. Flower's Prometheus integration documents port 5555 and path `/metrics` as the standard endpoint. This matches Pattern A ("OpenMetrics on a known port") - a generic openmetrics-port-scan can probe `http://%%host%%:5555/metrics` without integration-specific knowledge. No auto_conf.yaml ships today, but the shape is exactly the bootstrap-friendly OpenMetrics case. | [spec](../celery/assets/configuration/spec.yaml), [check](../celery/datadog_checks/celery/check.py), [readme](../celery/README.md), [upstream](https://flower.readthedocs.io/en/latest/prometheus-integration.html) |
| cert-manager (`cert_manager`) | `openmetrics_endpoint` | openmetrics-port-scan — cert-manager is an OpenMetricsBaseCheckV2 integration that scrapes the cert-manager controller's Prometheus endpoint. Default port 9402 and path `/metrics` are universal across cert-manager deployments (set by the controller's `--metrics-listen-address`). The shipped `auto_conf.yaml` already templates the endpoint as `http://%%host%%:9402/metrics` keyed on the `cert-manager-controller` AD identifier, so no credentials or per-deployment config are needed. | [spec](../cert_manager/assets/configuration/spec.yaml), [auto_conf](../cert_manager/datadog_checks/cert_manager/data/auto_conf.yaml), [check](../cert_manager/datadog_checks/cert_manager/cert_manager.py), [upstream](https://cert-manager.io/docs/devops-tips/prometheus-metrics/) |
| Cilium (`cilium`) | `agent_endpoint` | openmetrics-port-scan — Cilium exposes Prometheus metrics on fixed default ports — `cilium-agent` on 9962/metrics and `cilium-operator` on 6942/metrics. The check is `OpenMetricsBaseCheckV2`; the existing `auto_conf.yaml` already templates `agent_endpoint: http://%%host%%:9962/metrics` keyed off the `cilium-agent`/`cilium` AD identifier. No credentials needed; an OpenMetrics port-scan on the two known ports is sufficient. | [spec](../cilium/assets/configuration/spec.yaml), [auto_conf](../cilium/datadog_checks/cilium/data/auto_conf.yaml), [check](../cilium/datadog_checks/cilium/check.py), [upstream](https://docs.cilium.io/en/stable/observability/metrics/) |
| CockroachDB (`cockroachdb`) | — | openmetrics-port-scan — CockroachDB exposes a Prometheus-format metrics endpoint at the fixed path `/_status/vars` on its admin HTTP port (default 8080). The check is an OpenMetricsBaseCheckV2 subclass that hardcodes this default in `get_default_config`, so a generic OpenMetrics port-scan that probes `http://%%host%%:8080/_status/vars` is sufficient. The spec overrides `openmetrics_endpoint.required` to false because the check supplies the default. No credentials are required. | [spec](../cockroachdb/assets/configuration/spec.yaml), [check](../cockroachdb/datadog_checks/cockroachdb/check.py), [upstream](https://www.cockroachlabs.com/docs/stable/monitoring-and-alerting.html#prometheus-endpoint) |
| Consul (`consul`) | `url` | http-path-probe — Consul's HTTP API listens on a single well-known port (8500 by default for the agent, 8501 for HTTPS) and the check just needs a base URL — it discovers everything else (`/v1/status/leader`, `/v1/agent/self`, `/v1/agent/metrics?format=prometheus`) from there. The shipped auto_conf templates `http://%%host%%:8500`, which is exactly the canonical default. ACL tokens are optional and only required if the operator has enabled ACLs (default deny is becoming common, but there is still a wide deployment base running unauthenticated localhost agents). Probing port 8500 for `/v1/status/leader` returning a JSON-quoted IP:port string is a strong, integration-agnostic HTTP probe. | [spec](../consul/assets/configuration/spec.yaml), [auto_conf](../consul/datadog_checks/consul/data/auto_conf.yaml), [check](../consul/datadog_checks/consul/consul.py), [upstream](https://developer.hashicorp.com/consul/api-docs/agent) |
| CoreDNS (`coredns`) | `openmetrics_endpoint` | openmetrics-port-scan — CoreDNS exposes Prometheus metrics on `/metrics`, with the prometheus plugin defaulting to port 9153. This is a single canonical OpenMetrics endpoint, well-known and singular — exactly the case a generic OpenMetrics port-scan would handle. | [spec](../coredns/assets/configuration/spec.yaml), [auto_conf](../coredns/datadog_checks/coredns/data/auto_conf.yaml), [upstream](https://coredns.io/plugins/metrics/) |
| CRI-O (`crio`) | `prometheus_url` | openmetrics-port-scan — CRI-O is a Kubernetes container runtime. The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint, defaulting to http://localhost:9090/metrics (the port CRI-O's `metrics_port` config option uses). No authentication is required — the metrics endpoint is plain HTTP on a known port. A generic OpenMetrics port-scan against the CRI-O metrics port satisfies discovery. | [spec](../crio/assets/configuration/spec.yaml), [check](../crio/datadog_checks/crio/crio.py) |
| Datadog Cluster Agent (`datadog_cluster_agent`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint at http://%%host%%:5000/metrics. The existing auto_conf.yaml uses the cluster-agent AD identifier and templates the host. Default port (5000) and path (/metrics) are hardcoded, with no auth required, so a generic OpenMetrics port scan suffices for discovery. | [spec](../datadog_cluster_agent/assets/configuration/spec.yaml), [auto_conf](../datadog_cluster_agent/datadog_checks/datadog_cluster_agent/data/auto_conf.yaml), [check](../datadog_cluster_agent/datadog_checks/datadog_cluster_agent/check.py) |
| Datadog CSI Driver (`datadog_csi_driver`) | `openmetrics_endpoint` | openmetrics-port-scan — Datadog's own CSI driver exposes Prometheus metrics on a fixed endpoint. The check inherits OpenMetricsBaseCheckV2 with a hardcoded default of http://localhost:5000/metrics, and the existing auto_conf.yaml templates this as http://%%host%%:5000/metrics with the `csi-driver` AD identifier. Single canonical port and path, no auth — generic OpenMetrics port-scan applies cleanly. | [spec](../datadog_csi_driver/assets/configuration/spec.yaml), [auto_conf](../datadog_csi_driver/datadog_checks/datadog_csi_driver/data/auto_conf.yaml), [check](../datadog_csi_driver/datadog_checks/datadog_csi_driver/check.py) |
| NVIDIA DCGM (`dcgm`) | `openmetrics_endpoint` | openmetrics-port-scan — NVIDIA's dcgm-exporter exposes GPU telemetry as Prometheus metrics on the universally documented port 9400 at `/metrics`. The check inherits OpenMetricsBaseCheckV2 with no additional auth; the spec template just overrides the example endpoint. Single canonical port and path, no credentials — generic OpenMetrics port-scan applies. | [spec](../dcgm/assets/configuration/spec.yaml), [check](../dcgm/datadog_checks/dcgm/check.py), [upstream](https://github.com/NVIDIA/dcgm-exporter) |
| Disk (`disk`) | — | other — The Disk check is a host-local system check with no required configuration fields and no remote target. It enumerates partitions/devices via `psutil.disk_partitions()` and `psutil.disk_usage()` on the Agent host, optionally calls `blkid`/`lsblk` for labels (Linux) or creates SMB/NFS mounts (Windows). All instance options are filters/tagging knobs over the locally-discovered set. Auto-config is trivial: emit the integration with the default empty instance on every Agent host (the check is in fact enabled by default in core Agent). Discovery is universal — every host has at least the root filesystem — and there is no network probe. This places it alongside btrfs/iis as a host-presence check. | [spec](../disk/assets/configuration/spec.yaml), [check](../disk/datadog_checks/disk/disk.py), [readme](../disk/README.md) |
| .NET CLR (`dotnetclr`) | — | other — .NET CLR metrics are collected via Windows Performance Counters (PDH) on the local host where the Agent runs. The spec uses `template: instances/perf_counters` and the check inherits `PerfCountersBaseCheckWithLegacySupport`. There is no remote host/port in the default mode; the check reads the local `.NET CLR Memory`, `.NET CLR Exceptions`, etc. perf-counter object set. Auto-config reduces to detecting whether any .NET CLR-instrumented process is registered with PDH on the Windows host and enabling the check with the default counter list. The hidden `pdh_legacy` template can target a remote machine via username/password but is not the default path. | [spec](../dotnetclr/assets/configuration/spec.yaml), [check](../dotnetclr/datadog_checks/dotnetclr/check.py), [check](../dotnetclr/datadog_checks/dotnetclr/metrics.py) |
| Amazon Fargate (`ecs_fargate`) | — | other — ECS Fargate metrics are collected by hitting the task-local ECS Container Metadata Endpoint, either v4 (URL provided in the `ECS_CONTAINER_METADATA_URI_V4` env var) or v2 (the link-local `http://169.254.170.2/v2`). Both endpoints are unauthenticated and reachable only from inside the task. The integration ships a `conf.yaml.default` template with `ad_identifiers: [_ecs_fargate]` that the Agent's Autodiscovery layer uses to schedule the check whenever it detects it is running in a Fargate task. There are no per-instance required fields driving endpoint resolution; the check derives the URL itself at runtime from the environment. | [spec](../ecs_fargate/assets/configuration/spec.yaml), [auto_conf](../ecs_fargate/datadog_checks/ecs_fargate/data/conf.yaml.default), [check](../ecs_fargate/datadog_checks/ecs_fargate/ecs_fargate.py), [upstream](https://docs.aws.amazon.com/AmazonECS/latest/userguide/task-metadata-endpoint-v4-fargate.html) |
| EKS Fargate (`eks_fargate`) | — | other — EKS Fargate generates heartbeat and capacity metrics for pods running on Fargate virtual nodes. The check inherits `KubeletBase` and obtains the kubelet URL via `kubeutil.get_connection_info()`, which the Agent populates from in-cluster ServiceAccount credentials (mounted bearer token + CA cert). Detection of Fargate mode is done by inspecting the `DD_KUBERNETES_KUBELET_NODENAME` env var for the substring `fargate`. The integration ships a `conf.yaml.default` template with `ad_identifiers: [_eks_fargate]`, so Autodiscovery already schedules the check when the Agent runs in an EKS Fargate pod. No user-supplied required fields drive endpoint resolution; credentials are auto-injected like other in-cluster integrations (pattern O). | [spec](../eks_fargate/assets/configuration/spec.yaml), [auto_conf](../eks_fargate/datadog_checks/eks_fargate/data/conf.yaml.default), [check](../eks_fargate/datadog_checks/eks_fargate/eks_fargate.py), [upstream](https://docs.aws.amazon.com/eks/latest/userguide/fargate.html) |
| etcd (`etcd`) | `prometheus_url` | openmetrics-port-scan — etcd exposes Prometheus metrics on `/metrics` over its client port (default 2379). The auto_conf already lists both `https://` and `http://` variants of the same path. A generic OpenMetrics scan that tries TLS-then-cleartext on the client port is sufficient. | [spec](../etcd/assets/configuration/spec.yaml), [auto_conf](../etcd/datadog_checks/etcd/data/auto_conf.yaml), [upstream](https://etcd.io/docs/v3.5/op-guide/monitoring/) |
| Exchange Server (`exchange_server`) | — | other — Exchange Server inherits the Windows perf_counters template (PerfCountersBaseCheckWithLegacySupport). In its default mode the Agent runs on the Exchange host itself and reads MSExchange* PDH performance counters locally; there is no remote host/port to probe. Auto-config reduces to 'is Exchange Server installed on this Windows host?' — a local service detection, not a network probe. The hidden pdh_legacy template adds remote host + credentials, but it's an opt-in mode. | [spec](../exchange_server/assets/configuration/spec.yaml), [check](../exchange_server/datadog_checks/exchange_server/check.py) |
| External DNS (`external_dns`) | `prometheus_url` | openmetrics-port-scan — External DNS is a Kubernetes controller that exposes Prometheus metrics on a fixed path `/metrics` at the conventional default port 7979. The check uses `OpenMetricsBaseCheck` with a single `prometheus_url` field, and the shipped `auto_conf.yaml` already templates `http://%%host%%:7979/metrics` against the `external-dns` AD identifier. No credentials are required. | [spec](../external_dns/assets/configuration/spec.yaml), [auto_conf](../external_dns/datadog_checks/external_dns/data/auto_conf.yaml), [check](../external_dns/datadog_checks/external_dns/external_dns.py) |
| Falco (`falco`) | `openmetrics_endpoint` | openmetrics-port-scan — The Falco check is `OpenMetricsBaseCheckV2` and the spec uses the standard `instances/openmetrics` template — a single required `openmetrics_endpoint` and no auth fields. The README documents the conventional Prometheus listener at port 8765 with the canonical `/metrics` path, configured via `metrics.listen_port` in `falco.yaml`. A generic OpenMetrics port scan (host + 8765 + `/metrics`) matches the canonical deployment cleanly. No `auto_conf.yaml` is shipped today. | [spec](../falco/assets/configuration/spec.yaml), [check](../falco/datadog_checks/falco/check.py), [readme](../falco/README.md) |
| Fluentd (`fluentd`) | `monitor_agent_url` | http-path-probe — Fluentd's monitor_agent plugin exposes a single canonical JSON endpoint at `/api/plugins.json` on the conventional port 24220. The check makes an unauthenticated HTTP GET against this URL; no credentials, tokens, or per-deployment paths are required. Auto-config can probe `http://%%host%%:24220/api/plugins.json` and validate the JSON shape (top-level `plugins` array). | [spec](../fluentd/assets/configuration/spec.yaml), [check](../fluentd/datadog_checks/fluentd/fluentd.py), [upstream](https://docs.fluentd.org/input/monitor_agent) |
| Flux (`fluxcd`) | `openmetrics_endpoint` | openmetrics-port-scan — FluxCD is a Kubernetes GitOps controller (helm-controller, kustomize-controller, source-controller, notification-controller). Each controller exposes Prometheus metrics on a fixed, well-known port (8080) and path (`/metrics`), as documented in the README and reflected in the typical autodiscovery template (`http://%%host%%:8080/metrics`). The check is a thin `OpenMetricsBaseCheckV2` subclass with a single required field (`openmetrics_endpoint`) and no auth surface. This matches pattern A (`openmetrics-port-scan`) cleanly. | [spec](../fluxcd/assets/configuration/spec.yaml), [check](../fluxcd/datadog_checks/fluxcd/check.py), [readme](../fluxcd/README.md) |
| Gearman (`gearmand`) | — | tcp-banner-probe — Gearman speaks a plain-text admin protocol over TCP on the well-known port 4730. The check uses `python3_gearman.GearmanAdminClient` against `host:port` and explicitly defaults to `127.0.0.1:4730` when either field is omitted (`_get_conf` warns and falls back). No authentication is configurable. The optional `tasks` allowlist only filters per-task metrics; aggregate metrics work without it. A discovery probe can connect to TCP/4730 and issue an admin command (e.g. `version\r\n`, `status\r\n`) to confirm the server. | [spec](../gearmand/assets/configuration/spec.yaml), [check](../gearmand/datadog_checks/gearmand/gearmand.py), [readme](../gearmand/README.md) |
| HDFS Datanode (`hdfs_datanode`) | `hdfs_datanode_jmx_uri` | http-path-probe — The HDFS DataNode check fetches metrics from the JMX HTTP servlet at `/jmx` (no JMX-RMI). The DataNode HTTP port defaults to 9864 (Hadoop 3) and the path `/jmx` is universal, so a single HTTP probe against `http://%%host%%:9864/jmx` is enough to discover and validate the endpoint. No authentication is required by default. Classification is generic because the URL is canonical and singular. | [spec](../hdfs_datanode/assets/configuration/spec.yaml), [check](../hdfs_datanode/datadog_checks/hdfs_datanode/hdfs_datanode.py), [upstream](https://hadoop.apache.org/docs/r3.1.3/hadoop-project-dist/hadoop-hdfs/hdfs-default.xml) |
| HDFS Namenode (`hdfs_namenode`) | `hdfs_namenode_jmx_uri` | http-path-probe — Despite the name, the HDFS NameNode check does not use JMX/RMI: it issues an HTTP GET against the Hadoop JMX servlet at `/jmx` on the NameNode HTTP port (default 9870 in Hadoop 3, 50070 in Hadoop 2). The Python check is a thin AgentCheck that joins the configured URI with `jmx` and parses the JSON beans (FSNamesystem, FSNamesystemState, NameNodeInfo). The path is canonical and singular, no auth is required by default, so a single HTTP probe against `http://%%host%%:9870/jmx` resolves discovery. Same pattern P as hdfs_datanode. | [spec](../hdfs_namenode/assets/configuration/spec.yaml), [check](../hdfs_namenode/datadog_checks/hdfs_namenode/hdfs_namenode.py), [readme](../hdfs_namenode/README.md), [upstream](https://hadoop.apache.org/docs/r3.1.3/hadoop-project-dist/hadoop-hdfs/hdfs-default.xml) |
| Hugging Face TGI (`hugging_face_tgi`) | `openmetrics_endpoint` | openmetrics-port-scan — Hugging Face Text Generation Inference exposes Prometheus metrics on a single canonical `/metrics` path. The check is a thin subclass of `OpenMetricsBaseCheckV2` that maps a fixed metric/label dictionary; the only required field is the OpenMetrics endpoint URL. The spec example points at port 80, which matches the TGI default HTTP server port (configurable via `--port`). This is pattern A (OpenMetrics on a known port) - one URL, one path, no auth - so a generic openmetrics-port-scan against the typical TGI port set (80, 8080, 3000) covers the deployment shapes documented upstream. | [spec](../hugging_face_tgi/assets/configuration/spec.yaml), [check](../hugging_face_tgi/datadog_checks/hugging_face_tgi/check.py), [upstream](https://huggingface.co/docs/text-generation-inference/en/basic_tutorials/monitoring) |
| Hyper-V (`hyperv`) | — | other — Hyper-V is collected via Windows Performance Counters (PDH) on the local host where the Agent runs — `template: instances/perf_counters` plus a per-integration metric set defined in code. There is no remote host/port in the default mode; the integration just reads PDH counters for the Hyper-V provider on the same machine. Auto-config reduces to: detect whether the Hyper-V role is installed (e.g. presence of the `vmms` service or the Hyper-V WMI namespace) and enable the check with the built-in METRICS_CONFIG. The hidden `pdh_legacy` template can target a remote machine via host/username/password, but that's an opt-in mode mirroring IIS. | [spec](../hyperv/assets/configuration/spec.yaml), [check](../hyperv/datadog_checks/hyperv/check.py), [check](../hyperv/datadog_checks/hyperv/hyperv.py), [readme](../hyperv/README.md), [upstream](https://docs.microsoft.com/en-us/windows-server/virtualization/hyper-v/hyper-v-on-windows-server) |
| IIS (`iis`) | — | other — IIS is collected via Windows Performance Counters (PDH) on the local host where the Agent runs — there's no remote host/port in the default mode. Auto-config reduces to: detect whether IIS is installed (the W3SVC service) on this machine and enable the check with default sites/app_pools (all). The legacy `pdh_legacy` template can target a remote machine via username/password, but that's an opt-in mode. | [spec](../iis/assets/configuration/spec.yaml), [upstream](https://learn.microsoft.com/en-us/iis/web-hosting/web-server-for-shared-hosting/installing-internet-information-services-iis-7) |
| Infiniband (`infiniband`) | — | other — The Infiniband check has no required configuration fields and no network endpoint. It walks `/sys/class/infiniband` on the Agent host (or `/host/sys/class/infiniband` when running containerised), enumerates IB devices and ports, and reads counter/hw_counter/status sysfs files directly. There is no host/port/URL/credential to discover - presence of the `/sys/class/infiniband` directory with at least one device is the trigger. This is the same shape as btrfs: a host-local kernel-surface check, pattern H/L analogue with no binary or network involved. Auto-config can emit the integration with default-empty instances on any Agent host where the sysfs path exists. | [spec](../infiniband/assets/configuration/spec.yaml), [check](../infiniband/datadog_checks/infiniband/check.py), [readme](../infiniband/README.md) |
| Karpenter (`karpenter`) | `openmetrics_endpoint` | openmetrics-port-scan — Karpenter is a Kubernetes cluster autoscaler controller that exposes Prometheus metrics on a fixed `/metrics` path. The spec inherits `template: instances/openmetrics` and the check is a thin `OpenMetricsBaseCheckV2` subclass with a static `METRIC_MAP`. The example endpoint is `http://localhost:8080/metrics`; upstream Karpenter documentation also describes a single canonical Prometheus metrics port (8080 for older releases, 8000 for v1+; both follow `/metrics`). No authentication is required for the in-cluster Prometheus endpoint. Pattern A applies — single canonical OpenMetrics endpoint, no credentials, port is well-known per the upstream docs. | [spec](../karpenter/assets/configuration/spec.yaml), [check](../karpenter/datadog_checks/karpenter/check.py), [readme](../karpenter/README.md), [upstream](https://karpenter.sh/docs/reference/metrics/) |
| KEDA (`keda`) | `openmetrics_endpoint` | openmetrics-port-scan — KedaCheck inherits OpenMetricsBaseCheckV2 and only requires an openmetrics_endpoint. KEDA's components (admissions controller, metrics API server, operator) each expose Prometheus metrics on a fixed `/metrics` path on port 8080, so the discovery layer just needs to scan that port on each KEDA pod. No credentials are needed and the path is canonical, matching the OpenMetrics-on-a-known-port pattern. | [spec](../keda/assets/configuration/spec.yaml), [check](../keda/datadog_checks/keda/check.py), [readme](../keda/README.md), [upstream](https://keda.sh/docs/2.16/integrations/prometheus/) |
| Kong (`kong`) | — | openmetrics-port-scan — Modern Kong exposes Prometheus/OpenMetrics on the Admin API at `http://<host>:8001/metrics` (Prometheus plugin) — the recommended path documented in the README. The legacy `kong_status_url` mode hits `http://<host>:8001/status/` on the same Admin API, also unauthenticated by default. Both endpoints share the same well-known port (8001) and don't require credentials in the default deployment. The check dispatches to `KongCheck` (OpenMetricsBaseCheckV2) when `openmetrics_endpoint` is set, otherwise to the legacy status parser. An OpenMetrics port scan on 8001 with `/metrics` is the canonical generic case. | [spec](../kong/assets/configuration/spec.yaml), [check](../kong/datadog_checks/kong/check.py), [check](../kong/datadog_checks/kong/kong.py), [readme](../kong/README.md), [upstream](https://docs.konghq.com/hub/kong-inc/prometheus/) |
| KrakenD (`krakend`) | `openmetrics_endpoint` | openmetrics-port-scan — KrakendCheck inherits OpenMetricsBaseCheckV2 and only needs `openmetrics_endpoint`. KrakenD's documented OpenTelemetry-Prometheus exporter listens on port 9090 at `/metrics` (per upstream docs and the integration README and tests), with no auth. The remaining options (`go_metrics`, `process_metrics`) are content filters, not endpoint-resolution inputs. Discovery only needs to find a TCP/9090 service responding to a `/metrics` GET. | [spec](../krakend/assets/configuration/spec.yaml), [check](../krakend/datadog_checks/krakend/check.py), [readme](../krakend/README.md), [upstream](https://www.krakend.io/docs/telemetry/opentelemetry/) |
| Kubernetes API server metrics (`kube_apiserver_metrics`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single `/metrics` path on the kube-apiserver. The shipped auto_conf.yaml already enumerates the canonical secure ports (`possible_prometheus_urls: [https://%%host%%:6443/metrics, https://%%host%%:8443/metrics]`) — i.e. discovery is reduced to picking the first responding port. Authentication uses the in-cluster ServiceAccount bearer token mounted at `/var/run/secrets/kubernetes.io/serviceaccount/token`, supplied automatically by the Agent (pattern O), not by user input. This matches the OpenMetrics-port-scan generic shape. | [spec](../kube_apiserver_metrics/assets/configuration/spec.yaml), [auto_conf](../kube_apiserver_metrics/datadog_checks/kube_apiserver_metrics/data/auto_conf.yaml), [check](../kube_apiserver_metrics/datadog_checks/kube_apiserver_metrics/kube_apiserver_metrics.py), [upstream](https://kubernetes.io/docs/concepts/cluster-administration/system-metrics/) |
| Kubernetes Controller Manager (`kube_controller_manager`) | `prometheus_url` | openmetrics-port-scan — kube-controller-manager exposes Prometheus metrics on a fixed `/metrics` path. The auto_conf already enumerates the canonical port pair (10257 secure / 10252 insecure) and prefers the first that responds. Bearer-token auth is needed for the secure 10257 endpoint in modern clusters, but the Agent's in-cluster service-account token (mounted at `/var/run/secrets/kubernetes.io/serviceaccount/token`) is supplied via `bearer_token_auth: tls_only` rather than user-supplied credentials, so the only thing discovery has to find is the host/port. This matches the OpenMetrics port-scan pattern. | [spec](../kube_controller_manager/assets/configuration/spec.yaml), [auto_conf](../kube_controller_manager/datadog_checks/kube_controller_manager/data/auto_conf.yaml), [check](../kube_controller_manager/datadog_checks/kube_controller_manager/kube_controller_manager.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kube-controller-manager/) |
| Kube DNS (`kube_dns`) | `prometheus_endpoint` | openmetrics-port-scan — kube-dns exposes Prometheus metrics on the fixed path `/metrics` over its dnsmasq metrics port (10055 by default). The shipped auto_conf templates `http://%%host%%:10055/metrics`, which is the upstream default. No credentials are required — the metrics port is unauthenticated by design. The integration is functionally identical in shape to coredns/etcd: one URL with one canonical port, OpenMetrics on the wire. | [spec](../kube_dns/assets/configuration/spec.yaml), [auto_conf](../kube_dns/datadog_checks/kube_dns/data/auto_conf.yaml), [check](../kube_dns/datadog_checks/kube_dns/kube_dns.py), [upstream](https://github.com/kubernetes/dns/blob/master/docs/kube-dns.md) |
| Kube metrics server (`kube_metrics_server`) | `prometheus_url` | openmetrics-port-scan — kube-metrics-server is a Kubernetes API extension server that exposes Prometheus metrics on a fixed path (/metrics) over HTTPS on port 443. The spec uses the openmetrics_legacy template and the example URL is https://%%host%%:443/metrics, indicating a single canonical endpoint. The check inherits OpenMetricsBaseCheck. Discovery reduces to scraping https://<pod_ip>:443/metrics on a discovered metrics-server pod. | [spec](../kube_metrics_server/assets/configuration/spec.yaml), [check](../kube_metrics_server/datadog_checks/kube_metrics_server/kube_metrics_server.py) |
| Kube Proxy (`kube_proxy`) | `prometheus_url` | openmetrics-port-scan — kube-proxy exposes Prometheus-format metrics on the fixed path `/metrics` at TCP port 10249 (the `metricsBindAddress` default), and a health endpoint at `/healthz` on port 10256. The check is an OpenMetricsBaseCheck subclass that hardcodes `http://localhost:10249/metrics` as its default and even derives the health URL from the prometheus URL automatically. Discovery just needs to find a kube-proxy pod and probe port 10249. This is the standard in-cluster control-plane pattern (O) — when authentication is required, the Agent's pod ServiceAccount token is auto-injected; in the default insecure setup, no credentials are needed at all. | [spec](../kube_proxy/assets/configuration/spec.yaml), [check](../kube_proxy/datadog_checks/kube_proxy/kube_proxy.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kube-proxy/) |
| Kubelet (`kubelet`) | — | other — The kubelet integration runs against the Agent's own host node. The shipped `conf.yaml.default` declares `ad_identifiers: [_kubelet]`, which is a built-in Autodiscovery listener — the Agent itself populates the kubelet URL, port, and bearer token via `kubeutil.get_connection_info()`, then `KubeletCredentials` injects the in-cluster ServiceAccount token. None of the per-instance fields are required: the user does not supply a host, port, or credentials. Per pattern O (in-cluster bearer-token auth) plus the agent-managed `_kubelet` listener, this is `generic`. No external network probe is needed because the Agent's runtime auto-detects the local kubelet, so the auto-config method is `other` (host-local detection rather than port scan). | [spec](../kubelet/assets/configuration/spec.yaml), [auto_conf](../kubelet/datadog_checks/kubelet/data/conf.yaml.default), [check](../kubelet/datadog_checks/kubelet/kubelet.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kubelet/) |
| Kubernetes Cluster Autoscaler (`kubernetes_cluster_autoscaler`) | `openmetrics_endpoint` | openmetrics-port-scan — The check inherits `OpenMetricsBaseCheckV2` and exposes a single required `openmetrics_endpoint`. Cluster Autoscaler serves Prometheus metrics on `/metrics` at the well-known port 8085 (per upstream and the spec's example). No authentication is required for the metrics endpoint, so a generic OpenMetrics port scan against the autoscaler pod can synthesize the URL. | [spec](../kubernetes_cluster_autoscaler/assets/configuration/spec.yaml), [check](../kubernetes_cluster_autoscaler/datadog_checks/kubernetes_cluster_autoscaler/check.py), [readme](../kubernetes_cluster_autoscaler/README.md), [upstream](https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/proposals/metrics.md) |
| Kubernetes State (`kubernetes_state`) | `kube_state_url` | openmetrics-port-scan — The legacy `kubernetes_state` check scrapes kube-state-metrics over Prometheus on port 8080 at `/metrics`. The auto_conf template (`ad_identifiers: [kube-state-metrics]`) targets the kube-state-metrics container, sets `kube_state_url: http://%%host%%:8080/metrics`, and requires no credentials — a classic OpenMetrics port-scan. The check inherits `OpenMetricsBaseCheck`. | [spec](../kubernetes_state/assets/configuration/spec.yaml), [auto_conf](../kubernetes_state/datadog_checks/kubernetes_state/data/auto_conf.yaml), [check](../kubernetes_state/datadog_checks/kubernetes_state/kubernetes_state.py), [upstream](https://github.com/kubernetes/kube-state-metrics) |
| Kubernetes State Core (`kubernetes_state_core`) | — | other — The Kubernetes State Core check is a Cluster-Agent built-in (Go-implemented, the `datadog_checks/kubernetes_state_core` directory ships only `conf.yaml.example` — no Python check). Per the README, it embeds the kube-state-metrics logic inside the Datadog Cluster Agent, so there is no external Prometheus endpoint to discover and no required URL/credentials in the spec. Discovery is purely 'is the Cluster Agent running?' which the Cluster Agent answers for itself via its own ServiceAccount and Kubernetes API access. Treat as `generic` with method `other` because no network probe is required. | [spec](../kubernetes_state_core/assets/configuration/spec.yaml), [readme](../kubernetes_state_core/README.md), [upstream](https://docs.datadoghq.com/integrations/kubernetes_state_core/) |
| KubeVirt API (`kubevirt_api`) | `kubevirt_api_metrics_endpoint` | openmetrics-port-scan — The KubeVirt API `virt-api` pod exposes Prometheus metrics over HTTPS on port 443 at `/metrics` (per README and tests). The check inherits `OpenMetricsBaseCheckV2` and additionally calls the Kubernetes API to enumerate VirtualMachine / VirtualMachineInstance custom resources. Both authentications are auto-injected: the metrics endpoint is reachable in-cluster and TLS verification is typically disabled (self-signed virt-api cert), and the Kubernetes API client uses `load_incluster_config()` with the Agent's ServiceAccount token (per pattern O). Spec marks no field as strictly required, but the check assumes `kubevirt_api_metrics_endpoint` is set. The endpoint URL is canonical (`https://<virt-api-pod>:443/metrics`), so an OpenMetrics port scan against the autodiscovered virt-api pod resolves it. | [spec](../kubevirt_api/assets/configuration/spec.yaml), [check](../kubevirt_api/datadog_checks/kubevirt_api/check.py), [check](../kubevirt_api/datadog_checks/kubevirt_api/kube_client.py), [readme](../kubevirt_api/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |
| KubeVirt Controller (`kubevirt_controller`) | `kubevirt_controller_metrics_endpoint` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheckV2 and points at the KubeVirt Controller's `/metrics` Prometheus endpoint, which it exposes on the pod at a fixed path (port 8443/443). The recommended autodiscovery pattern in the README uses `https://%%host%%:%%port%%/metrics`, so once the pod is identified, host+port+/metrics is sufficient. `kube_namespace` and `kube_pod_name` are tag inputs supplied automatically by Autodiscovery template variables — they don't drive endpoint resolution. No user-supplied credentials are needed; TLS verify is typically disabled or relies on cluster CA. This is the canonical OpenMetrics port-scan case. | [spec](../kubevirt_controller/assets/configuration/spec.yaml), [check](../kubevirt_controller/datadog_checks/kubevirt_controller/check.py), [readme](../kubevirt_controller/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |
| KubeVirt Handler (`kubevirt_handler`) | `kubevirt_handler_metrics_endpoint` | openmetrics-port-scan — Mirrors kubevirt_controller: the check inherits OpenMetricsBaseCheckV2 and scrapes the KubeVirt Handler (`virt-handler`) DaemonSet pod's `/metrics` endpoint on a fixed path. Recommended autodiscovery uses `https://%%host%%:%%port%%/metrics`. `kube_namespace`/`kube_pod_name` are template-variable tag inputs, not discovery inputs. No user credentials are needed (TLS verify typically off). Canonical OpenMetrics port-scan. | [spec](../kubevirt_handler/assets/configuration/spec.yaml), [check](../kubevirt_handler/datadog_checks/kubevirt_handler/check.py), [readme](../kubevirt_handler/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |
| Kuma (`kuma`) | `openmetrics_endpoint` | openmetrics-port-scan — Kuma's control plane exposes Prometheus-formatted metrics at `/metrics` on the well-known port 5680. The check inherits OpenMetricsBaseCheckV2 and the spec template (`instances/openmetrics`) only needs a single URL. The README's autodiscovery snippet uses `http://%%host%%:5680/metrics`. No authentication is required for the control plane metrics endpoint. Pattern A (OpenMetrics on a known port). | [spec](../kuma/assets/configuration/spec.yaml), [check](../kuma/datadog_checks/kuma/check.py), [readme](../kuma/README.md), [upstream](https://kuma.io/docs/latest/production/monitoring/prometheus/) |
| Kyoto Tycoon (`kyototycoon`) | `report_url` | http-path-probe — Kyoto Tycoon serves an HTTP RPC interface; the check issues a single GET against the canonical `/rpc/report` endpoint on the well-known port 1978 and parses its tab-separated response. No authentication is required. The shipped auto_conf.yaml already templates `http://%%host%%:1978/rpc/report`, confirming a single canonical URL. Pattern B (single canonical HTTP path). | [spec](../kyototycoon/assets/configuration/spec.yaml), [auto_conf](../kyototycoon/datadog_checks/kyototycoon/data/auto_conf.yaml), [check](../kyototycoon/datadog_checks/kyototycoon/kyototycoon.py), [readme](../kyototycoon/README.md) |
| Kyverno (`kyverno`) | `openmetrics_endpoint` | openmetrics-port-scan — Kyverno controllers (Admissions, Backup, Cleanup, Reports) each expose Prometheus-formatted metrics at `/metrics` on the well-known port 8000. The check inherits OpenMetricsBaseCheckV2 with no extra config beyond the standard openmetrics template; the README's autodiscovery snippet is `http://%%host%%:8000/metrics`. No authentication is required. Pattern A (OpenMetrics on a known port). | [spec](../kyverno/assets/configuration/spec.yaml), [check](../kyverno/datadog_checks/kyverno/check.py), [readme](../kyverno/README.md), [upstream](https://kyverno.io/docs/monitoring/) |
| Lighttpd (`lighttpd`) | `lighttpd_status_url` | http-path-probe — Lighttpd's `mod_status` exposes a single canonical status page at `/server-status` with the `?auto` (or `?format=plain` on Lighttpd 2) query string for machine-readable output. The check itself even auto-appends the suffix variant when the first attempt returns no metrics. A generic HTTP path probe against `http://%%host%%/server-status?auto` on port 80 (or whatever the admin port is) is sufficient. No credentials are required by default. | [spec](../lighttpd/assets/configuration/spec.yaml), [check](../lighttpd/datadog_checks/lighttpd/lighttpd.py), [upstream](https://redmine.lighttpd.net/projects/1/wiki/Docs_ModStatus) |
| Linkerd (`linkerd`) | — | openmetrics-port-scan — Linkerd's data-plane proxy exposes Prometheus-format metrics on its admin port (default 9990) at the fixed path /admin/metrics/prometheus. The check inherits OpenMetricsBaseCheckV2 with a hard-coded metric map; once the endpoint URL is supplied, no auth or further configuration is needed. The spec ships an example openmetrics_endpoint of http://localhost:9990/admin/metrics/prometheus, which is the universal Linkerd admin path. Discovery is a generic 'GET /admin/metrics/prometheus on port 9990 and verify Prometheus-format response' probe. | [spec](../linkerd/assets/configuration/spec.yaml), [check](../linkerd/datadog_checks/linkerd/check.py), [upstream](https://linkerd.io/2/reference/proxy-metrics/) |
| Linux Proc Extras (`linux_proc_extras`) | — | other — The check reads files under the Agent host's `procfs_path` (defaults to `/proc`): `sys/fs/inode-nr`, `stat`, `sys/kernel/random/entropy_avail`, `<pid>/status`, and optionally `interrupts`. There is no required configuration field, no host/port/URL/credential, and no upstream service to probe. Discovery is a host-local presence check — "is /proc available?" — which is true on every Linux host. The only optional knob (`include_interrupt_metrics`) is a cardinality toggle, not an endpoint selector. Auto-config can therefore deploy the integration unconditionally on Linux hosts with its default empty instance. Direct analogue of btrfs (pattern: host-local generic, no network probe, no binary subprocess). | [spec](../linux_proc_extras/assets/configuration/spec.yaml), [check](../linux_proc_extras/datadog_checks/linux_proc_extras/linux_proc_extras.py), [readme](../linux_proc_extras/README.md) |
| LiteLLM (`litellm`) | — | openmetrics-port-scan — LiteLLM Proxy exposes Prometheus metrics on a single canonical path `/metrics` (per upstream docs at https://docs.litellm.ai/docs/proxy/prometheus) at the proxy's HTTP port — `4000` by default in the spec example and the project's documented default. The check inherits `OpenMetricsBaseCheckV2`. The only override is `openmetrics_endpoint.required: false`, so the spec marks it optional, but the example reflects the universal upstream default. The optional `litellm_health_endpoint` augments OpenMetrics with a JSON `/health` poll at the same host:port. This is a textbook pattern A ("OpenMetrics on a known port") with one extra optional auxiliary URL on the same origin — port-scan for Prometheus on 4000/metrics is sufficient. | [spec](../litellm/assets/configuration/spec.yaml), [check](../litellm/datadog_checks/litellm/check.py), [readme](../litellm/README.md), [upstream](https://docs.litellm.ai/docs/proxy/prometheus) |
| MapReduce (`mapreduce`) | `resourcemanager_uri`, `cluster_name` | http-path-probe — MapReduce is the YARN ResourceManager check's MapReduce-flavoured sibling: the check hits the same REST API root (`ws/v1/cluster`, `ws/v1/cluster/apps`, `ws/v1/mapreduce/jobs`) on the ResourceManager (default port 8088) and then follows the application master URLs. The default deployment leaves the REST API unauthenticated. `cluster_name` is marked required in the spec but is a freeform tag (per the procedure rule, that doesn't force impossible classification). A single HTTP probe at `http://%%host%%:8088/ws/v1/cluster/metrics` is sufficient to identify the integration target, mirroring the YARN classification. | [spec](../mapreduce/assets/configuration/spec.yaml), [check](../mapreduce/datadog_checks/mapreduce/mapreduce.py), [upstream](https://hadoop.apache.org/docs/current/hadoop-yarn/hadoop-yarn-site/ResourceManagerRest.html) |
| Memcached (`mcache`) | `url` | tcp-banner-probe — Memcached speaks a well-known text/binary protocol on port 11211; the check connects via bmemcached and issues stats commands. Required configuration is just url (host) and an optional port that defaults to 11211. Authentication via username/password is supported but optional and rarely deployed (default Memcached has no auth). The shipped auto_conf.yaml templates url=%%host%% with port 11211, confirming Autodiscovery already works generically. Discovery via TCP banner probe matches procedure pattern C. | [spec](../mcache/assets/configuration/spec.yaml), [check](../mcache/datadog_checks/mcache/mcache.py), [auto_conf](../mcache/datadog_checks/mcache/data/auto_conf.yaml), [readme](../mcache/README.md) |
| Mesos Master (`mesos_master`) | `url` | http-path-probe — Mesos Master exposes an unauthenticated HTTP REST API on the well-known port 5050. The check GETs /state (cluster topology, version) and /metrics/snapshot (counter snapshot) and parses JSON. Both endpoints are universal across Mesos versions and require no credentials in the default deployment. A generic http-path-probe to GET /state on port 5050 (verifying a JSON body with a 'version' key and Mesos-shaped fields like 'leader' / 'activated_slaves') is sufficient. DC/OS and HTTP-auth-enabled clusters need an auth_token, but those are not the typical case. | [spec](../mesos_master/assets/configuration/spec.yaml), [check](../mesos_master/datadog_checks/mesos_master/mesos_master.py), [readme](../mesos_master/README.md), [upstream](http://mesos.apache.org/documentation/latest/endpoints/master/state/) |
| Mesos Slave (`mesos_slave`) | `url` | http-path-probe — Mesos Slave (agent) exposes the same unauthenticated HTTP REST API shape as Mesos Master, on the well-known port 5051. The check GETs /state (slave-side topology, version, frameworks/executors/tasks) and /metrics/snapshot (counter snapshot) and parses JSON. Both paths are universal across Mesos versions and require no credentials in the default deployment. A generic http-path-probe to GET /state on port 5051 (verifying a JSON body with a 'version' key plus slave-shaped fields like 'id' and 'frameworks') is sufficient. DC/OS deployments need an auth_token, but those are the minority case (mirroring mesos_master). | [spec](../mesos_slave/assets/configuration/spec.yaml), [check](../mesos_slave/datadog_checks/mesos_slave/mesos_slave.py), [readme](../mesos_slave/README.md), [upstream](http://mesos.apache.org/documentation/latest/endpoints/) |
| Milvus (`milvus`) | `openmetrics_endpoint` | openmetrics-port-scan — Milvus is a vector database that natively exposes Prometheus metrics on a fixed, well-known endpoint. The check inherits OpenMetricsBaseCheckV2 and ships only the standard `instances/openmetrics` template with `openmetrics_endpoint` defaulting to http://localhost:9091/metrics (Milvus' documented Prometheus port and path). No credentials are required and the path is a single canonical /metrics — Pattern A (OpenMetrics on a known port). A generic OpenMetrics port scan against 9091 with path /metrics, validating Prometheus exposition format with milvus_*-prefixed series, is sufficient to autodiscover. | [spec](../milvus/assets/configuration/spec.yaml), [check](../milvus/datadog_checks/milvus/check.py), [readme](../milvus/README.md), [upstream](https://milvus.io/docs/monitor.md) |
| NGINX Ingress Controller (`nginx_ingress_controller`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint at http://%%host%%:10254/metrics. The /metrics endpoint exposed by the kubernetes/ingress-nginx controller on port 10254 is universal, unauthenticated, and well documented. Discovery reduces to probing that port + path. | [spec](../nginx_ingress_controller/assets/configuration/spec.yaml), [check](../nginx_ingress_controller/datadog_checks/nginx_ingress_controller/nginx_ingress_controller.py), [upstream](https://kubernetes.github.io/ingress-nginx/user-guide/monitoring/) |
| Pulsar (`pulsar`) | `openmetrics_endpoint` | openmetrics-port-scan — Pulsar's check inherits `OpenMetricsBaseCheckV2` and the spec uses `template: instances/openmetrics` with a single canonical endpoint at `http://%%host%%:8080/metrics` (the broker's web service port). The check's `get_default_config` hard-codes the same URL. No authentication is required for the metrics endpoint by default. This is the standard Pattern A shape — single OpenMetrics endpoint on a well-known port. | [spec](../pulsar/assets/configuration/spec.yaml), [check](../pulsar/datadog_checks/pulsar/check.py), [readme](../pulsar/README.md), [upstream](https://pulsar.apache.org/docs/admin-api-overview/) |
| Redis (`redisdb`) | `host`, `port` | tcp-banner-probe — Redis answers a banner over TCP and the default port 6379 is universal. A generic TCP/banner probe (PING / CLIENT INFO) is sufficient for the unauthenticated case. Authenticated deployments need a password, which is out of scope for generic discovery, but the existing autodiscovery template covers them once the password is provided. | [spec](../redisdb/assets/configuration/spec.yaml), [auto_conf](../redisdb/datadog_checks/redisdb/data/auto_conf.yaml) |
| Riak (`riak`) | `url` | http-path-probe — Riak's HTTP REST API exposes a singular, well-known stats endpoint at `/stats` on the default HTTP port 8098. The shipped auto_conf.yaml templates `url: http://%%host%%:8098/stats` and the check simply GETs that URL and parses the JSON. No authentication is required by default, and there is only one canonical path. Pattern B (single canonical HTTP path). | [spec](../riak/assets/configuration/spec.yaml), [auto_conf](../riak/datadog_checks/riak/data/auto_conf.yaml), [check](../riak/datadog_checks/riak/riak.py), [readme](../riak/README.md), [upstream](https://docs.riak.com/riak/kv/latest/developing/api/http/status.1.html) |
| Scylla (`scylla`) | `openmetrics_endpoint` | openmetrics-port-scan — Scylla exposes Prometheus metrics on the Scylla Prometheus exporter at the well-known port 9180 with path `/metrics`. The check inherits `OpenMetricsBaseCheckV2` (via `ScyllaCheckV2`) and the spec uses `template: instances/openmetrics` with the example `http://localhost:9180/metrics`. There is no required username/password and no Scylla-specific endpoint logic in the path. A generic openmetrics port-scan against `:9180/metrics` (matching `scylla_*` series) is sufficient for discovery. The legacy `prometheus_url` field is a deprecated alias and not required. | [spec](../scylla/assets/configuration/spec.yaml), [check](../scylla/datadog_checks/scylla/scylla.py), [check](../scylla/datadog_checks/scylla/scylla_v2.py), [readme](../scylla/README.md), [upstream](https://opensource.docs.scylladb.com/stable/operating-scylla/monitoring/monitoring-stack.html) |
| Squid (`squid`) | `name` | http-path-probe — Squid exposes its Cache Manager counters over HTTP at the single canonical path `/squid-internal-mgr/counters` on the proxy port (default 3128). The check just GETs that URL and parses the `key = value` body. `name` is the only spec-required field, but it's just an instance label (no discovery semantics). Optional `cachemgr_username`/`cachemgr_password` cover hardened deployments but are not the default; localhost-from-the-proxy access is typically unauthenticated. A generic HTTP-path probe on port 3128 with that one well-known path is sufficient. | [spec](../squid/assets/configuration/spec.yaml), [check](../squid/datadog_checks/squid/squid.py), [readme](../squid/README.md), [upstream](https://wiki.squid-cache.org/Features/CacheManager) |
| StatsD (`statsd`) | — | tcp-banner-probe — Despite the name, this is NOT a DogStatsD-only integration. The check opens a TCP socket to a non-Datadog Etsy/StatsD server's management interface (default port 8126), sends text commands (`health`, `stats`, `counters`, `gauges`, `timers`) and parses the responses — a simple line-protocol over TCP. No authentication, no credentials, no API keys. The README explicitly documents `host: localhost, port: 8126` as the canonical configuration and the Containerized template uses `{"host": "%%host%%", "port":"8126"}`. A generic TCP banner-style probe that sends `health\n` and looks for `health: up` or `health: down` would identify the service unambiguously. Pattern C (TCP banner / known protocol) — same shape as redisdb / memcached. | [spec](../statsd/assets/configuration/spec.yaml), [check](../statsd/datadog_checks/statsd/statsd.py), [readme](../statsd/README.md) |
| Traffic Server (`traffic_server`) | `traffic_server_url` | http-path-probe — Apache Traffic Server exposes metrics through the `stats_over_http` plugin at a single canonical path `/_stats`, returning a JSON document with a `global` key. The plugin's default port is 8080 (the same as the proxy by default; operators commonly bind it on the proxy port or remap it). The check just performs a GET and parses JSON — no credentials, no path enumeration. A generic HTTP-path probe that hits `http://%%host%%:8080/_stats` and validates the JSON shape (`global` key) is sufficient. Pattern B. | [spec](../traffic_server/assets/configuration/spec.yaml), [check](../traffic_server/datadog_checks/traffic_server/check.py), [readme](../traffic_server/README.md), [upstream](https://docs.trafficserver.apache.org/en/latest/admin-guide/plugins/stats_over_http.en.html) |
| Twemproxy (nutcracker) (`twemproxy`) | `host`, `port` | tcp-banner-probe — Twemproxy (nutcracker) exposes a stats monitoring port that emits a single JSON document immediately upon TCP connect (no request needed). The check just opens a socket to host:port, drains the response, and parses it. No authentication, no path negotiation. A generic TCP-banner probe that opens a connection and verifies the response decodes as JSON containing the well-known top-level keys (service:nutcracker, source, version, total_connections) is sufficient. The conventional stats port is 22222 per upstream documentation, although the spec example/code default is 2222. | [spec](../twemproxy/assets/configuration/spec.yaml), [check](../twemproxy/datadog_checks/twemproxy/twemproxy.py), [readme](../twemproxy/README.md), [upstream](https://github.com/twitter/twemproxy#observability) |
| Windows Service (`windows_service`) | `services` | other — The check runs on the local Windows host and queries the Service Control Manager (win32service.OpenSCManager / EnumServicesStatusEx). There is no remote network probe. The required `services` list specifies which services to monitor; the user can pass `ALL` to enumerate every registered service, in which case discovery is purely 'is this Windows?' and the check trivially populates targets from SCM. Auto-config can default the instance to `services: [ALL]` on any Windows host where the Agent is installed. | [spec](../windows_service/assets/configuration/spec.yaml), [check](../windows_service/datadog_checks/windows_service/windows_service.py) |
| YARN (`yarn`) | `resourcemanager_uri` | http-path-probe — YARN's check hits the ResourceManager REST API at well-known paths under `/ws/v1/cluster` (metrics, apps, nodes, scheduler). The ResourceManager web UI defaults to port 8088 across Hadoop versions and the `/ws/v1/cluster/metrics` JSON endpoint is universal, unauthenticated by default. A single HTTP probe to `http://%%host%%:8088/ws/v1/cluster/metrics` confirms the integration target. No credentials are required in the default mode. | [spec](../yarn/assets/configuration/spec.yaml), [check](../yarn/datadog_checks/yarn/yarn.py), [upstream](https://hadoop.apache.org/docs/current/hadoop-yarn/hadoop-yarn-site/ResourceManagerRest.html) |
| ZooKeeper (`zk`) | `host` | tcp-banner-probe — The ZooKeeper check opens a TCP socket to host:port (default 2181) and sends the `ruok`, `stat`, and `mntr` four-letter words; `ruok` returns the literal `imok` and `stat` returns a multi-line response starting with `Zookeeper version: ...`. Both make excellent banner probes — a generic discovery layer can connect to 2181, send `ruok\n`, and key off `imok`. No credentials are involved by default. TLS is optional. The 4LW interface needs to be enabled in `zoo.cfg` (`4lw.commands.whitelist`) on modern ZooKeeper, which is a deployment detail, not a config-discovery problem. | [spec](../zk/assets/configuration/spec.yaml), [check](../zk/datadog_checks/zk/zk.py), [upstream](https://zookeeper.apache.org/doc/r3.8.0/zookeeperAdmin.html#sc_4lw) |

### Custom auto-config possible

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| ActiveMQ XML (`activemq_xml`) | `url` | credentials-required — Unlike its JMX-based sibling `activemq`, this integration scrapes ActiveMQ's web console: it appends `/admin/xml/queues.jsp`, `/admin/xml/topics.jsp`, `/admin/xml/subscribers.jsp` to the user-supplied base `url` and parses the XML with `ElementTree`. The path triplet is universal for ActiveMQ Classic, the default web console port is 8161, and the check inherits the standard `instances/http` template (so basic auth is optional in the spec). However, ActiveMQ Classic's web console ships with HTTP basic auth enabled out of the box (the realm is configured in `jetty-realm.properties`, default user `admin`/`admin`), and any production deployment changes those credentials. The Prometheus-style fallback that pattern F uses for rabbitmq/haproxy does not exist here — there is only the authenticated /admin path. A network probe could detect the listening port, but extracting metrics requires credentials. | [spec](../activemq_xml/assets/configuration/spec.yaml), [check](../activemq_xml/datadog_checks/activemq_xml/activemq_xml.py), [readme](../activemq_xml/README.md), [upstream](https://activemq.apache.org/web-console) |
| Airflow (`airflow`) | `url` | http-path-probe — The `airflow` Agent check itself is an HTTP probe against the Airflow webserver (default port 8080). It first calls `/api/v1/version` to detect Airflow 2.x; on success it collects from `/api/v1/health` and `/api/v1/dags/~/dagRuns/~/taskInstances?state=running`, otherwise it falls back to the legacy 1.x endpoint `/api/experimental/test`. Discovery must therefore probe port 8080 and try the stable API path first, falling back to the experimental path. Note Airflow 2 commonly enables web auth, in which case the API is reachable but `/api/v1/health` requires no auth while task-instance endpoints do — so generic probing of `/health` works, but the full check coverage may degrade. Separately, the bulk of `airflow.*` metrics actually come from the Airflow StatsD plugin into DogStatsD; that path is configured on the Airflow side and is orthogonal to Agent-side auto-config. | [spec](../airflow/assets/configuration/spec.yaml), [check](../airflow/datadog_checks/airflow/airflow.py), [readme](../airflow/README.md), [upstream](https://airflow.apache.org/docs/apache-airflow/stable/stable-rest-api-ref.html) |
| Cassandra Nodetool (`cassandra_nodetool`) | `keyspaces` | other — The check shells out to a local `nodetool` binary (default /usr/bin/nodetool) and parses the output of `nodetool status -- <keyspace>`. There is no Agent-driven network probe; nodetool itself opens the JMX connection on port 7199. Discovery on the Agent host reduces to 'is the nodetool binary installed and runnable, and which Cassandra cluster does it point at?'. The required `keyspaces` list is operator policy (which keyspaces to monitor) and cannot be inferred from the wire without already running `nodetool` plus credentials when the JMX server is authenticated. This is the same shape as varnish/ceph/postfix (pattern L: local CLI subprocess), with the additional caveat that production JMX deployments add `username`/`password` requirements. | [spec](../cassandra_nodetool/assets/configuration/spec.yaml), [check](../cassandra_nodetool/datadog_checks/cassandra_nodetool/cassandra_nodetool.py), [readme](../cassandra_nodetool/README.md) |
| Ceph (`ceph`) | — | other — The Ceph check shells out to the local `ceph` CLI (`/usr/bin/ceph --cluster ceph status -fjson`, etc.) via `get_subprocess_output`. There is no network endpoint to probe: discovery means detecting whether the `ceph` binary is installed locally and whether the dd-agent user has permission to read the cluster admin keyring (often via `sudo`). This is a host-local presence check rather than a network probe, similar to Windows perf-counter integrations. Ceph also ships a Prometheus mgr module that the Datadog Agent could scrape generically, but that path uses the openmetrics check, not this one. Classification is custom because feasibility hinges on file-system presence and permission detection rather than any of the standard wire probes. | [spec](../ceph/assets/configuration/spec.yaml), [check](../ceph/datadog_checks/ceph/ceph.py), [upstream](https://docs.ceph.com/en/latest/mgr/prometheus/) |
| CouchDB (`couch`) | `server` | http-path-probe — CouchDB exposes a REST API on the well-known port 5984. The existing auto_conf.yaml templates the server URL as http://%%host%%:5984. The check fetches the server root for version detection then walks `/_node/*/_stats`, `/_active_tasks`, and per-database stats. CouchDB 2.x+ admin endpoints typically require basic auth (admin party is dev-only), so realistic deployments need credentials, but the version-probing root endpoint and unauthenticated read paths can confirm a CouchDB server is on the wire. Custom (not generic) because the integration probes multiple HTTP paths and the upstream supports two distinct API shapes (1.x vs 2.x cluster). | [spec](../couch/assets/configuration/spec.yaml), [auto_conf](../couch/datadog_checks/couch/data/auto_conf.yaml), [check](../couch/datadog_checks/couch/couch.py) |
| Druid (`druid`) | `url` | http-path-probe — The Druid check takes a single required `url` (the base URL of a Druid process) and issues GETs against `/status/properties` and `/status/health`. The path is universal for Druid 0.16+, but the *port* is not: Druid is a multi-process system and the user must point the check at one of several services (coordinator 8081, overlord 8090, broker 8082, historical 8083, middlemanager 8091, router 8888) and ideally instantiate one check per process. There is no canonical "Druid port". The auto-config layer would need to probe the well-known Druid HTTP ports on a host, hit `/status/properties` on each, parse `druid.service` from the JSON response to identify which component it is, and emit one instance per discovered process. No credentials are required by default (Druid's HTTP API is unauthenticated unless the operator configures `druid.auth.*` extensions), so an HTTP probe of the Druid status servlet is sufficient. | [spec](../druid/assets/configuration/spec.yaml), [check](../druid/datadog_checks/druid/druid.py), [readme](../druid/README.md), [upstream](https://druid.apache.org/docs/latest/operations/api-reference) |
| Elasticsearch (`elastic`) | `url` | http-path-probe — The check only requires a URL (default `http://localhost:9200`) — Elasticsearch's REST API is on a well-known port. However, virtually all production deployments enable xpack security and require basic auth and TLS, so a discovery layer must (a) probe the port for the ES root document, (b) detect whether security is enabled, and (c) defer to the existing auto_conf template for the credentialed path. Classification is custom because of the deployment-mode branching even though the unauthenticated case is trivial. | [spec](../elastic/assets/configuration/spec.yaml), [auto_conf](../elastic/datadog_checks/elastic/data/auto_conf.yaml), [upstream](https://www.elastic.co/guide/en/elasticsearch/reference/current/cluster.html) |
| Envoy (`envoy`) | — | http-path-probe — Envoy exposes its admin/stats interface on a listener whose port is operator-configured (commonly 9901, but the shipped example uses 80, and in service-mesh deployments it's typically 15000 for Istio sidecars or 8001/8081 in upstream samples). The integration supports two collection paths in one instance schema: the modern OpenMetrics path `/stats/prometheus` (preferred, OpenMetricsBaseCheckV2) and the legacy `/stats` admin endpoint parsed by the in-tree parser. Discovery must therefore probe candidate admin ports and try the Prometheus path first, falling back to `/stats`. Both paths are unauthenticated by default. This is the dual-mode pattern (procedure pattern F): doable, but not generic — it needs Envoy-specific port candidates and path order. | [spec](../envoy/assets/configuration/spec.yaml), [check](../envoy/datadog_checks/envoy/check.py), [readme](../envoy/README.md), [upstream](https://www.envoyproxy.io/docs/envoy/latest/operations/admin) |
| GitLab (`gitlab`) | — | http-path-probe — GitLab exposes Prometheus metrics on the main app at `/-/metrics`, but the endpoint is IP-allowlisted by default (`monitoring.ip_whitelist` in gitlab.rb) — the Agent host must be allowed before the URL works. Additionally there are component-specific endpoints (Gitaly on 9236, sidekiq exporter, postgres exporter, etc.) which the check supports via separate fields. The legacy `prometheus_url` and the v2 `openmetrics_endpoint` paths trigger different implementations (gitlab.py vs gitlab_v2.py). Auto-config must probe `/-/metrics` and degrade gracefully when the allowlist blocks the Agent; the `api_token` for version metadata is optional but typically required in production. | [spec](../gitlab/assets/configuration/spec.yaml), [check](../gitlab/datadog_checks/gitlab/gitlab.py), [check](../gitlab/datadog_checks/gitlab/gitlab_v2.py), [upstream](https://docs.gitlab.com/ee/administration/monitoring/prometheus/gitlab_metrics.html) |
| GitLab Runner (`gitlab_runner`) | `gitlab_url`, `prometheus_endpoint`, `allowed_metrics` | openmetrics-port-scan — GitLab Runner's prometheus listener (default `:9252/metrics`) is unauthenticated and could be discovered with a generic OpenMetrics port-scan. However the check also requires `gitlab_url` (a separate connectivity probe to the GitLab master, sometimes with `?token=<TOKEN>` for CE deployments) and `allowed_metrics` (an init_config metric-allowlist with no usable default — the spec example is the canonical list but it's marked required). Discovery layer must (a) probe the runner's prometheus port and (b) inject a default allowlist; the gitlab_url field is best-effort and tokens are out-of-band. | [spec](../gitlab_runner/assets/configuration/spec.yaml), [check](../gitlab_runner/datadog_checks/gitlab_runner/gitlab_runner.py), [upstream](https://docs.gitlab.com/runner/monitoring/) |
| GlusterFS (`glusterfs`) | — | other — The GlusterFS check shells out to the local `gstatus` binary (shipped with the Agent at `<run_path>/embedded/sbin/gstatus`) via `subprocess.run`, parses the JSON output, and emits cluster/volume/brick metrics. There is no remote host or port — discovery means "is gstatus installed and runnable on this host, and is glusterd present?". A passwordless sudoers entry for `dd-agent` is also required. This matches Pattern L (local CLI subprocess), analogous to varnish/ceph/postfix. | [spec](../glusterfs/assets/configuration/spec.yaml), [check](../glusterfs/datadog_checks/glusterfs/check.py), [readme](../glusterfs/README.md) |
| HAProxy (`haproxy`) | — | http-path-probe — HAProxy supports two collection modes: a Prometheus exporter (HAProxy 2+ exposes `/metrics` on a configurable port, often 8404) which is unauthenticated and OpenMetrics-compatible, and the legacy stats page (`/admin?stats` or similar) which typically needs HTTP basic auth. A discovery layer must probe `/metrics` first and fall back to the stats page; classification is custom because of the multi-mode logic, but the OpenMetrics path is the easy case. | [spec](../haproxy/assets/configuration/spec.yaml), [upstream](https://www.haproxy.com/documentation/haproxy-configuration-tutorials/metrics-and-observability/prometheus/) |
| HTTP (`http_check`) | `name`, `url` | other — http_check is a synthetic / user-intent probe (pattern K). The check accepts an arbitrary user-nominated URL and issues an HTTP request, validating status codes, response body content, and optionally the TLS certificate. There is no upstream service running alongside the Agent to discover - the user picks an arbitrary remote target plus a freeform `name` label. The required `url` field captures intent, not a discoverable property: any URL on the public internet (or internal network) is a valid configuration, and the auto-config layer cannot guess which endpoints the operator wants synthetic monitoring on. Once the URL is known, the probe itself is generic (any HTTP server), but the missing input is the user's choice of target. | [spec](../http_check/assets/configuration/spec.yaml), [check](../http_check/datadog_checks/http_check/http_check.py), [readme](../http_check/README.md) |
| Impala (`impala`) | `service_type`, `openmetrics_endpoint` | http-path-probe — Impala's check is OpenMetricsBaseCheckV2 hitting `/metrics_prometheus` on each daemon's debug HTTP server. No credentials are needed for the Prometheus endpoint. However, the integration is a topology multiplexer (pattern S): `service_type` is required and selects between three distinct backend roles (daemon=25000, statestore=25010, catalog=25020), each on a different port and emitting a different metric set. Discovery must probe all three known ports for `/metrics_prometheus` and tag the resulting instance with the matching service_type. The path is non-standard enough (not `/metrics`) that a generic OpenMetrics port-scan won't suffice — it needs the specific path. | [spec](../impala/assets/configuration/spec.yaml), [check](../impala/datadog_checks/impala/check.py), [readme](../impala/README.md) |
| Istio (`istio`) | — | http-path-probe — Istio exposes Prometheus metrics from multiple distinct components: the proxy (sidecar) at :15020/stats/prometheus, the istiod control plane at :15014/metrics, plus ambient-mode ztunnel and waypoint (also :15020). The check requires at least one endpoint depending on mode (sidecar vs ambient), and the shipped auto_conf.yaml is tied to ad_identifiers=[proxyv2, proxyv2-rhel8] and templates istio_mesh_endpoint=http://%%host%%:15020/stats/prometheus. There is also a v1/v2 OpenMetrics split (use_openmetrics) and a deprecated istio_mesh_endpoint vs istiod_endpoint distinction. Discovery requires probing multiple paths, ports, and modes plus selecting the correct namespace, so it is integration-specific (custom). | [spec](../istio/assets/configuration/spec.yaml), [check](../istio/datadog_checks/istio/check.py), [auto_conf](../istio/datadog_checks/istio/data/auto_conf.yaml), [readme](../istio/README.md) |
| Kafka Consumer (`kafka_consumer`) | `kafka_connect_str` | other — The check connects to a Kafka broker bootstrap list (default port 9092 for plaintext, 9093 for SSL/SASL). Strictly per spec only `kafka_connect_str` is required, so unauthenticated localhost setups would discover via a Metadata API request. In practice, production Kafka uses SASL/PLAIN, SASL/SCRAM, or mTLS, all of which need credentials or certs that aren't on the wire. A discovery layer must probe the bootstrap port and either (a) succeed with PLAINTEXT or (b) note the protocol mismatch and require credentials. | [spec](../kafka_consumer/assets/configuration/spec.yaml), [upstream](https://kafka.apache.org/protocol) |
| Kube Scheduler (`kube_scheduler`) | `prometheus_url` | http-path-probe — kube-scheduler exposes Prometheus metrics on /metrics, but the port and scheme have changed between Kubernetes versions: legacy clusters expose unauthenticated http on :10251, while modern (1.13+) clusters expose authenticated https on :10259 and require a service-account bearer token. The shipped auto_conf.yaml encodes this with possible_prometheus_urls listing four URL/port/scheme combinations the check tries in order. Authentication is automated via the in-cluster service-account token mounted at the standard path with bearer_token_auth=tls_only, so the Agent running in-cluster needs no user-supplied credential. Discovery requires the probe to walk the four candidates and respect TLS/auth quirks. | [spec](../kube_scheduler/assets/configuration/spec.yaml), [check](../kube_scheduler/datadog_checks/kube_scheduler/kube_scheduler.py), [auto_conf](../kube_scheduler/datadog_checks/kube_scheduler/data/auto_conf.yaml) |
| Kubeflow (`kubeflow`) | `openmetrics_endpoint` | http-path-probe — KubeflowCheck inherits OpenMetricsBaseCheckV2 with a single `openmetrics_endpoint` field, but Kubeflow is a multi-component umbrella (pipelines / `ml-pipeline`, kserve, katib, plus profile/notebook/training controllers). Each component runs in its own pod and exposes Prometheus on its own port — the README documents 9090 generically and points users at kube-prometheus-stack ServiceMonitors per component, with no single canonical port. Discovery would need to enumerate the Kubeflow components in the cluster and probe each pod's metrics port, so it is not handled by a single integration-agnostic port scan. Multiple plausible endpoints under one user-facing field is the pattern G / kubeflow-multi-component shape. | [spec](../kubeflow/assets/configuration/spec.yaml), [check](../kubeflow/datadog_checks/kubeflow/check.py), [readme](../kubeflow/README.md) |
| LPARStats (`lparstats`) | — | other — The LPARStats check shells out to the AIX `lparstat` CLI (`/usr/bin/lparstat`, optionally via `sudo`) and parses its tabular output for memory, hypervisor, and SPURR processor utilization metrics. There is no network endpoint, no credential, and no required field — discovery is purely host-local: "is this an AIX LPAR with `lparstat` runnable?". This is pattern L ("Local CLI subprocess"): the Agent host either has the binary installed (AIX-only) or it doesn't. Several optional sub-collectors (`memory_entitlements`, `hypervisor`) require root or a sudoers entry for `dd-agent`, which is an environmental privilege constraint rather than a credential the auto-config layer could discover. | [spec](../lparstats/assets/configuration/spec.yaml), [check](../lparstats/datadog_checks/lparstats/lparstats.py), [readme](../lparstats/README.md) |
| Lustre (`lustre`) | — | other — The Lustre check shells out to local Lustre administrative binaries — `lctl` (default `/usr/sbin/lctl`), `lnetctl` (`/usr/sbin/lnetctl`), and `lfs` (`/usr/bin/lfs`) — to enumerate devices, parse params, collect LNET stats, and (on clients) read changelogs. There is no network endpoint and no credential. All instance fields are optional with sensible defaults: `node_type` is auto-detected from `lctl dl` output (mds if any `mdt`, oss if any `ost`, otherwise client), and `filesystems` defaults to discovery via `lctl`. Discovery is host-local: "are the Lustre user-space binaries installed and is this host a Lustre client/MDS/OSS?". This matches pattern L (Local CLI subprocess), with the additional wrinkle that some collection paths (changelogs, MDS/OSS jobstats) require root or a registered changelog user — environmental constraints, not credentials to discover. | [spec](../lustre/assets/configuration/spec.yaml), [check](../lustre/datadog_checks/lustre/check.py), [readme](../lustre/README.md) |
| Marathon (`marathon`) | `url` | http-path-probe — The check GETs Marathon REST API paths (`/v2/apps`, `/v2/deployments`, `/v2/queue`) under a single user-supplied `url`. The Marathon master listens on a well-known port (8080 for HTTP, 8443 for HTTPS), so a port-scan + path probe of `/v2/info` or `/v2/apps` could identify Marathon distinctively (the JSON shape is unambiguous). However, two complications push this into custom territory: (1) DC/OS deployments require ACS authentication via a separate `acs_url` plus username/password (or a richer `auth_token` reader/writer config) — discovery would have to detect ACS and follow the login dance; (2) self-hosted Marathon masters often run behind basic auth or a reverse proxy. The unauthenticated localhost case works for development and matches pattern E (auth-optional in spec, often gated in production). Without ACS/auth credentials the discovery layer can identify the endpoint but cannot complete the connection. | [spec](../marathon/assets/configuration/spec.yaml), [check](../marathon/datadog_checks/marathon/marathon.py), [readme](../marathon/README.md) |
| MongoDB (`mongo`) | `hosts` | credentials-required — MongoDB's default port (27017) is universal and the wire protocol is detectable. Strictly per spec, only `hosts` is required, so an unauthenticated localhost dev instance could be discovered generically. In practice, every production MongoDB enables authentication and most also require a dedicated `datadog` user with `clusterMonitor` role, plus a `connection_scheme` decision (`mongodb` vs. `mongodb+srv`) for replica sets. Classification is custom because of the auth-mode branching, but in real deployments it falls through to credentials-required. | [spec](../mongo/assets/configuration/spec.yaml), [upstream](https://www.mongodb.com/docs/manual/tutorial/configure-x509-client-authentication/) |
| Nagios (`nagios`) | `nagios_conf` | config-file-parse — The Nagios integration runs on the same host as a Nagios/Icinga v1 server and tails its log/perfdata files. The required `nagios_conf` points to the main config file (e.g. `/etc/nagios3/nagios.cfg`); the check parses it to discover `log_file`, `host_perfdata_file`, `service_perfdata_file`, and the corresponding template strings. There is no network endpoint at all. Auto-config could plausibly look for well-known config paths (`/etc/nagios*/nagios.cfg`, `/etc/icinga/icinga.cfg`, `/usr/local/nagios/etc/nagios.cfg`) and parse the first one found — Nagios-specific logic, hence custom. | [spec](../nagios/assets/configuration/spec.yaml), [check](../nagios/datadog_checks/nagios/nagios.py), [readme](../nagios/README.md) |
| NGINX (`nginx`) | `nginx_status_url` | http-path-probe — NGINX has at least three plausible status paths depending on the build: stub_status (open-source nginx), the NGINX Plus `/api` (R13+), and the vhost_traffic_status (`vts`) module's JSON endpoint. The status path is operator-configurable, so a discovery layer must probe candidate paths and inspect the response shape to pick the right mode. The port itself is whatever the operator configured. | [spec](../nginx/assets/configuration/spec.yaml), [upstream](https://nginx.org/en/docs/http/ngx_http_stub_status_module.html), [upstream](https://docs.nginx.com/nginx/admin-guide/monitoring/live-activity-monitoring/) |
| PHP-FPM (`php_fpm`) | `status_url` | http-path-probe — PHP-FPM exposes status/ping over an HTTP path that the user must configure on a FastCGI passthrough (typically nginx or Apache). The path is conventionally /status and /ping but is fully user-configurable in the FPM pool config (pm.status_path, ping.path) and depends on the front-end web server's routing. The check requires that at least one of status_url or ping_url be supplied; both default to localhost examples but no port/path is universal. Direct FastCGI mode (use_fastcgi=true) targets port 9000 directly but still needs the path. Discovery is feasible only via probing several plausible paths (/status, /ping, /fpm-status, /fpm-ping) and possibly direct FastCGI on 9000. | [spec](../php_fpm/assets/configuration/spec.yaml), [check](../php_fpm/datadog_checks/php_fpm/php_fpm.py), [readme](../php_fpm/README.md) |
| Postfix (`postfix`) | `directory` | other — Postfix is a host-local check: it counts files in /var/spool/postfix/<queue> directories or shells out to postqueue -p. There is no network endpoint to probe. Discovery has to detect that Postfix is installed on the local host (presence of /var/spool/postfix or a postqueue binary on PATH) and that the agent has the required sudoers / authorized_mailq_users grants. Because the choice between the directory-based mode (needs sudo + a queue_directory path) and the postqueue mode (needs the binary + ACL grant) depends on local OS state and operator-supplied permissions, the discovery logic is integration-specific. | [spec](../postfix/assets/configuration/spec.yaml), [check](../postfix/datadog_checks/postfix/postfix.py) |
| RabbitMQ (`rabbitmq`) | — | http-path-probe — RabbitMQ supports two collection modes: the Prometheus plugin (port 15692, `/metrics`, no auth — easy generic case) and the Management plugin (port 15672, `/api/`, basic auth — typically `guest:guest` only on localhost; remote access needs a dedicated monitoring user). A discovery layer must probe the Prometheus port first and fall back to the Management API. Classification is custom because of the multi-mode logic; the Prometheus path alone would be generic. | [spec](../rabbitmq/assets/configuration/spec.yaml), [auto_conf](../rabbitmq/datadog_checks/rabbitmq/data/auto_conf.yaml), [upstream](https://www.rabbitmq.com/docs/prometheus) |
| RethinkDB (`rethinkdb`) | `host`, `port` | credentials-required — The check connects to RethinkDB's driver port (default 28015) using the official `rethinkdb` Python driver and runs ReQL queries against the `rethinkdb` system database for cluster/server/table/job metrics. The spec marks `username` / `password` as optional, but the README explicitly instructs operators to create a `datadog-agent` user with a password and read-only grants (RethinkDB 2.4+ requires authenticated access for system tables in any production deployment; only an unsecured 2.3.x or pre-bootstrapped admin instance accepts unauthenticated connections). Pattern E: auth-optional in spec but practically required. | [spec](../rethinkdb/assets/configuration/spec.yaml), [check](../rethinkdb/datadog_checks/rethinkdb/check.py), [check](../rethinkdb/datadog_checks/rethinkdb/config.py), [readme](../rethinkdb/README.md), [upstream](https://rethinkdb.com/docs/permissions-and-accounts/) |
| Spark (`spark`) | `spark_url`, `cluster_name` | http-path-probe — The Spark check supports four mutually-exclusive deployment topologies — Spark Standalone master (port 8080), YARN ResourceManager (port 8088), Mesos master (port 5050), and Spark driver (port 4040) — selected by `spark_cluster_mode`. Each has a different REST UI shape: the YARN mode hits `/ws/v1/cluster/apps`, Mesos hits `/frameworks`, Standalone scrapes the master HTML page, etc. Discovery would have to probe several candidate ports and inspect response shape to identify the topology, then choose the right code path. On top of that, `cluster_name` is an arbitrary user-chosen tag that has no on-the-wire equivalent, and many production deployments add proxy auth, Knox/DC-OS tokens, or YARN security. | [spec](../spark/assets/configuration/spec.yaml), [check](../spark/datadog_checks/spark/spark.py), [upstream](https://spark.apache.org/docs/latest/monitoring.html) |
| Supervisord (`supervisord`) | `name` | http-path-probe — Supervisord is monitored via its XML-RPC interface, reachable either through an inet HTTP server (default `http://localhost:9001/RPC2`) or through a UNIX domain socket whose path is deployment-specific. The only spec-required field is `name`, an arbitrary tag chosen by the user. Two things make this non-generic: (1) the typical production deployment uses a UNIX socket whose path cannot be guessed without parsing supervisor's own config, and (2) when authentication is enabled in `inet_http_server`, calls return 401 unless `username`/`password` are supplied. The HTTP+no-auth case (port 9001) is probable, but real deployments often need socket-path discovery or credentials. | [spec](../supervisord/assets/configuration/spec.yaml), [check](../supervisord/datadog_checks/supervisord/supervisord.py), [upstream](http://supervisord.org/configuration.html#inet-http-server-section-settings) |
| TLS (`tls`) | `server` | other — TLS is a synthetic check: the user explicitly nominates a hostname/IP (or local cert file) they want to monitor. There is no upstream system to discover on the host where the Agent runs - the integration's purpose is to validate someone else's certificate. Without a target list provided by the user, auto-config has nothing to enumerate. Once a target is known, the check is a generic TCP+TLS handshake on port 443 (or any port), so the technical probe is trivial; the missing piece is intent. | [spec](../tls/assets/configuration/spec.yaml), [check](../tls/datadog_checks/tls/tls.py), [readme](../tls/README.md) |
| Varnish (`varnish`) | `varnishstat` | other — The Varnish check does not talk to Varnish over the network — it shells out to the local `varnishstat` binary (and optionally `varnishadm`) and parses its text/XML/JSON output. Discovery is therefore not a network probe but a local-host concern: "is the varnishstat binary present, and which path is it at?". The spec field is even marked `require_trusted_provider: true`, signalling that the value is a path to an executable on the Agent's host, not a URL. The conventional path is `/usr/bin/varnishstat`, but containerized deployments require wrapping the command (e.g. `docker exec`) and passing `%%host%%` template variables, which is inherently per-environment. Backend health additionally needs `varnishadm` plus the secretfile, which is a credential. | [spec](../varnish/assets/configuration/spec.yaml), [check](../varnish/datadog_checks/varnish/varnish.py), [upstream](https://varnish-cache.org/docs/trunk/reference/varnishstat.html) |
| Vault (`vault`) | `api_url` | credentials-required — Vault exposes its API on a known port (8200) under /v1, and the agent can reach /sys/health unauthenticated for basic status checks. However, the OpenMetrics /v1/sys/metrics endpoint is gated by an ACL-bound client token in any production deployment, and metric collection is the integration's primary purpose. A discovery layer could probe http://%%host%%:8200/v1/sys/health to detect Vault, but useful metric collection still requires a client token (or token file path) that doesn't come over the wire. Closest pattern: auth-optional-but-practically-required (like elastic). | [spec](../vault/assets/configuration/spec.yaml), [check](../vault/datadog_checks/vault/vault.py), [check](../vault/datadog_checks/vault/check.py) |

### Auto-config impossible

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| ActiveMQ (`activemq`) | `host`, `port` | credentials-required — The activemq integration is a JMXFetch-backed check (spec inherits `instances/jmx`, `is_jmx: true`). JMX-based checks always need `host` + `port` and, in any production-shaped deployment, `user` + `password` — ActiveMQ's JMX agent enables authentication by default in modern releases via `jmx.access` / `jmx.password` files. Spec marks user/password as optional (so an unauthenticated localhost broker works), but per the procedure rubric, JMXFetch-based integrations are classified as impossible because credentials cannot be discovered from the wire even when the JMX port itself can be probed. | [spec](../activemq/assets/configuration/spec.yaml), [other](../activemq/datadog_checks/activemq/data/conf.yaml.example), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml), [upstream](https://activemq.apache.org/components/classic/documentation/jmx) |
| Amazon MSK (`amazon_msk`) | `cluster_arn` | credentials-required — The check requires a cluster ARN and uses boto3 (`kafka.list_nodes`) to enumerate broker endpoints, then scrapes the JMX/Node exporters per broker. Two blockers make this impossible for generic auto-config: (1) `cluster_arn` is a required user-supplied AWS resource identifier — it cannot be inferred from the wire and is not a property of the host the Agent runs on; (2) the AWS API call requires IAM credentials (default boto3 chain or `assume_role`) that the user must provision out-of-band. Even though the broker-side Prometheus endpoints are unauthenticated and on fixed ports, discovery cannot proceed without first authenticating to the AWS control plane to learn which brokers exist. | [spec](../amazon_msk/assets/configuration/spec.yaml), [check](../amazon_msk/datadog_checks/amazon_msk/check.py) |
| Ambari (`ambari`) | `url` | credentials-required — Ambari is the Hadoop cluster management REST API (typically on port 8080). The check walks the `/api/v1/clusters/...` tree to enumerate hosts, services, and components. Although `username`/`password` are marked optional in the spec, every production Ambari server has authentication enabled (default credentials are notoriously `admin:admin`, which a generic auto-config layer must not assume), and even discovering which `services` and `components` to scrape requires user policy: the `services` mapping is a per-deployment whitelist of metric headers per component. Per pattern D (credential-required REST API) and the user-intent caveat, this is `impossible`. | [spec](../ambari/assets/configuration/spec.yaml), [check](../ambari/datadog_checks/ambari/ambari.py), [readme](../ambari/README.md) |
| ArangoDB (`arangodb`) | `openmetrics_endpoint` | credentials-required — ArangoDB exposes Prometheus metrics on a fixed path (`/_admin/metrics/v2`) on its HTTP coordinator port (default 8529). The check (`OpenMetricsBaseCheckV2`) is generic-shaped and the path/port are universal, but ArangoDB enables JWT authentication by default (`authentication = true` is the production posture, mirrored in the integration's own test config). Reaching `/_admin/metrics/v2` therefore requires either basic auth credentials or a JWT bearer token provisioned out-of-band. Without those, autodiscovery cannot scrape a real-world cluster, so this is `impossible` / `credentials-required`. | [spec](../arangodb/assets/configuration/spec.yaml), [check](../arangodb/datadog_checks/arangodb/check.py), [other](../arangodb/tests/docker/arangodb3/arangod.conf), [readme](../arangodb/README.md) |
| Arctic Wolf Aurora Endpoint Security (`arctic_wolf_aurora_endpoint_security`) | — | other — Arctic Wolf Aurora Endpoint Security is a logs-only marketplace tile. The spec defines only a `logs` template (TCP/UDP syslog listener with a user-chosen port) and there is no Agent check class — the `datadog_checks/arctic_wolf_aurora_endpoint_security/` package contains only `__init__.py`, `__about__.py`, and an empty `data/` directory, with no `<name>.py` and no metrics implementation. There is no upstream service to probe; the integration's purpose is to receive forwarded events at a port the user picks. Auto-config in the network-probe sense does not apply (pattern N: instrumentation-only). | [spec](../arctic_wolf_aurora_endpoint_security/assets/configuration/spec.yaml), [readme](../arctic_wolf_aurora_endpoint_security/README.md) |
| Avi Vantage (`avi_vantage`) | `avi_controller_url` | credentials-required — Avi Vantage is a load balancer whose Controller exposes Prometheus metrics under `/api/analytics/prometheus-metrics/<entity>`. The check inherits `OpenMetricsBaseCheckV2` but wraps every scrape in a `login()` context manager that POSTs `username`/`password` to `/login` and stores the auth cookie/CSRF token; without successful login the scrape is skipped and a CRITICAL service check is emitted. The Controller URL is also tenant-specific (different controllers per tenancy). Pattern D / E. | [spec](../avi_vantage/assets/configuration/spec.yaml), [check](../avi_vantage/datadog_checks/avi_vantage/check.py) |
| Barracuda SecureEdge (`barracuda_secure_edge`) | — | other — Barracuda SecureEdge is a marketplace tile that ships only a logs configuration: the spec defines a fixed set of file tail sources under /var/phion/logs/ (auth, firewall, vpn, ssh, etc.) and the datadog_checks/barracuda_secure_edge package contains no check.py - just __about__.py, __init__.py and the conf.yaml.example. There is no Agent metric check, no network probe, and no endpoint to discover. Enabling the integration is a matter of whether the SecureEdge log files happen to be present on the Agent host (which they only are if the Agent is co-located on the appliance or its logs are rsynced/mounted there). | [spec](../barracuda_secure_edge/assets/configuration/spec.yaml), [readme](../barracuda_secure_edge/README.md) |
| BeyondTrust Password Safe (`beyondtrust_password_safe`) | — | other — BeyondTrust Password Safe is a marketplace tile that ships only a logs configuration - a TCP syslog receiver where the user must supply <PORT> for the appliance to forward syslog to. The datadog_checks/beyondtrust_password_safe package contains no check.py and there is no metric collection or network probe. Enabling the integration requires user policy decisions (which port to listen on, which appliance to forward from) and configuration on the upstream Password Safe appliance to send syslog to that port; none of this is discoverable from the wire on the Agent host. | [spec](../beyondtrust_password_safe/assets/configuration/spec.yaml), [readme](../beyondtrust_password_safe/README.md) |
| BeyondTrust Privileged Remote Access (`beyondtrust_privileged_remote_access`) | — | other — BeyondTrust Privileged Remote Access is a marketplace tile that ships only a logs configuration - a TCP syslog receiver with custom log_processing_rules where the user must supply <PORT> for the appliance to forward syslog to. The datadog_checks/beyondtrust_privileged_remote_access package contains no check.py and there is no metric collection or network probe. Enabling the integration requires user policy decisions (which port to listen on) plus configuration on the upstream PRA appliance to send syslog to that port; neither is discoverable from the wire on the Agent host. | [spec](../beyondtrust_privileged_remote_access/assets/configuration/spec.yaml), [readme](../beyondtrust_privileged_remote_access/README.md) |
| Cacti (`cacti`) | `mysql_host`, `mysql_user`, `rrd_path` | credentials-required — The Cacti check connects to Cacti's MySQL backend (via pymysql) to enumerate hosts and RRD file metadata, then reads the on-disk RRD files (via rrdtool) for actual metric values. This requires a MySQL username (and password in any non-trivial deployment) plus a filesystem path to the RRD directory (`/var/lib/cacti/rra`, `/var/www/html/cacti/rra`, or wherever the operator put it). The MySQL credentials are not discoverable from the wire, and the RRD path is site-specific filesystem state. There is no network endpoint for autodiscovery to probe. | [spec](../cacti/assets/configuration/spec.yaml), [check](../cacti/datadog_checks/cacti/cacti.py), [upstream](https://www.cacti.net/) |
| Cassandra (`cassandra`) | `host`, `port`, `user`, `password` | credentials-required — Cassandra is a JMXFetch-based integration: the spec inherits `template: instances/jmx` and sets `is_jmx: true`. JMX collection requires a JVM-level connection to the Cassandra JMX port (default 7199) and, in any production deployment, JMX authentication credentials (user + password). The credentials are provisioned out-of-band by the cluster operator and cannot be derived from the wire. Per the procedure's pattern I, JMXFetch integrations are categorically `impossible` for generic auto-config. | [spec](../cassandra/assets/configuration/spec.yaml), [upstream](https://cassandra.apache.org/doc/latest/cassandra/managing/operating/security.html) |
| Checkpoint Harmony Endpoint (`checkpoint_harmony_endpoint`) | — | other — Checkpoint Harmony Endpoint is a logs-only vendor security tile. The spec defines only `init_config/default` and `instances/default` (no metric collection options) and the README's Setup section is exclusively about pointing Checkpoint's syslog/log-exporter (typically port 514) at the Agent's syslog listener. There is no Agent metric check (only an `__init__.py` with a stale `from .check import CheckpointHarmonyEndpointCheck` import - no actual check.py exists), no metrics endpoint, no host/port to autodiscover from the wire. Enabling the integration depends on the user knowing which port the Agent should listen on for incoming syslog and on configuring Smart Console log streaming - neither of which is discoverable. | [spec](../checkpoint_harmony_endpoint/assets/configuration/spec.yaml), [readme](../checkpoint_harmony_endpoint/README.md) |
| Check Point Quantum Firewall (`checkpoint_quantum_firewall`) | — | other — Check Point Quantum Firewall is a logs-only vendor security tile. The spec defines only a `logs` template (tcp/udp syslog with `<PORT>` placeholder) and there is no Agent check class under `datadog_checks/checkpoint_quantum_firewall/` (only `__init__.py` exposing `__version__`). Setup is purely configuring `cp_log_export` on the Check Point management server to forward JSON-format syslog to a port the Agent listens on. There is no metrics endpoint to probe, no host/port the Agent can autodiscover from the wire, and the listener port is operator policy. | [spec](../checkpoint_quantum_firewall/assets/configuration/spec.yaml), [readme](../checkpoint_quantum_firewall/README.md) |
| Cisco ACI (`cisco_aci`) | `aci_url`, `username`, `pwd` | credentials-required — The Cisco ACI check polls the APIC controller's REST API. The instance must specify the controller URL (`aci_url` or a list of URLs in `aci_urls`) plus authentication: either a `username` + `pwd`, or certificate-based auth (`cert_name`, `cert_key` / `cert_key_path`, optional `cert_key_password`). The check raises ConfigurationError if no URL is given and authenticates against `/api/aaaLogin.json` for every poll. Per pattern D, classify as impossible: no URL or credential is derivable from a wire probe, and APIC is a centralized controller, not a per-host service. | [spec](../cisco_aci/assets/configuration/spec.yaml), [check](../cisco_aci/datadog_checks/cisco_aci/cisco.py), [readme](../cisco_aci/README.md) |
| Cisco ASA (`cisco_asa`) | — | other — Cisco ASA is a logs-only vendor security tile. The spec defines only a `logs` template (tcp/udp syslog with `<PORT>` placeholder) and there is no Agent check class under `datadog_checks/cisco_asa/` (only `__init__.py` exposing `__version__`). Setup is configuring `logging host ... tcp/<port>` on the ASA CLI to forward syslog to the Datadog Agent listener. There is no metrics endpoint to probe, no upstream host/port to autodiscover, and the listener port is operator policy. Identical shape to cisco_secure_firewall. | [spec](../cisco_asa/assets/configuration/spec.yaml), [readme](../cisco_asa/README.md) |
| Cisco Secure Client (`cisco_secure_client`) | — | other — Cisco Secure Client (formerly AnyConnect) is a logs-only vendor security tile. The spec defines only a `logs` template (tcp/udp syslog with a `<PORT>` placeholder and an AnyConnect-specific `log_processing_rules` filter) and there is no Agent check class under `datadog_checks/cisco_secure_client/` (only `__init__.py` exposing `__version__`). Logs are sourced from the upstream Cisco firewall (FTD/Meraki/ASA) with syslog forwarded to the Agent. There is no metrics endpoint to probe, no upstream host/port for the Agent to autodiscover, and the listener port is operator policy. | [spec](../cisco_secure_client/assets/configuration/spec.yaml), [readme](../cisco_secure_client/README.md) |
| Cisco Secure Firewall (`cisco_secure_firewall`) | — | other — Cisco Secure Firewall is a logs-only vendor security tile. The spec defines only a `logs` template (tcp/udp syslog) and there is no Agent check class under `datadog_checks/cisco_secure_firewall/` (no <name>.py implementation). Enabling the integration is purely a question of whether the user routes ASA/FTD syslog to a port the Agent listens on; there is no metric endpoint to probe and no host/port the Agent can autodiscover from the wire. The user must also know which TCP/UDP port their firewall is configured to ship syslog to. | [spec](../cisco_secure_firewall/assets/configuration/spec.yaml), [readme](../cisco_secure_firewall/README.md) |
| Cisco Secure Web Appliance (`cisco_secure_web_appliance`) | — | other — Cisco Secure Web Appliance (CSWA) is a logs-only vendor security tile. The spec exposes only a `logs` template offering two collection modes (file tail of access_logs/l4tm_logs, or UDP syslog). There is no Agent check class under `datadog_checks/cisco_secure_web_appliance/` (no <name>.py implementation), no metrics endpoint, and no network probe. Enabling the integration depends on user-specific paths (where CSWA writes its log files) or syslog port choice, neither of which is discoverable from the wire. | [spec](../cisco_secure_web_appliance/assets/configuration/spec.yaml), [readme](../cisco_secure_web_appliance/README.md) |
| Citrix Hypervisor (`citrix_hypervisor`) | `url` | credentials-required — Citrix Hypervisor (formerly XenServer) exposes its RRD metrics over an HTTP endpoint (`/host_rrd`, `/rrd_updates`) protected by HTTP Basic auth against the XAPI user database. The check uses the standard `instances/http` template, which inherits `username`/`password` and forwards them to the inner HTTP client. While `username`/`password` are not strictly `required: true` in the spec, the XAPI endpoints will not return RRD data without a valid hypervisor login, so a real deployment cannot be auto-configured without them. Even the URL itself is per-host (each pool master has its own management address). Pattern D / E. | [spec](../citrix_hypervisor/assets/configuration/spec.yaml), [check](../citrix_hypervisor/datadog_checks/citrix_hypervisor/check.py) |
| ClickHouse (`clickhouse`) | `server` | credentials-required — The check uses the `clickhouse-connect` Python client to authenticate to ClickHouse's HTTP interface (default port 8123) and runs SQL queries against `system.*` tables. While the spec marks `username`/`password` as optional (defaulting to `default` / empty), that combination only works on completely unsecured ClickHouse installs; any production deployment, ClickHouse Cloud, or DBM-enabled setup requires real credentials with `SHOW`/`SELECT` privileges on system tables. Even if discovery picked the host+port, it cannot generate or supply a valid SQL user. This is pattern D (spec-required credentials) softened to pattern E (auth-optional in spec, practically required in production). | [spec](../clickhouse/assets/configuration/spec.yaml), [check](../clickhouse/datadog_checks/clickhouse/clickhouse.py), [readme](../clickhouse/README.md) |
| Cloud Foundry API (`cloud_foundry_api`) | `api_url`, `client_id`, `client_secret` | credentials-required — The Cloud Foundry API check authenticates to UAA with a `client_id` / `client_secret` (OAuth2 client credentials grant) and uses the resulting bearer token to query the Cloud Controller v2/v3 APIs. The check raises `ConfigurationError` if any of `api_url`, `client_id`, or `client_secret` is missing. The credentials must be provisioned out-of-band against the deployment's UAA; they are not discoverable from the wire. Even though the API URL could in principle be probed, classification is dominated by the auth blocker. | [spec](../cloud_foundry_api/assets/configuration/spec.yaml), [check](../cloud_foundry_api/datadog_checks/cloud_foundry_api/cloud_foundry_api.py) |
| Cloudera (`cloudera`) | `api_url`, `workload_username`, `workload_password` | credentials-required — The Cloudera integration scrapes the Cloudera Manager REST API (e.g. `https://cluster1.cloudera.site/cluster_1/cdp-proxy-api/cm-api/v48`). The check requires `workload_username` and `workload_password` (CDP Workload credentials) at init_config level plus a per-instance `api_url` that includes the deployment-specific cluster slug and API version. None of this is recoverable from a generic network probe: the URL embeds tenant-specific paths and the API will not respond without the workload credential. Even the optional mTLS path needs a user-provisioned `cert_file`/`key_file`. | [spec](../cloudera/assets/configuration/spec.yaml), [check](../cloudera/datadog_checks/cloudera/check.py) |
| Barracuda CloudGen Firewall (`cloudgen_firewall`) | — | other — Barracuda CloudGen Firewall is a logs-only vendor security tile. The spec defines only a `logs` template tailing a file (default example `/var/log/cloudgen_firewall.log`). There is no Agent check class under `datadog_checks/cloudgen_firewall/` (no <name>.py implementation), no metrics endpoint, and no network probe. Enabling the integration depends on whether the user has configured the firewall to ship logs to a path the Agent can read; that is a deployment policy decision, not something discoverable from the wire. | [spec](../cloudgen_firewall/assets/configuration/spec.yaml), [readme](../cloudgen_firewall/README.md) |
| Confluent Platform (`confluent_platform`) | `host`, `port` | credentials-required — Confluent Platform is a JMX integration (`is_jmx: true`, `template: instances/jmx`) that connects via the Java RMI registry to brokers, Connect, Schema Registry, REST Proxy, etc. JMX in production Confluent deployments is gated behind RMI credentials and frequently SSL (trust/key stores). Even when unauthenticated locally, JMX requires per-component port configuration, no canonical default port, and one instance per component. The check has no auto_conf.yaml because Autodiscovery cannot template JMX targets reliably without operator input. Per the procedure shortcut, JMX-fetch integrations are always impossible to auto-configure. | [spec](../confluent_platform/assets/configuration/spec.yaml), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml), [upstream](https://docs.confluent.io/platform/current/kafka/monitoring.html) |
| Control-M (`control_m`) | `control_m_api_endpoint` | credentials-required — Control-M is BMC's enterprise job scheduler. The check talks to the Control-M Automation API at a deployment-specific URL (e.g. https://example.com:8443/automation-api) and authenticates via either session login (username + password) or a static bearer token passed through `headers`. The API endpoint host/port and base path are not standardized across deployments, and every meaningful API call requires authentication. There is no unauthenticated discovery surface. | [spec](../control_m/assets/configuration/spec.yaml), [check](../control_m/datadog_checks/control_m/check.py) |
| Couchbase (`couchbase`) | `server` | credentials-required — Couchbase exposes a REST API on port 8091 and the auto_conf template already covers host/port via `http://%%host%%:8091`. However, every Couchbase deployment beyond a fresh install requires admin credentials (username/password) to access `/pools`, `/pools/default`, bucket stats, query stats, and index stats. The HTTP_CONFIG_REMAPPER in the check explicitly remaps `user` to `username`, confirming auth is the standard mode. Credentials cannot be discovered from the wire, so classification is impossible. | [spec](../couchbase/assets/configuration/spec.yaml), [auto_conf](../couchbase/datadog_checks/couchbase/data/auto_conf.yaml), [check](../couchbase/datadog_checks/couchbase/couchbase.py), [upstream](https://docs.couchbase.com/server/current/rest-api/rest-intro.html) |
| Delinea Privilege Manager (`delinea_privilege_manager`) | — | other — Delinea Privilege Manager is a vendor security marketplace tile with no Agent metric check. The spec defines only a `logs` template (TCP/UDP syslog ingestion at a user-supplied port) and the `datadog_checks/delinea_privilege_manager/` package contains no `.py` check implementation. Required setup is on the upstream Delinea side (configure syslog forwarding to the Agent's TCP/UDP listener); there is no host/port/endpoint to discover from the Agent's vantage point and no service for a probe to fingerprint. Auto-config in the network-probe sense does not apply. | [spec](../delinea_privilege_manager/assets/configuration/spec.yaml), [readme](../delinea_privilege_manager/README.md) |
| Delinea Secret Server (`delinea_secret_server`) | — | other — Delinea Secret Server is a vendor security marketplace tile with no Agent metric check. The spec exposes only a `logs` template (TCP/UDP syslog ingestion at a user-supplied port) and the `datadog_checks/delinea_secret_server/` package contains no `.py` check implementation. The integration is realized end-to-end by configuring Delinea Secret Server to forward syslog to the Agent. There is no host/port/endpoint or service to autodiscover from the Agent side. | [spec](../delinea_secret_server/assets/configuration/spec.yaml), [readme](../delinea_secret_server/README.md) |
| Directory (`directory`) | `directory` | other — The Directory check is a generic filesystem-scanning template: the user MUST supply an absolute path in `directory`, and the check walks it with `os.walk`, calling `stat()` on matching files to emit gauges/histograms (sizes, mtimes, file/folder counts). There is no upstream service, no port, no protocol, and no canonical default path — every deployment monitors a different user-chosen directory (logs dir, upload dir, queue dir, etc.). The `directory` field is the entire purpose of the integration; auto-config has no signal from which to synthesize it. This is pattern J ("user-supplied generic check template"): the integration is itself the configuration template. | [spec](../directory/assets/configuration/spec.yaml), [check](../directory/datadog_checks/directory/directory.py), [readme](../directory/README.md) |
| DNS (`dns_check`) | `hostname` | other — The DNS check is a synthetic probe: the user nominates a `hostname` (required) to resolve via `dns.resolver.Resolver().query()` and the check measures resolution time and optionally validates the result against an expected `resolves_as` IP list. There is no upstream service co-located with the Agent; the target is whichever name the user wants to monitor (e.g. `www.example.com`, an internal service name, or a CNAME being failover-tested). The required `hostname` is user *intent*, not a discoverable property. This is pattern K (synthetic / user-intent probe), with no credentials involved, but in this case the missing input is the target identity itself, so auto-config cannot synthesize a default instance. | [spec](../dns_check/assets/configuration/spec.yaml), [check](../dns_check/datadog_checks/dns_check/dns_check.py), [readme](../dns_check/README.md) |
| DO Query Actions (`do_query_actions`) | `db_identifier`, `username`, `password`, `db_type`, `queries` | credentials-required — DO Query Actions is a generic SQL-execution framework (currently Postgres only) that runs user-defined queries on a user-nominated database on a user-defined schedule. Required spec fields include `db_identifier` (host + dbname), `username`, `db_type`, and `queries` (a list of SQL with monitor IDs, intervals, entity metadata, etc.). The check connects with psycopg using either a password, AWS RDS IAM tokens, or Azure Managed Identity. Auto-config cannot generate any of these inputs: the user's intent (which database, which queries, which monitor IDs, which entity tags) is the integration's content, not something discoverable on the wire. Combines pattern J (user-supplied template) with pattern D (credentials required). | [spec](../do_query_actions/assets/configuration/spec.yaml), [check](../do_query_actions/datadog_checks/do_query_actions/check.py), [check](../do_query_actions/datadog_checks/do_query_actions/postgres_connection.py) |
| DuckDB (`duckdb`) | `db_name` | other — DuckDB is an embedded analytical database stored as a file on the local filesystem. The required `db_name` field is a filesystem path to a `.db` file (e.g. `/path-to-file/my_database.db`), and the check opens it via `duckdb.connect(self.db_name, read_only=True)`. There is no port, no network endpoint, and no canonical filesystem location — the path is entirely user-defined. Auto-config would have to guess where the user has stored their DuckDB file(s), which is not discoverable from the wire or from any standard location. Closest analogue is pattern M (process-name local discovery) but applied to filesystem state: the user's intent (which file to monitor) is the missing input. | [spec](../duckdb/assets/configuration/spec.yaml), [check](../duckdb/datadog_checks/duckdb/check.py) |
| ESET Protect (`eset_protect`) | — | other — ESET Protect is a logs-only integration. The spec defines no Agent metric check; the only options are a `logs` template (TCP/UDP listener with a user-chosen port, source eset-protect). There is no datadog_checks/eset_protect/<name>.py — the package contains only metadata (__init__.py, __about__.py). Logs are forwarded from the ESET Protect Web Console, so there is no upstream service for the Agent to discover and probe. Auto-config in the network-probe sense does not apply. | [spec](../eset_protect/assets/configuration/spec.yaml), [readme](../eset_protect/README.md) |
| ESXi (`esxi`) | `host`, `username`, `password` | credentials-required — The ESXi check connects to the host's vSphere Web Services API via pyVmomi (`pyVim.connect.SmartConnect`) using `host`, `username`, and `password`, all marked required in the spec. The check verifies it is talking to an ESXi host (not vCenter) and then walks the inventory and perfManager. There is no anonymous probe path — every API call requires authenticated session credentials provisioned out-of-band by the user. | [spec](../esxi/assets/configuration/spec.yaml), [check](../esxi/datadog_checks/esxi/check.py), [readme](../esxi/README.md) |
| Flink (`flink`) | — | other — The Flink integration ships no Agent check binary — datadog_checks/flink/ contains only __init__.py and a logs-only conf.yaml.example. Metrics are collected by configuring Flink's built-in Datadog HTTP Reporter (which posts directly to the Datadog API) or its StatsD reporter (to dogstatsd). The Agent does not probe Flink at all, so there is no endpoint to autodiscover. The only Agent-side configuration is an optional logs tailer pointed at /var/log/flink.log. Separately, Flink JVMs can be monitored via the generic JMX integration, which has its own credential needs. | [spec](../flink/assets/configuration/spec.yaml), [readme](../flink/README.md), [other](../flink/datadog_checks/flink/data/conf.yaml.example) |
| Fly.io (`fly_io`) | `org_slug` | credentials-required — Fly.io is a hosted platform, not a co-located service. The check (`OpenMetricsBaseCheckV2`) builds its endpoint from a tenant-specific `org_slug` (`https://api.fly.io/prometheus/<org_slug>/federate?...`) and requires an `Authorization` header with a `FlyV1` access token (created via `fly tokens create readonly`) for both the Prometheus federate API and the Machines REST API. The `org_slug` and the access token are user-supplied state that does not come over the wire and cannot be probed. | [spec](../fly_io/assets/configuration/spec.yaml), [check](../fly_io/datadog_checks/fly_io/check.py), [readme](../fly_io/README.md) |
| Forescout (`forescout`) | — | other — Forescout is a logs-only vendor tile. The spec defines no `instances` block at all, only a `logs` template that expects the user to configure a syslog `tcp`/`udp` listener `port` on the Agent (the Forescout Syslog plugin must be installed and pointed at this port). There is no Agent metric check, no Python check class, and no upstream service to probe — Forescout pushes syslog into the Agent rather than the Agent pulling. The integration's value is the log pipeline plus dashboards; auto-config in the network-probe sense does not apply. | [spec](../forescout/assets/configuration/spec.yaml), [readme](../forescout/README.md) |
| FoundationDB (`foundationdb`) | — | credentials-required — FoundationDB clients connect through a binary cluster file (`fdb.cluster`) which contains a description, an ID, and the coordinator IP/port list. The check uses the `fdb` Python binding to call `fdb.open(cluster_file=...)` and then reads the `\xff\xff/status/json` special key. There is no host/port to probe directly; without the cluster file there is no way for the discovery layer to address the database. TLS-secured deployments additionally require a key/cert/CA bundle. The cluster file is host-specific state (typically provisioned by the FoundationDB installer or operator) and cannot be derived over the wire. | [spec](../foundationdb/assets/configuration/spec.yaml), [check](../foundationdb/datadog_checks/foundationdb/check.py) |
| Go Expvar (`go_expvar`) | `expvar_url` | other — go_expvar scrapes Go's built-in `/debug/vars` endpoint exposed by `net/http/pprof` or `expvar.Handler()` in any Go program that opted in. The path component is fixed (`GO_EXPVAR_URL_PATH = "/debug/vars"` in the check), but the host, port, and even whether expvar is mounted at the root or behind a custom mux are entirely application-specific: there is no canonical port for arbitrary Go binaries, and the user must also pick a `namespace` and (optionally) a custom `metrics` allowlist with paths like `memstats/PauseTotalNs`. The missing input is user *intent* — which Go application to monitor — so this falls under pattern J (user-supplied generic check template) rather than a discoverable upstream service. | [spec](../go_expvar/assets/configuration/spec.yaml), [check](../go_expvar/datadog_checks/go_expvar/go_expvar.py), [readme](../go_expvar/README.md) |
| Go-Metro (TCP RTT) (`go_metro`) | `interface`, `ips`, `hosts` | other — go-metro is a passive TCP RTT sniffer (a Go binary using libpcap) that ships only with the legacy 64-bit Agent v5 DEB/RPM packages and is not available in Agent v6+. There is no upstream service to probe; instead the user nominates a network `interface` plus an `ips`/`hosts` allowlist of remote endpoints whose round-trip times should be measured. Like dns_check / http_check, the target is purely user intent (which peers do you care about RTT for?), not a discoverable on-host signal. Setup additionally requires `setcap cap_net_raw+ep` on the binary, so even runtime feasibility is host-state dependent. | [spec](../go-metro/assets/configuration/spec.yaml), [readme](../go-metro/README.md) |
| GuardDog (`guarddog`) | `guarddog_path`, `package_ecosystem`, `dependency_file_path` | other — GuardDog is a Datadog-authored CLI scanner that audits dependency manifests for malicious packages. The Agent check (`check.py`) shells out via `subprocess.run` to a user-installed `guarddog` binary (`init_config.guarddog_path`) and runs `<guarddog_path> <package_ecosystem> verify <dependency_file_path> --output-format=json`. The two per-instance required fields are user-policy: which ecosystem (`pypi`/`npm`/`go`/`github_action`) and the absolute path to a `requirements.txt` / `package.json` / `go.mod` / workflow file the user wants scanned. There is no network endpoint, no port, no service to probe — discovery would have to enumerate dependency files on the host, which is an arbitrary user choice rather than a fingerprintable upstream system. | [spec](../guarddog/assets/configuration/spec.yaml), [check](../guarddog/datadog_checks/guarddog/check.py), [readme](../guarddog/README.md) |
| Gunicorn (`gunicorn`) | `proc_name` | other — The Gunicorn check does not probe the network at all. It uses `psutil.process_iter` on the host where the Agent runs to find a master process whose cmdline matches `gunicorn: master [<proc_name>]`. The required `proc_name` is application-specific (it's the value the user passed to `gunicorn --name`) and is set by the application owner; it cannot be derived from the wire or from any well-known list. Discovery would require enumerating every gunicorn master process on every host the Agent can see and then deciding which app names matter — this is process-name discovery, not credential or endpoint discovery, and is out of scope for generic auto-config. Note `setproctitle` must also be installed in the app's Python env for the master title pattern to appear at all. | [spec](../gunicorn/assets/configuration/spec.yaml), [check](../gunicorn/datadog_checks/gunicorn/gunicorn.py), [readme](../gunicorn/README.md) |
| Harbor (`harbor`) | `url`, `username`, `password` | credentials-required — Harbor is a container registry with a REST API on port 80/443. The spec overrides the standard HTTP template to mark `username` and `password` as required, and the shipped auto_conf.yaml ships `username: <USERNAME>` / `password: <PASSWORD>` placeholders. The check calls registry, project, volume, and read-only endpoints that require an admin (or at least authenticated) account. Some endpoints (registries, volume) silently return 401/403 for non-admin users. Credentials cannot be discovered from the wire. Pattern D (spec-required credentials). | [spec](../harbor/assets/configuration/spec.yaml), [auto_conf](../harbor/datadog_checks/harbor/data/auto_conf.yaml), [check](../harbor/datadog_checks/harbor/harbor.py), [readme](../harbor/README.md), [upstream](https://goharbor.io/docs/latest/install-config/) |
| Hazelcast (`hazelcast`) | `host`, `port` | credentials-required — Hazelcast is primarily a JMXFetch integration: the spec inherits both `init_config/jmx` and `instances/jmx` (with default JMX port 1099), and the bundled Python check (`HazelcastCheck`) only adds a small HTTP poller for the optional Management Center health endpoint (`mc_health_check_endpoint`) plus a cluster-state mapping. Real metric collection requires JMX RMI to a running Hazelcast member with `-Dhazelcast.jmx=true`, which in production is gated by JMX authentication (user + password) and often TLS. Per pattern D and the JMXFetch shortcut, this is impossible. The Management Center HTTP probe alone does not produce the integration's metric set. | [spec](../hazelcast/assets/configuration/spec.yaml), [check](../hazelcast/datadog_checks/hazelcast/check.py), [readme](../hazelcast/README.md) |
| Hive (`hive`) | `host`, `port` | credentials-required — Hive is a JMXFetch-only integration: the spec inherits `template: instances/jmx` and there is no Python check class (the package only re-exports `__version__`). Collection runs through JMXFetch against HiveServer2 / Hive Metastore JMX (default example port 8809). Per the procedure's JMXFetch shortcut and pattern D, host+port+user+password are needed in any production deployment, and JMX credentials cannot be derived from the wire. Classify as impossible. | [spec](../hive/assets/configuration/spec.yaml), [check](../hive/datadog_checks/hive/__init__.py), [readme](../hive/README.md) |
| HiveMQ (`hivemq`) | `host`, `port` | credentials-required — HiveMQ is monitored via JMXFetch: the spec inherits `template: instances/jmx` and `init_config/jmx`, so collection happens over JMX/RMI on the broker's monitoring port (default 9010). Although user/password are marked optional in the spec, any production HiveMQ deployment with JMX exposed off-localhost will require JMX authentication credentials, and TLS-protected RMI additionally requires trust/key stores. Per pattern I (JMXFetch) and pattern D (spec credentials), this falls into the categorical `impossible` bucket for auto-config because the credentials and keystores are operator-provisioned out-of-band. | [spec](../hivemq/assets/configuration/spec.yaml), [other](../hivemq/datadog_checks/hivemq/data/conf.yaml.example), [readme](../hivemq/README.md) |
| Hudi (`hudi`) | `host`, `port` | credentials-required — Hudi is a JMX integration: the spec only references `init_config/jmx` and `instances/jmx`, the integration ships a `metrics.yaml` MBean list, and there is no Python check (only an `__init__.py` re-exporting the version). JMXFetch is dispatched against a Spark / Flink / standalone Hudi JVM that the user must expose with `-Dcom.sun.management.jmxremote.*` flags. JMX RMI ports are not standardised across Hudi deployments and production JMX endpoints almost always require `user` / `password` plus possibly a key/trust store. By the JMXFetch rule (pattern D / shortcut), this is impossible. | [spec](../hudi/assets/configuration/spec.yaml), [other](../hudi/datadog_checks/hudi/data/metrics.yaml), [other](../hudi/datadog_checks/hudi/data/conf.yaml.example) |
| IBM ACE (`ibm_ace`) | `mq_server`, `mq_port`, `channel`, `queue_manager` | credentials-required — IBM App Connect Enterprise is collected by subscribing to IBM MQ pub/sub topics over MQI (via `pymqi`). The check requires `mq_server`, `mq_port`, `channel`, and `queue_manager`, none of which are discoverable on the wire: `channel` and `queue_manager` are administrator-defined identifiers configured inside the MQ broker, and standard production deployments authenticate the channel with `mq_user`/`mq_password` (and frequently TLS using a `.kdb` key repository plus a cipher spec). The Agent must also be running on a host with the IBM MQ client libraries installed. No combination of host+port probes yields the channel/QM names or credentials. | [spec](../ibm_ace/assets/configuration/spec.yaml), [check](../ibm_ace/datadog_checks/ibm_ace/check.py), [check](../ibm_ace/datadog_checks/ibm_ace/subscription.py) |
| IBM Db2 (`ibm_db2`) | `db`, `username`, `password` | credentials-required — IBM Db2 connects via the `ibm_db` driver and the spec marks `db` (database name), `username`, and `password` as required. The check builds a connection string from these and calls `ibm_db.connect`, so monitoring is not possible without credentials and a specific catalogued/uncataloged database name. Pattern D (spec-required credentials). | [spec](../ibm_db2/assets/configuration/spec.yaml), [check](../ibm_db2/datadog_checks/ibm_db2/ibm_db2.py), [readme](../ibm_db2/README.md) |
| IBM i ⚠ (`ibm_i`) | `username`, `password`, `driver` | credentials-required — IBM i (formerly AS/400) is queried over ODBC using a proprietary driver (`iSeries Access ODBC Driver` or equivalent). The check spawns a Python subprocess that opens a pyodbc connection and runs SQL against `SYSIBMADM` views. It needs (a) a user-profile name + password (or a raw `connection_string`), and (b) a host-side ODBC driver installed and registered. Neither is discoverable from the wire: the credentials must be provisioned out of band, and the driver must be installed by the operator. Pattern D plus a host-prerequisite check. None of `username`, `password`, `driver`, or `connection_string` are flagged `required: true` in the spec, but in practice authentication is not optional — without one of them the check cannot connect. | [spec](../ibm_i/assets/configuration/spec.yaml), [check](../ibm_i/datadog_checks/ibm_i/check.py), [upstream](https://www.ibm.com/docs/en/i/7.4?topic=services-odbc-driver) |
| IBM MQ (`ibm_mq`) | `channel`, `queue_manager` | credentials-required — IBM MQ requires the proprietary `pymqi` client library and connects to a queue manager over a named SVRCONN channel. The two required fields (`channel`, `queue_manager`) are MQ-administrative identifiers that are not discoverable from the wire — every site picks its own naming. Production deployments add username/password and/or SSL with a key repository and cipher spec. None of this can be discovered by probing the listener port (1414); even discovering the existence of an MQ listener does not yield a working configuration. | [spec](../ibm_mq/assets/configuration/spec.yaml), [check](../ibm_mq/datadog_checks/ibm_mq/ibm_mq.py), [upstream](https://www.ibm.com/docs/en/ibm-mq) |
| IBM Spectrum LSF (`ibm_spectrum_lsf`) | `cluster_name` | other — IBM Spectrum LSF is a HPC workload scheduler. The check executes vendor CLI binaries (`lsid`, `lsclusters`, `lshosts`, `bhosts`, `lsload`, `bqueues`, `bjobs`, `bhist`, `bslots`, `badmin perfmon`, etc.) via `subprocess.run` and parses their output - it does not speak any network protocol. To run, the Agent host must (a) have the LSF client binaries installed and on PATH, (b) be configured as an LSF submission/management host with valid LSF environment (LSF_TOP, LSF_ENVDIR, profile.lsf sourced) so the binaries authenticate against the cluster master, and (c) supply the operator-chosen `cluster_name`. Pattern L (local CLI subprocess) plus an unknowable user-policy field (cluster name) makes this not autodiscoverable: the Agent cannot infer which LSF cluster name to attach without either reading LSF config files or being told. | [spec](../ibm_spectrum_lsf/assets/configuration/spec.yaml), [check](../ibm_spectrum_lsf/datadog_checks/ibm_spectrum_lsf/check.py), [check](../ibm_spectrum_lsf/datadog_checks/ibm_spectrum_lsf/client.py), [readme](../ibm_spectrum_lsf/README.md) |
| IBM WAS (`ibm_was`) | `servlet_url` | credentials-required — Despite the JMX/WebSphere reputation, this check is an HTTP scraper of WebSphere's PerfServlet (`requests.get(servlet_url)` parsing PMI XML), not JMXFetch. However the path it scrapes (`/wasPerfTool/servlet/perfservlet`) requires the operator to first deploy `PerfServletApp.ear` from `<WAS_HOME>/installableApps`, switch the PMI statistic set from `Basic` to `All` via the WebSphere admin console, and (per the README) enable application security, which forces basic auth. The port (typically 9080 or 9443) varies by WAS profile, the servlet context root is operator-chosen, and authentication credentials are required once application security is on. None of this is recoverable from a generic HTTP probe. | [spec](../ibm_was/assets/configuration/spec.yaml), [check](../ibm_was/datadog_checks/ibm_was/ibm_was.py), [readme](../ibm_was/README.md) |
| iboss (`iboss`) | — | other — iboss is a logs-only vendor security tile (Secure Web Gateway / Zero Trust). The integration ships no Python check class - the `datadog_checks/iboss/` directory contains only `__about__.py`, `__init__.py`, and a sample conf - and the spec.yaml defines no `instances` block at all, only a `logs` template that listens for syslog (`type: tcp/udp`) on a user-chosen port. There is no upstream service to probe, no metric endpoint, and no host/port the Agent would dial out to. Setup is operator-driven: configure the iboss appliance to forward syslog to the Agent and wire dashboards/SIEM rules. This is pattern N (logs-only / instrumentation), so auto-config in the network-probe sense does not apply. | [spec](../iboss/assets/configuration/spec.yaml), [readme](../iboss/README.md), [other](../iboss/manifest.json) |
| Ignite (`ignite`) | `host`, `port` | credentials-required — Ignite is a JMXFetch-based integration: the spec inherits `init_config/jmx` and `instances/jmx`, with no Python check class — collection happens through JMXFetch using a shipped `metrics.yaml` MBean list. Per the procedure rubric, JMXFetch checks always need host + port and, in any production-shaped deployment, JMX authentication credentials (user/password) and likely SSL trust material. Credentials cannot be discovered from the wire. | [spec](../ignite/assets/configuration/spec.yaml), [other](../ignite/datadog_checks/ignite/data/metrics.yaml), [readme](../ignite/README.md) |
| Ivanti Connect Secure (`ivanti_connect_secure`) | — | other — Ivanti Connect Secure is a logs-only vendor security tile. The spec only defines a `logs` template (TCP/UDP syslog listener on a user-supplied port) and ships no Agent metric check (`datadog_checks/ivanti_connect_secure/` contains only `__about__.py` and `__init__.py`, with no `<name>.py`). There is no upstream HTTP/JMX/OpenMetrics endpoint to discover; the Agent passively receives forwarded syslog from the Ivanti appliance. Auto-config in the network-probe sense does not apply — the appliance must be configured out-of-band to ship logs to the Agent's listener port chosen by the operator. | [spec](../ivanti_connect_secure/assets/configuration/spec.yaml), [readme](../ivanti_connect_secure/README.md) |
| JBoss/WildFly (`jboss_wildfly`) | `jmx_url` | credentials-required — JBoss/WildFly is a JMX integration handled by JMXFetch. The spec marks `jmx_url` as required (e.g. `service:jmx:remote+http://<HOST>:<PORT>` for WildFly 10+, `service:jmx:http-remoting-jmx://...` for 9 and older), and the spec also mandates a custom client JAR (`custom_jar_paths` pointing at `jboss-cli-client.jar`). Production JBoss/WildFly management interfaces require authentication (a ManagementRealm user provisioned via `add-user.sh`), so `user`/`password` are effectively required. None of these (URL scheme variant, client JAR path, management realm credentials) can be inferred from a wire probe. | [spec](../jboss_wildfly/assets/configuration/spec.yaml), [readme](../jboss_wildfly/README.md), [upstream](https://docs.wildfly.org/30/Admin_Guide.html#JMX) |
| journald (`journald`) | — | other — journald is a logs-only integration: there is no Agent metric check (no `journald.py`), only a logs configuration template. The Agent's logs subsystem reads the systemd journal locally via `/var/log/journal` or `/run/log/journal`. There is no network endpoint, no host/port, no authentication. The integration's required input is the operator's intent — which units to include/exclude and what `source`/`service` tags to apply — which is policy, not a discoverable attribute. Discovery would also have to verify that the Agent runs on a host with systemd-journald and that `dd-agent` is in the `systemd-journal` group, both of which are out-of-band setup steps. Closest existing patterns: pattern N (DogStatsD/instrumentation-only — non-probe integration) and pattern J (user supplies the configuration template). | [spec](../journald/assets/configuration/spec.yaml), [readme](../journald/README.md) |
| Juniper SRX Firewall (`juniper_srx_firewall`) | — | other — Juniper SRX Firewall is a logs-only vendor security tile. The spec only defines a `logs` template (UDP syslog listener on a user-supplied port, tagged `ndm_syslog:true`) and ships no Agent metric check (`datadog_checks/juniper_srx_firewall/` contains only `__about__.py` and `__init__.py`). There is no upstream service endpoint for the Agent to probe; logs are forwarded from the SRX device to the Agent's syslog listener. Auto-config in the network-probe sense does not apply — the operator must point the firewall at the Agent's listener port out-of-band. | [spec](../juniper_srx_firewall/assets/configuration/spec.yaml), [readme](../juniper_srx_firewall/README.md) |
| Kafka (`kafka`) | `host`, `port` | credentials-required — The `kafka` integration is the JMX-based Kafka broker check (distinct from `kafka_consumer`). The spec inherits `template: instances/jmx` and forces `is_jmx: true`, which routes collection through JMXFetch against the broker's JMX RMI port (the spec's example uses 9999; broker JMX ports are operator-chosen and not universal). Per the procedure's pattern D / pattern I, JMXFetch integrations require a JMX endpoint that, in production, is gated by JMX authentication (user + password) and is typically bound to localhost or a private interface unless `com.sun.management.jmxremote.*` flags are explicitly set. Neither the JMX port nor the credentials can be derived from the wire, so generic auto-config is not feasible. | [spec](../kafka/assets/configuration/spec.yaml), [readme](../kafka/README.md) |
| Kafka Actions (`kafka_actions`) | `remote_config_id`, `kafka_connect_str` | other — Kafka Actions is not a discovery-style metric check. The README states explicitly that the integration is exclusively triggered by Remote Configuration and should never be scheduled manually. The spec marks `remote_config_id` as required and notes 'This ID is automatically set by the Datadog backend via remote configuration. This parameter is REQUIRED — the integration will not start without it.' The check (`KafkaActionsCheck` extending `AgentCheck`) executes one of seven imperative admin actions (read_messages, create_topic, delete_topic, produce_message, etc.) against a user-supplied `kafka_connect_str` bootstrap server, with action-specific payload (cluster ID, topic name, base64-encoded message bytes) supplied by the operator. There is no upstream `kafka_actions` service to probe — the user's intent (which action, which topic, which payload) is the missing input, and the credential/transport fields (SASL, mTLS, OAuth) are user-provided. Pattern N / pattern J apply. | [spec](../kafka_actions/assets/configuration/spec.yaml), [check](../kafka_actions/datadog_checks/kafka_actions/check.py), [readme](../kafka_actions/README.md) |
| Keycloak (`keycloak`) | — | other — The Keycloak integration is logs-only: spec.yaml defines a single `logs` block (TCP/UDP syslog listener) and no `instances` block, and the package ships no Agent check class — there is no live endpoint for an auto-config probe to hit. The user must configure Keycloak to forward syslog to a port the Agent listens on, which is user policy, not a discoverable property. This matches the pattern N (instrumentation/log-only) shape. | [spec](../keycloak/assets/configuration/spec.yaml), [readme](../keycloak/README.md) |
| Linux Audit Logs (`linux_audit_logs`) | — | other — Linux Audit Logs is a logs-only integration. The spec contains a single `logs` template tailing `/var/log/audit/audit.log` (source/service `linux-audit-logs`); there is no Agent metric check (no check.py — `datadog_checks/linux_audit_logs/` only has `__about__.py` and `__init__.py`). There is no host/port/URL/credential to discover and no per-instance options that drive endpoint resolution. Auto-config in the network-probe sense does not apply: the only "discovery" needed is whether the Agent host has auditd running and writing to the standard audit log path, which is a host-local presence check tied to a fixed file path rather than a probe. Treat as impossible like other logs-only / instrumentation-only integrations (pattern N). | [spec](../linux_audit_logs/assets/configuration/spec.yaml), [readme](../linux_audit_logs/README.md), [other](../linux_audit_logs/datadog_checks/linux_audit_logs/__init__.py) |
| Mac Audit Logs (`mac_audit_logs`) | `MONITOR`, `AUDIT_LOGS_DIR_PATH` | other — Mac Audit Logs is a logs-only integration. The check runs locally on a macOS host with BSM auditing enabled and shells out to `auditreduce` / `praudit` (via sudo) to parse files in /var/audit. There is no remote network endpoint to probe — discovery would reduce to 'is BSM auditing enabled and is the agent running on a Mac with sudo access to auditreduce?'. The required `AUDIT_LOGS_DIR_PATH` is conventionally /var/audit but the operating model is host-local log file ingestion, not a service probe; auto-config in the network-probe sense does not apply. | [spec](../mac_audit_logs/assets/configuration/spec.yaml), [check](../mac_audit_logs/datadog_checks/mac_audit_logs/check.py), [readme](../mac_audit_logs/README.md) |
| MapR (`mapr`) | — | credentials-required — MapR is the Hadoop distribution. The check consumes metrics from a MapR Streams (Kafka-API) topic at `<stream_path>/<stream_id>:<hostname>` using the MapR-flavoured `confluent_kafka` library. On secured clusters (the production default) the consumer must authenticate with a MapR ticket file passed via `ticket_location` or the `MAPR_TICKETFILE_LOCATION` environment variable; the check warns if neither is set. The ticket is a per-user credential issued by `maprlogin` on the cluster, not something discoverable over the wire. The integration also depends on the `mapr-streams-python` package and on `LD_LIBRARY_PATH` pointing at the MapR client libraries, which is host provisioning state. Pattern D / I. | [spec](../mapr/assets/configuration/spec.yaml), [check](../mapr/datadog_checks/mapr/mapr.py) |
| MarkLogic (`marklogic`) | `url`, `username`, `password` | credentials-required — MarkLogic is a NoSQL DB whose Management REST API on port 8002 (and the health view) is gated behind a user with the `manage-user` (or `manage-admin` for health checks) role. The check authenticates via the standard `instances/http` template (username, password, auth_type). The `manage-admin` permission is explicitly required for health service checks per the spec. Production deployments cannot be probed without credentials. Pattern D. | [spec](../marklogic/assets/configuration/spec.yaml), [check](../marklogic/datadog_checks/marklogic/check.py), [upstream](https://docs.marklogic.com/REST/management) |
| Microsoft DNS (`microsoft_dns`) | — | other — Microsoft DNS is a logs-only integration (despite the hint suggesting a perf-counters shape). The spec contains only a `logs` template that wires the Windows Event Log channel `Microsoft-Windows-DNSServer/Audit` to Datadog with `type: windows_event`; there is no `instances` block, no Agent metric check, and no check.py implementation. The README explicitly states 'The Microsoft DNS integration does not include any metrics.' Discovery reduces to 'is the DNS Server role installed and is the audit channel enabled on this Windows host?', but there is no network probe and no metric check to dispatch — the integration is purely a Windows Event Log forwarder. | [spec](../microsoft_dns/assets/configuration/spec.yaml), [readme](../microsoft_dns/README.md) |
| Microsoft Sysmon (`microsoft_sysmon`) | — | other — Microsoft Sysmon is a logs-only integration. The spec contains only a `logs` template that forwards the Windows Event Log channel `Microsoft-Windows-Sysmon/Operational` via `type: windows_event`; there is no `instances` block, no Agent metric check, and no check.py implementation. There is no network endpoint to probe and no metric check to dispatch — auto-config in the network-probe sense does not apply. Discovery reduces to 'is Sysmon installed and is its operational event channel emitting on this Windows host?'. | [spec](../microsoft_sysmon/assets/configuration/spec.yaml), [readme](../microsoft_sysmon/README.md) |
| MySQL (`mysql`) | `host`, `username`, `password` | credentials-required — MySQL needs a dedicated `datadog` user with explicit grants and a password to read performance schema and replication state. The credentials cannot be discovered from the wire — they must be provisioned in the database first. | [spec](../mysql/assets/configuration/spec.yaml) |
| OpenLDAP (`openldap`) | `url` | credentials-required — The check binds to slapd (`ldap3.Connection(...).bind()`) and reads the `cn=Monitor` backend. While the spec marks `username`/`password` as optional (anonymous bind syntactically works), the README and OpenLDAP defaults require an explicit Distinguished Name with read access provisioned via ACL on the monitor database. The setup section walks the operator through `slappasswd`, `ldapadd`, and an `olcAccess` rule for a dedicated monitoring user. Without those credentials the bind will succeed but the search of `cn=Monitor` returns no entries. Like other auth-gated databases (postgres, mysql), there is no production-shaped deployment where unauthenticated discovery yields metrics. | [spec](../openldap/assets/configuration/spec.yaml), [check](../openldap/datadog_checks/openldap/openldap.py), [readme](../openldap/README.md), [upstream](https://www.openldap.org/doc/admin24/monitoringslapd.html) |
| OpenMetrics (`openmetrics`) | `openmetrics_endpoint`, `namespace`, `metrics` | other — The OpenMetrics integration is a generic, user-supplied scraper for arbitrary Prometheus / OpenMetrics endpoints. It has no upstream system to discover — the user MUST provide `openmetrics_endpoint` (or legacy `prometheus_url`), a `namespace`, and a `metrics` allowlist that names which series to collect. There is no canonical port, path, or default that a probe could derive: any of the dozens of integrations elsewhere in this repo is a more specific instance of OpenMetrics. By definition this integration is the user-config-required fallback, so auto-config cannot apply. | [spec](../openmetrics/assets/configuration/spec.yaml), [check](../openmetrics/datadog_checks/openmetrics/openmetrics.py), [readme](../openmetrics/README.md) |
| OpenStack (`openstack`) | `keystone_server_url`, `name`, `user` | credentials-required — The legacy OpenStack check authenticates against Keystone Identity API v3 to obtain a scoped/unscoped token, then walks Nova / Neutron service catalogs. The required user field is a structured object {password, name, domain.id} that must match an OpenStack identity, and keystone_server_url points at a tenant-specific Keystone endpoint that varies per deployment. Neither the URL nor the credentials can be discovered from the wire — they are tenant configuration. No unauthenticated mode exists. | [spec](../openstack/assets/configuration/spec.yaml), [check](../openstack/datadog_checks/openstack/openstack.py) |
| OpenStack Controller (`openstack_controller`) | `keystone_server_url`, `user` | credentials-required — The OpenStack Controller check authenticates against Keystone (Identity API v3) using a username/password/domain triple (or an openstack-sdk clouds.yaml file referenced via openstack_config_file_path / openstack_cloud_name). The check then walks every component endpoint (Nova, Neutron, Cinder, Ironic, Octavia, Glance, Heat, Swift) using a scoped/unscoped token. Without credentials and a Keystone URL, no API call succeeds. The Keystone URL itself is also site-specific (custom hostname/port), so it cannot be probed generically. | [spec](../openstack_controller/assets/configuration/spec.yaml), [check](../openstack_controller/datadog_checks/openstack_controller/openstack_controller.py), [readme](../openstack_controller/README.md) |
| PgBouncer (`pgbouncer`) | `host`, `username` | credentials-required — PgBouncer speaks the Postgres wire protocol on its admin port (default 6432) and exposes statistics via `SHOW` admin queries against the virtual `pgbouncer` database. The check raises ConfigurationError unless either `database_url` is set, or both `host` and `username` are provided. Even though the password may be empty for `localhost` ident auth, any production deployment requires a stats user defined in `pgbouncer.ini`'s `stats_users` (or the legacy `admin_users`) — that user and its password are out-of-band configuration that cannot be discovered from the wire. | [spec](../pgbouncer/assets/configuration/spec.yaml), [check](../pgbouncer/datadog_checks/pgbouncer/pgbouncer.py), [upstream](https://www.pgbouncer.org/config.html) |
| Postgres (`postgres`) | `host`, `port`, `username`, `password` | credentials-required — Postgres requires a dedicated `datadog` user with explicit grants and a password to read the statistics views. The credentials cannot be discovered from the wire; they must be provisioned out-of-band. Once provided, host/port have universal defaults but the auth blocker dominates classification. | [spec](../postgres/assets/configuration/spec.yaml) |
| PowerDNS Recursor (`powerdns_recursor`) | `host`, `port`, `api_key` | credentials-required — The PowerDNS Recursor check hits the recursor's webserver statistics API (`/api/v1/servers/localhost/statistics` on v4, `/servers/localhost/statistics` on v3). The webserver is disabled by default; when the operator enables it they must also set `api-key=<secret>` in `recursor.conf`, and every request must carry that key in the `X-API-Key` header. The spec marks `api_key` as `required: true` with `secret: true` and the check sets `self.http.options['headers']['X-API-Key'] = self.instance['api_key']` in `__init__`. There is no unauthenticated mode, so auto-config cannot synthesize a working instance from the wire alone — pattern D. | [spec](../powerdns_recursor/assets/configuration/spec.yaml), [check](../powerdns_recursor/datadog_checks/powerdns_recursor/powerdns_recursor.py), [readme](../powerdns_recursor/README.md), [upstream](https://doc.powerdns.com/recursor/http-api/index.html) |
| Presto (`presto`) | `host`, `port` | credentials-required — Presto is a JMXFetch-only integration: the spec inherits `template: instances/jmx`, the Python package has no check class (only `__version__`), and the shipped auto_conf.yaml sets `is_jmx: true` with `%%host%%` / `%%port%%` templating. JMXFetch connects to the Presto coordinator's JMX RMI port (example 9999); production Presto / Trino clusters that enable the JMX agent require JMX authentication (user + password). Per the procedure's JMXFetch shortcut and pattern D, classify as impossible: JMX credentials and TLS material cannot be derived from a wire probe. | [spec](../presto/assets/configuration/spec.yaml), [auto_conf](../presto/datadog_checks/presto/data/auto_conf.yaml), [check](../presto/datadog_checks/presto/__init__.py), [readme](../presto/README.md) |
| Prometheus (`prometheus`) | `prometheus_url`, `namespace`, `metrics` | other — This is the user-supplied Generic Prometheus check (`GenericPrometheusCheck`). It is not tied to any single upstream system: the user must point it at an arbitrary application's Prometheus endpoint, choose a metric `namespace`, and explicitly enumerate the `metrics` to scrape (with optional renaming). None of these can be inferred from the wire — the URL, namespace, and metric allow-list are inherently per-application configuration decisions. There is no canonical port, path, or service to probe. | [spec](../prometheus/assets/configuration/spec.yaml), [check](../prometheus/datadog_checks/prometheus/prometheus.py), [readme](../prometheus/README.md) |
| ProxySQL (`proxysql`) | `host`, `port`, `username`, `password` | credentials-required — ProxySQL exposes its admin interface via the MySQL wire protocol on a separate admin port. The check connects with pymysql and queries stats_* tables in the admin schema, so a username and password (the ProxySQL admin user) are required. Both are marked required: true with no defaults in the spec, and there is no unauthenticated mode. Default port is also user-supplied (no default) — typically 6032, but not enforced by the check. | [spec](../proxysql/assets/configuration/spec.yaml), [check](../proxysql/datadog_checks/proxysql/proxysql.py), [readme](../proxysql/README.md) |
| RiakCS (`riakcs`) | `access_id`, `access_secret` | credentials-required — RiakCS is the S3-compatible Riak fork. The check uses boto3 to issue an S3 GetObject against the special `riak-cs` bucket, which requires an `access_id` and `access_secret` pair from the user's RiakCS deployment. These are tenant-specific credentials that cannot be obtained via wire-level probing. Pattern D (spec-required credentials). | [spec](../riakcs/assets/configuration/spec.yaml), [check](../riakcs/datadog_checks/riakcs/riakcs.py) |
| SAP HANA (`sap_hana`) | `server`, `username`, `password` | credentials-required — The SAP HANA check connects to the HANA database over the SQL protocol via the proprietary `hdbcli` driver and runs SQL queries against monitoring views (`M_*`, `SYS_DATABASES.*`). It requires a user with `MONITORING` (or equivalent) role privileges granted ahead of time. The spec marks `server`, `username`, and `password` as `required: true`, with `password` flagged `secret: true`. There is no unauthenticated query path on HANA — this is pattern D (spec-required credentials), the same shape as postgres / mysql / sqlserver. | [spec](../sap_hana/assets/configuration/spec.yaml), [check](../sap_hana/datadog_checks/sap_hana/sap_hana.py), [readme](../sap_hana/README.md), [upstream](https://help.sap.com/docs/SAP_HANA_PLATFORM) |
| Sidekiq (`sidekiq`) | — | other — Sidekiq is not an Agent metric check at all. The integration is wired through DogStatsD: the application emits StatsD metrics via the dogstatsd-ruby client and the Agent ingests them through its DogStatsD listener. The only Agent-side configuration in the spec is a logs file path (`/var/log/sidekiq.log`). There is no host/port/endpoint to discover and no check.py; auto-config in the network-probe sense does not apply. Real setup requires user code changes (gem install, initializer, dogstatsd_mapper_profiles in datadog.yaml), which can't be inferred from the wire. | [spec](../sidekiq/assets/configuration/spec.yaml), [readme](../sidekiq/README.md) |
| Silk (`silk`) | `host_address`, `username`, `password` | credentials-required — The Silk check builds a base URL `<host_address>/api/v2/` and issues authenticated HTTPS GETs to the Silk Cloud Data Platform REST API (`/system/state`, `/system/servers`, etc.). The spec only marks `host_address` as `required: true`, but the upstream Silk SDP REST API gates every endpoint behind the user's Silk credentials supplied via the standard HTTP `username`/`password` template, and the integration's documented setup requires a dedicated read-only Silk user. No unauthenticated path exists, so a wire probe cannot bring up the integration. | [spec](../silk/assets/configuration/spec.yaml), [check](../silk/datadog_checks/silk/check.py), [check](../silk/datadog_checks/silk/constants.py) |
| SingleStore (`singlestore`) | `host`, `username`, `password` | credentials-required — SingleStore (formerly MemSQL) speaks the MySQL wire protocol (default port 3306). The check opens a `pymysql` connection and runs SQL against `MV_GLOBAL_STATUS`, the `AGGREGATORS`/`LEAVES` cluster views, and optionally the `MV_SYSINFO_*` tables. Although `username`/`password` are technically marked optional in the spec, no production SingleStore cluster permits anonymous SQL, and the queries the check runs require a user with metadata-read privileges. Per pattern D and the decision rule for auth-optional specs (no realistic unauthenticated mode), this is `impossible` / `credentials-required`. | [spec](../singlestore/assets/configuration/spec.yaml), [check](../singlestore/datadog_checks/singlestore/check.py), [other](../singlestore/datadog_checks/singlestore/queries.py), [readme](../singlestore/README.md) |
| SNMP (`snmp`) | `ip_address` | credentials-required — SNMP needs at minimum the device IP and a credential: a community string (v1/v2c) or a v3 user with auth/priv keys. Community strings could occasionally be guessed (`public`/`private`) but production devices have unique strings, and v3 is the modern default. The integration has its own network-discovery feature (`network_address` + profile auto-detection) but it still needs a credential to authenticate. Classification is impossible for credential discovery; SNMP-internal device discovery is orthogonal. | [spec](../snmp/assets/configuration/spec.yaml), [upstream](https://datatracker.ietf.org/doc/html/rfc3411) |
| Solr (`solr`) | `host`, `port` | credentials-required — Solr is collected via the JMXFetch helper (`template: instances/jmx`). Production Solr deployments enable JMX Remote with authentication and frequently TLS, requiring `user`, `password`, and key/trust-store paths. The example port (9999) is not a true convention — JMX listening ports are deployment-specific. Even a host+port probe cannot validate the JMX endpoint without credentials. Per the procedure's pattern I (JMXFetch always needs host+port+user+password), Solr falls into the impossible bucket. | [spec](../solr/assets/configuration/spec.yaml), [readme](../solr/README.md), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml) |
| SonarQube (`sonarqube`) | `web_endpoint` | credentials-required — SonarQube has two distinct collection modes: (1) a web API instance using `web_endpoint` (typically http://host:9000) plus basic auth, and (2) a JMX instance using JMXFetch with multiple JVMs (web, ce, search) on user-defined ports. The web API requires credentials in any non-trivial deployment, and the README confirms `username`/`password` are needed. The JMX side requires host+port+user+password and the ports are not standardized (10443/10444/10445 are documentation examples). Either path requires credentials, so auto-config cannot complete from the wire. | [spec](../sonarqube/assets/configuration/spec.yaml), [check](../sonarqube/datadog_checks/sonarqube/check.py), [readme](../sonarqube/README.md) |
| SQL Server (`sqlserver`) | `host` | credentials-required — SQL Server requires either SQL authentication (username + password for a dedicated `datadog` user with `VIEW SERVER STATE` and other grants) or Windows authentication (the Agent's process identity must have those grants on the SQL Server instance). Either form is provisioned out-of-band; the credentials cannot be derived from the wire. Default port 1433 is universal but irrelevant given the auth blocker. | [spec](../sqlserver/assets/configuration/spec.yaml), [upstream](https://learn.microsoft.com/en-us/sql/relational-databases/system-dynamic-management-views/system-dynamic-management-views) |
| SSH/SFTP (`ssh_check`) | `host`, `username` | credentials-required — The SSH check uses paramiko.SSHClient.connect with a required username and either password or private key, then verifies transport.is_authenticated(). Without credentials the check cannot exercise its core service check (ssh.can_connect) nor the SFTP probe, so auto-config cannot meaningfully enable it. Detecting an SSH server on port 22 is trivial, but it is also out-of-scope - the integration is designed to validate authenticated access for a specific user. | [spec](../ssh_check/assets/configuration/spec.yaml), [check](../ssh_check/datadog_checks/ssh_check/ssh_check.py), [readme](../ssh_check/README.md) |
| TeamCity (`teamcity`) | `server` | credentials-required — TeamCity is a JetBrains CI server reached over HTTP at a per-deployment hostname (e.g. `http://teamcity.<ACCOUNT_NAME>.com`). The spec marks `server` as required with no canonical port or path, and the README requires the operator to either enable Guest authentication on the TeamCity server (server-side configuration that can't be probed) or supply `username`/`password` for HTTP basic auth. The REST API instance further needs an explicit `projects`/`build_configuration` mapping that defines what to monitor, which is a user policy choice. No combination of host/port probes can stand in for a TeamCity URL plus credentials plus project filters. | [spec](../teamcity/assets/configuration/spec.yaml), [check](../teamcity/datadog_checks/teamcity/check.py), [readme](../teamcity/README.md) |
| Tenable (Nessus) (`tenable`) | — | other — Tenable ships only a logs configuration that tails Nessus log files (backend.log, www_server.log) - there is no Agent check class, no metrics endpoint, and no network probe. Auto-config in the network-probe sense does not apply: enabling the integration is purely a question of whether Nessus log files exist at the expected paths on the host, and which API access keys the user wants to use for any external Tenable APIs. | [spec](../tenable/assets/configuration/spec.yaml), [readme](../tenable/README.md) |
| Teradata (`teradata`) | `server`, `database`, `username`, `password` | credentials-required — The Teradata check connects via the `teradatasql` driver (port 1025 by default) and runs SQL queries (DBC views, ResUsageSpma, AllSpaceV) under a dedicated Datadog database user. While the spec marks only `server` and `database` as required, every supported `auth_mechanism` (TD2, TDNEGO, LDAP, KRB5, JWT) needs at minimum a username plus either a password or a JWT/Kerberos token in `auth_data`. The integration also requires the user to grant SELECT on internal Teradata views before any metric works. None of this can be discovered from a wire probe. | [spec](../teradata/assets/configuration/spec.yaml), [check](../teradata/datadog_checks/teradata/check.py), [check](../teradata/datadog_checks/teradata/queries.py) |
| TokuMX (`tokumx`) | `server` | credentials-required — TokuMX is a deprecated MongoDB fork. The check uses a vendored pymongo client (`MongoClient`) and requires a full MongoDB URI in the form `mongodb://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>`. Authenticated access to MongoDB-style admin commands (`serverStatus`, replica-set state) is the norm in production deployments, and the URI must encode credentials. Like the `mongo` integration, real deployments fall through to credential discovery, which is out of scope for wire-only autodiscovery. Pattern D / E. | [spec](../tokumx/assets/configuration/spec.yaml), [check](../tokumx/datadog_checks/tokumx/tokumx.py) |
| Tomcat (`tomcat`) | `host`, `port`, `user`, `password` | credentials-required — Tomcat is a JMX-based check that inherits JMXFetch (template: instances/jmx). The shipped auto_conf.yaml supplies host/port via Autodiscovery (%%host%%:9012), but production Tomcat JMX endpoints are almost always secured with JMX user/password authentication (set via -Dcom.sun.management.jmxremote.authenticate=true and a passwords file). Per procedure pattern D and the documented rule that JMXFetch integrations always need host+port+user+password, this falls into impossible/credentials-required. The localhost-no-auth case works only for unsecured dev/test servers. | [spec](../tomcat/assets/configuration/spec.yaml), [auto_conf](../tomcat/datadog_checks/tomcat/data/auto_conf.yaml), [readme](../tomcat/README.md) |
| Prisma Cloud Compute (Twistlock) (`twistlock`) | `url`, `username`, `password` | credentials-required — The Twistlock check targets the Prisma Cloud Compute Console API and authenticates with basic auth using a Prisma Cloud API access key ID (username) and secret key (password). The spec marks username/password as optional in the http template, but every realistic deployment of the Console enforces auth, and the SaaS variant explicitly requires the access key pair. There is no unauthenticated metrics endpoint to probe, and the URL itself (custom Console hostname / project name) is not discoverable from the wire. | [spec](../twistlock/assets/configuration/spec.yaml), [check](../twistlock/datadog_checks/twistlock/config.py), [check](../twistlock/datadog_checks/twistlock/twistlock.py), [readme](../twistlock/README.md) |
| Vertica (`vertica`) | `server`, `port`, `username`, `password`, `db` | credentials-required — Vertica is a relational database. The check uses `vertica_python` to open a SQL connection on port 5433 and queries the `v_monitor` / `v_catalog` schemas. A username and password are required to authenticate — the spec's example placeholders (`<USERNAME>`, `<PASSWORD>`) signal that these are user-supplied even though the YAML doesn't mark them with `required: true`. There is no anonymous mode. Pattern D — standard spec-required database credentials. | [spec](../vertica/assets/configuration/spec.yaml), [check](../vertica/datadog_checks/vertica/vertica.py), [readme](../vertica/README.md) |
| VoltDB (`voltdb`) | `url`, `username`, `password` | credentials-required — The VoltDB check posts to the JSON HTTP client interface (default port 8080) and invokes system procedures such as @Statistics and @SystemInformation. The spec overrides the standard HTTP template to mark both `username` and `password` as required, and the check passes them to its Client to authenticate every request (with optional `password_hashed` to indicate the password is already SHA-1 hashed). Without a user provisioned in VoltDB's deployment file, no stats are reachable, so credentials cannot come from a wire probe. | [spec](../voltdb/assets/configuration/spec.yaml), [check](../voltdb/datadog_checks/voltdb/check.py), [check](../voltdb/datadog_checks/voltdb/client.py) |
| vSphere (`vsphere`) | `host`, `username`, `password` | credentials-required — vSphere requires read-only vCenter credentials (host + username + password) to authenticate against the vSphere SOAP and REST APIs. There is no unauthenticated mode and no fixed endpoint that returns useful data without a valid session. The credentials cannot be discovered from the wire. | [spec](../vsphere/assets/configuration/spec.yaml), [check](../vsphere/datadog_checks/vsphere/vsphere.py) |
| WebLogic (`weblogic`) | `host`, `port`, `user`, `password` | credentials-required — WebLogic is a JMXFetch-based integration. The spec inherits `template: instances/jmx` and the README mandates enabling JMX on the WebLogic Platform MBean Server with `-Djavax.management.builder.initial=weblogic.management.jmx.mbeanserver.WLSMBeanServerBuilder`. Production WebLogic JMX endpoints require authentication (a WebLogic admin user/password). Per pattern I (JMXFetch integrations are categorically impossible), credentials and the specific JMX setup cannot be derived from a wire probe. | [spec](../weblogic/assets/configuration/spec.yaml), [readme](../weblogic/README.md), [upstream](https://docs.oracle.com/en/middleware/standalone/weblogic-server/14.1.1.0/jmxcu/index.html) |
| Windows Performance Counters (`windows_performance_counters`) | `metrics` | other — This is a generic, user-supplied template check — not a check tied to any specific upstream service. The required `metrics` field is a mapping of PDH counter sets (object name + counters + tag_name + instance_counts) that the user defines for whatever Windows component they want to monitor. There is no canonical service, port, or endpoint to discover; without a user-provided counter list there is nothing for the check to do. The check class (`PerfCountersBaseCheck`) is the same machinery used by integrations like `iis` and `active_directory`, but here the integration is by definition a configuration template. Auto-config cannot synthesize the counter list from the wire. | [spec](../windows_performance_counters/assets/configuration/spec.yaml), [check](../windows_performance_counters/datadog_checks/windows_performance_counters/check.py), [readme](../windows_performance_counters/README.md) |
| WMI (`wmi_check`) | `class`, `metrics` | other — wmi_check is a generic, schema-driven WMI bridge: the user must declare which WMI `class` to query and which `[<WMI_PROPERTY>, <METRIC_NAME>, <METRIC_TYPE>]` mappings to emit. There is no upstream system to fingerprint — every deployment is bespoke. Unlike `iis` (which targets a known Windows role with fixed perf counters), `wmi_check` has no defaults to discover: discovery would have to invent a class name and a property→metric mapping, which is application-specific by design. Out of scope for auto-config. | [spec](../wmi_check/assets/configuration/spec.yaml), [check](../wmi_check/datadog_checks/wmi_check/wmi_check.py), [upstream](https://learn.microsoft.com/en-us/windows/win32/wmisdk/wmi-start-page) |
