_Generated 2026-04-30. 260 total: 96 generic / 40 custom / 124 impossible / 2 need review (⚠)._

**Sections:** [Fully generic](#fully-generic) · [HTTP probe with integration-specific verification](#http-probe-with-integration-specific-verification) · [TCP probe with integration-specific protocol](#tcp-probe-with-integration-specific-protocol) · [Local detection (no network, no credentials)](#local-detection-no-network-no-credentials) · [Credentials required](#credentials-required) · [No probe surface](#no-probe-surface)

## Fully generic

_No integration-specific verification code; the discovery layer carries at most a per-integration port + path table._

### `generic-openmetrics-scan` (51)

Probe a port for `/metrics`, validate Prometheus exposition format. Per-integration data is just port + path.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Aerospike (`aerospike`) | `openmetrics_endpoint` | openmetrics-port-scan — Modern Aerospike monitoring is OpenMetrics-based: the integration is a thin `OpenMetricsBaseCheckV2` subclass (`AerospikeCheckV2`) that scrapes the Aerospike Prometheus Exporter, which conventionally listens on port 9145 at `/metrics`. | [spec](../aerospike/assets/configuration/spec.yaml), [check](../aerospike/datadog_checks/aerospike/check.py), [readme](../aerospike/README.md), [upstream](https://github.com/aerospike/aerospike-prometheus-exporter) |
| Appgate SDP (`appgate_sdp`) | `openmetrics_endpoint` | openmetrics-port-scan — Appgate SDP's check is a minimal OpenMetricsBaseCheckV2 wrapper. | [spec](../appgate_sdp/assets/configuration/spec.yaml), [check](../appgate_sdp/datadog_checks/appgate_sdp/check.py), [readme](../appgate_sdp/README.md) |
| Argo Rollouts (`argo_rollouts`) | `openmetrics_endpoint` | openmetrics-port-scan — Argo Rollouts is a Kubernetes-native progressive delivery controller. | [spec](../argo_rollouts/assets/configuration/spec.yaml), [check](../argo_rollouts/datadog_checks/argo_rollouts/check.py), [readme](../argo_rollouts/README.md), [upstream](https://argo-rollouts.readthedocs.io/en/stable/features/controller-metrics/) |
| Argo Workflows (`argo_workflows`) | `openmetrics_endpoint` | openmetrics-port-scan — Argo Workflows is a Kubernetes-native workflow engine. | [spec](../argo_workflows/assets/configuration/spec.yaml), [check](../argo_workflows/datadog_checks/argo_workflows/check.py), [readme](../argo_workflows/README.md), [upstream](https://argo-workflows.readthedocs.io/en/latest/metrics/) |
| ArgoCD (`argocd`) | — | openmetrics-port-scan — ArgoCD ships several components, each exposing Prometheus metrics on a fixed well-known port: Application Controller (8082), ApplicationSet Controller (8080), API Server (8083), Repo Server (8084), Notifications Controller (9001) and Commit Server (8087), all at `/metrics`. | [spec](../argocd/assets/configuration/spec.yaml), [check](../argocd/datadog_checks/argocd/check.py), [readme](../argocd/README.md), [upstream](https://argo-cd.readthedocs.io/en/stable/operator-manual/metrics/) |
| AWS Neuron (`aws_neuron`) | `openmetrics_endpoint` | openmetrics-port-scan — AWS Neuron monitoring relies on the `neuron-monitor` companion tool, which exposes Prometheus-formatted metrics on the well-known port `8000/metrics` on the EC2/Inferentia/Trainium instance. | [spec](../aws_neuron/assets/configuration/spec.yaml), [check](../aws_neuron/datadog_checks/aws_neuron/check.py), [readme](../aws_neuron/README.md), [upstream](https://awsdocs-neuron.readthedocs-hosted.com/en/latest/tools/neuron-sys-tools/neuron-monitor-user-guide.html) |
| Azure IoT Edge (`azure_iot_edge`) | `edge_hub_prometheus_url`, `edge_agent_prometheus_url` | openmetrics-port-scan — Azure IoT Edge runtime exposes Prometheus metrics from two modules (edgeHub and edgeAgent), both on the well-known port 9600 at the canonical /metrics path. | [spec](../azure_iot_edge/assets/configuration/spec.yaml), [check](../azure_iot_edge/datadog_checks/azure_iot_edge/check.py), [readme](../azure_iot_edge/README.md), [upstream](https://learn.microsoft.com/en-us/azure/iot-edge/how-to-access-built-in-metrics) |
| BentoML (`bentoml`) | `openmetrics_endpoint` | openmetrics-port-scan — BentoML serves Prometheus metrics on the model server's HTTP port (default 3000) at the canonical /metrics path. | [spec](../bentoml/assets/configuration/spec.yaml), [check](../bentoml/datadog_checks/bentoml/check.py), [upstream](https://docs.bentoml.com/en/latest/build-with-bentoml/observability/metrics.html) |
| Boundary (`boundary`) | `health_endpoint`, `openmetrics_endpoint` | openmetrics-port-scan — HashiCorp Boundary's controller exposes Prometheus metrics on `/metrics` and a health endpoint on `/health`, both on the controller listener (default port 9203). | [spec](../boundary/assets/configuration/spec.yaml), [check](../boundary/datadog_checks/boundary/check.py), [upstream](https://developer.hashicorp.com/boundary/docs/operations/metrics) |
| Calico (`calico`) | `openmetrics_endpoint` | openmetrics-port-scan — Calico's check is a thin OpenMetricsBaseCheckV2 wrapper around Felix's Prometheus endpoint. The spec uses the standard `instances/openmetrics` template with a single required `openmetrics_endpoint` field; no credentials are involved. | [spec](../calico/assets/configuration/spec.yaml), [check](../calico/datadog_checks/calico/check.py), [readme](../calico/README.md), [upstream](https://docs.tigera.io/calico/3.25/operations/monitor/monitor-component-metrics) |
| Celery (`celery`) | `openmetrics_endpoint` | openmetrics-port-scan — Despite the hint, this integration is not DogStatsD-based: it is a standard OpenMetricsBaseCheckV2 implementation that scrapes the Celery Flower Prometheus metrics endpoint. | [spec](../celery/assets/configuration/spec.yaml), [check](../celery/datadog_checks/celery/check.py), [readme](../celery/README.md), [upstream](https://flower.readthedocs.io/en/latest/prometheus-integration.html) |
| cert-manager (`cert_manager`) | `openmetrics_endpoint` | openmetrics-port-scan — cert-manager is an OpenMetricsBaseCheckV2 integration that scrapes the cert-manager controller's Prometheus endpoint. | [spec](../cert_manager/assets/configuration/spec.yaml), [auto_conf](../cert_manager/datadog_checks/cert_manager/data/auto_conf.yaml), [check](../cert_manager/datadog_checks/cert_manager/cert_manager.py), [upstream](https://cert-manager.io/docs/devops-tips/prometheus-metrics/) |
| Cilium (`cilium`) | `agent_endpoint` | openmetrics-port-scan — Cilium exposes Prometheus metrics on fixed default ports — `cilium-agent` on 9962/metrics and `cilium-operator` on 6942/metrics. | [spec](../cilium/assets/configuration/spec.yaml), [auto_conf](../cilium/datadog_checks/cilium/data/auto_conf.yaml), [check](../cilium/datadog_checks/cilium/check.py), [upstream](https://docs.cilium.io/en/stable/observability/metrics/) |
| CockroachDB (`cockroachdb`) | — | openmetrics-port-scan — CockroachDB exposes a Prometheus-format metrics endpoint at the fixed path `/_status/vars` on its admin HTTP port (default 8080). | [spec](../cockroachdb/assets/configuration/spec.yaml), [check](../cockroachdb/datadog_checks/cockroachdb/check.py), [upstream](https://www.cockroachlabs.com/docs/stable/monitoring-and-alerting.html#prometheus-endpoint) |
| CoreDNS (`coredns`) | `openmetrics_endpoint` | openmetrics-port-scan — CoreDNS exposes Prometheus metrics on `/metrics`, with the prometheus plugin defaulting to port 9153. This is a single canonical OpenMetrics endpoint, well-known and singular — exactly the case a generic OpenMetrics port-scan would handle. | [spec](../coredns/assets/configuration/spec.yaml), [auto_conf](../coredns/datadog_checks/coredns/data/auto_conf.yaml), [upstream](https://coredns.io/plugins/metrics/) |
| CRI-O (`crio`) | `prometheus_url` | openmetrics-port-scan — CRI-O is a Kubernetes container runtime. The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint, defaulting to http://localhost:9090/metrics (the port CRI-O's `metrics_port` config option uses). | [spec](../crio/assets/configuration/spec.yaml), [check](../crio/datadog_checks/crio/crio.py) |
| Datadog Cluster Agent (`datadog_cluster_agent`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint at http://%%host%%:5000/metrics. The existing auto_conf.yaml uses the cluster-agent AD identifier and templates the host. | [spec](../datadog_cluster_agent/assets/configuration/spec.yaml), [auto_conf](../datadog_cluster_agent/datadog_checks/datadog_cluster_agent/data/auto_conf.yaml), [check](../datadog_cluster_agent/datadog_checks/datadog_cluster_agent/check.py) |
| Datadog CSI Driver (`datadog_csi_driver`) | `openmetrics_endpoint` | openmetrics-port-scan — Datadog's own CSI driver exposes Prometheus metrics on a fixed endpoint. | [spec](../datadog_csi_driver/assets/configuration/spec.yaml), [auto_conf](../datadog_csi_driver/datadog_checks/datadog_csi_driver/data/auto_conf.yaml), [check](../datadog_csi_driver/datadog_checks/datadog_csi_driver/check.py) |
| NVIDIA DCGM (`dcgm`) | `openmetrics_endpoint` | openmetrics-port-scan — NVIDIA's dcgm-exporter exposes GPU telemetry as Prometheus metrics on the universally documented port 9400 at `/metrics`. | [spec](../dcgm/assets/configuration/spec.yaml), [check](../dcgm/datadog_checks/dcgm/check.py), [upstream](https://github.com/NVIDIA/dcgm-exporter) |
| etcd (`etcd`) | `prometheus_url` | openmetrics-port-scan — etcd exposes Prometheus metrics on `/metrics` over its client port (default 2379). The auto_conf already lists both `https://` and `http://` variants of the same path. | [spec](../etcd/assets/configuration/spec.yaml), [auto_conf](../etcd/datadog_checks/etcd/data/auto_conf.yaml), [upstream](https://etcd.io/docs/v3.5/op-guide/monitoring/) |
| External DNS (`external_dns`) | `prometheus_url` | openmetrics-port-scan — External DNS is a Kubernetes controller that exposes Prometheus metrics on a fixed path `/metrics` at the conventional default port 7979. | [spec](../external_dns/assets/configuration/spec.yaml), [auto_conf](../external_dns/datadog_checks/external_dns/data/auto_conf.yaml), [check](../external_dns/datadog_checks/external_dns/external_dns.py) |
| Falco (`falco`) | `openmetrics_endpoint` | openmetrics-port-scan — The Falco check is `OpenMetricsBaseCheckV2` and the spec uses the standard `instances/openmetrics` template — a single required `openmetrics_endpoint` and no auth fields. | [spec](../falco/assets/configuration/spec.yaml), [check](../falco/datadog_checks/falco/check.py), [readme](../falco/README.md) |
| Flux (`fluxcd`) | `openmetrics_endpoint` | openmetrics-port-scan — FluxCD is a Kubernetes GitOps controller (helm-controller, kustomize-controller, source-controller, notification-controller). | [spec](../fluxcd/assets/configuration/spec.yaml), [check](../fluxcd/datadog_checks/fluxcd/check.py), [readme](../fluxcd/README.md) |
| Hugging Face TGI (`hugging_face_tgi`) | `openmetrics_endpoint` | openmetrics-port-scan — Hugging Face Text Generation Inference exposes Prometheus metrics on a single canonical `/metrics` path. | [spec](../hugging_face_tgi/assets/configuration/spec.yaml), [check](../hugging_face_tgi/datadog_checks/hugging_face_tgi/check.py), [upstream](https://huggingface.co/docs/text-generation-inference/en/basic_tutorials/monitoring) |
| Karpenter (`karpenter`) | `openmetrics_endpoint` | openmetrics-port-scan — Karpenter is a Kubernetes cluster autoscaler controller that exposes Prometheus metrics on a fixed `/metrics` path. | [spec](../karpenter/assets/configuration/spec.yaml), [check](../karpenter/datadog_checks/karpenter/check.py), [readme](../karpenter/README.md), [upstream](https://karpenter.sh/docs/reference/metrics/) |
| KEDA (`keda`) | `openmetrics_endpoint` | openmetrics-port-scan — KedaCheck inherits OpenMetricsBaseCheckV2 and only requires an openmetrics_endpoint. | [spec](../keda/assets/configuration/spec.yaml), [check](../keda/datadog_checks/keda/check.py), [readme](../keda/README.md), [upstream](https://keda.sh/docs/2.16/integrations/prometheus/) |
| Kong (`kong`) | — | openmetrics-port-scan — Modern Kong exposes Prometheus/OpenMetrics on the Admin API at `http://<host>:8001/metrics` (Prometheus plugin) — the recommended path documented in the README. | [spec](../kong/assets/configuration/spec.yaml), [check](../kong/datadog_checks/kong/check.py), [check](../kong/datadog_checks/kong/kong.py), [readme](../kong/README.md), [upstream](https://docs.konghq.com/hub/kong-inc/prometheus/) |
| KrakenD (`krakend`) | `openmetrics_endpoint` | openmetrics-port-scan — KrakendCheck inherits OpenMetricsBaseCheckV2 and only needs `openmetrics_endpoint`. | [spec](../krakend/assets/configuration/spec.yaml), [check](../krakend/datadog_checks/krakend/check.py), [readme](../krakend/README.md), [upstream](https://www.krakend.io/docs/telemetry/opentelemetry/) |
| Kube DNS (`kube_dns`) | `prometheus_endpoint` | openmetrics-port-scan — kube-dns exposes Prometheus metrics on the fixed path `/metrics` over its dnsmasq metrics port (10055 by default). The shipped auto_conf templates `http://%%host%%:10055/metrics`, which is the upstream default. | [spec](../kube_dns/assets/configuration/spec.yaml), [auto_conf](../kube_dns/datadog_checks/kube_dns/data/auto_conf.yaml), [check](../kube_dns/datadog_checks/kube_dns/kube_dns.py), [upstream](https://github.com/kubernetes/dns/blob/master/docs/kube-dns.md) |
| Kube Proxy (`kube_proxy`) | `prometheus_url` | openmetrics-port-scan — kube-proxy exposes Prometheus-format metrics on the fixed path `/metrics` at TCP port 10249 (the `metricsBindAddress` default), and a health endpoint at `/healthz` on port 10256. | [spec](../kube_proxy/assets/configuration/spec.yaml), [check](../kube_proxy/datadog_checks/kube_proxy/kube_proxy.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kube-proxy/) |
| Kubernetes Cluster Autoscaler (`kubernetes_cluster_autoscaler`) | `openmetrics_endpoint` | openmetrics-port-scan — The check inherits `OpenMetricsBaseCheckV2` and exposes a single required `openmetrics_endpoint`. Cluster Autoscaler serves Prometheus metrics on `/metrics` at the well-known port 8085 (per upstream and the spec's example). | [spec](../kubernetes_cluster_autoscaler/assets/configuration/spec.yaml), [check](../kubernetes_cluster_autoscaler/datadog_checks/kubernetes_cluster_autoscaler/check.py), [readme](../kubernetes_cluster_autoscaler/README.md), [upstream](https://github.com/kubernetes/autoscaler/blob/master/cluster-autoscaler/proposals/metrics.md) |
| Kubernetes State (`kubernetes_state`) | `kube_state_url` | openmetrics-port-scan — The legacy `kubernetes_state` check scrapes kube-state-metrics over Prometheus on port 8080 at `/metrics`. | [spec](../kubernetes_state/assets/configuration/spec.yaml), [auto_conf](../kubernetes_state/datadog_checks/kubernetes_state/data/auto_conf.yaml), [check](../kubernetes_state/datadog_checks/kubernetes_state/kubernetes_state.py), [upstream](https://github.com/kubernetes/kube-state-metrics) |
| Kuma (`kuma`) | `openmetrics_endpoint` | openmetrics-port-scan — Kuma's control plane exposes Prometheus-formatted metrics at `/metrics` on the well-known port 5680. The check inherits OpenMetricsBaseCheckV2 and the spec template (`instances/openmetrics`) only needs a single URL. | [spec](../kuma/assets/configuration/spec.yaml), [check](../kuma/datadog_checks/kuma/check.py), [readme](../kuma/README.md), [upstream](https://kuma.io/docs/latest/production/monitoring/prometheus/) |
| Kyverno (`kyverno`) | `openmetrics_endpoint` | openmetrics-port-scan — Kyverno controllers (Admissions, Backup, Cleanup, Reports) each expose Prometheus-formatted metrics at `/metrics` on the well-known port 8000. | [spec](../kyverno/assets/configuration/spec.yaml), [check](../kyverno/datadog_checks/kyverno/check.py), [readme](../kyverno/README.md), [upstream](https://kyverno.io/docs/monitoring/) |
| Linkerd (`linkerd`) | — | openmetrics-port-scan — Linkerd's data-plane proxy exposes Prometheus-format metrics on its admin port (default 9990) at the fixed path /admin/metrics/prometheus. | [spec](../linkerd/assets/configuration/spec.yaml), [check](../linkerd/datadog_checks/linkerd/check.py), [upstream](https://linkerd.io/2/reference/proxy-metrics/) |
| LiteLLM (`litellm`) | — | openmetrics-port-scan — LiteLLM Proxy exposes Prometheus metrics on a single canonical path `/metrics` (per upstream docs at https://docs.litellm.ai/docs/proxy/prometheus) at the proxy's HTTP port — `4000` by default in the spec example and the project's documented default. | [spec](../litellm/assets/configuration/spec.yaml), [check](../litellm/datadog_checks/litellm/check.py), [readme](../litellm/README.md), [upstream](https://docs.litellm.ai/docs/proxy/prometheus) |
| Milvus (`milvus`) | `openmetrics_endpoint` | openmetrics-port-scan — Milvus is a vector database that natively exposes Prometheus metrics on a fixed, well-known endpoint. | [spec](../milvus/assets/configuration/spec.yaml), [check](../milvus/datadog_checks/milvus/check.py), [readme](../milvus/README.md), [upstream](https://milvus.io/docs/monitor.md) |
| n8n (`n8n`) | `openmetrics_endpoint` | openmetrics-port-scan — n8n is configured via the `instances/openmetrics` template with a single required `openmetrics_endpoint`. | [spec](../n8n/assets/configuration/spec.yaml), [check](../n8n/datadog_checks/n8n/check.py), [readme](../n8n/README.md), [upstream](https://docs.n8n.io/hosting/logging-monitoring/monitoring/) |
| NGINX Ingress Controller (`nginx_ingress_controller`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single fixed Prometheus endpoint at http://%%host%%:10254/metrics. | [spec](../nginx_ingress_controller/assets/configuration/spec.yaml), [check](../nginx_ingress_controller/datadog_checks/nginx_ingress_controller/nginx_ingress_controller.py), [upstream](https://kubernetes.github.io/ingress-nginx/user-guide/monitoring/) |
| NVIDIA NIM (`nvidia_nim`) | `openmetrics_endpoint` | openmetrics-port-scan — NVIDIA NIM (Inference Microservice) exposes Prometheus metrics on a single canonical `/metrics` endpoint, defaulting to port 8000. The check is a thin OpenMetricsBaseCheckV2 subclass with one required field (`openmetrics_endpoint`). | [spec](../nvidia_nim/assets/configuration/spec.yaml), [check](../nvidia_nim/datadog_checks/nvidia_nim/check.py), [upstream](https://docs.nvidia.com/nim/large-language-models/latest/observability.html) |
| NVIDIA Triton (`nvidia_triton`) | `openmetrics_endpoint` | openmetrics-port-scan — NVIDIA Triton Inference Server exposes Prometheus metrics on a fixed `/metrics` path at the documented default port 8002 (configured via `--metrics-port`). | [spec](../nvidia_triton/assets/configuration/spec.yaml), [check](../nvidia_triton/datadog_checks/nvidia_triton/check.py), [readme](../nvidia_triton/README.md), [upstream](https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/user_guide/metrics.html) |
| Pulsar (`pulsar`) | `openmetrics_endpoint` | openmetrics-port-scan — Pulsar's check inherits `OpenMetricsBaseCheckV2` and the spec uses `template: instances/openmetrics` with a single canonical endpoint at `http://%%host%%:8080/metrics` (the broker's web service port). | [spec](../pulsar/assets/configuration/spec.yaml), [check](../pulsar/datadog_checks/pulsar/check.py), [readme](../pulsar/README.md), [upstream](https://pulsar.apache.org/docs/admin-api-overview/) |
| Quarkus (`quarkus`) | `openmetrics_endpoint` | openmetrics-port-scan — Quarkus exposes Prometheus metrics through its Micrometer Prometheus MeterRegistry extension at the well-known path `/q/metrics`, on the same HTTP port the application serves on (default 8080). | [spec](../quarkus/assets/configuration/spec.yaml), [check](../quarkus/datadog_checks/quarkus/check.py), [readme](../quarkus/README.md), [upstream](https://quarkus.io/guides/telemetry-micrometer) |
| Ray (`ray`) | `openmetrics_endpoint` | openmetrics-port-scan — Ray exposes Prometheus metrics from its dashboard/metrics agent on a fixed default port (8080), as documented in the README sample (`http://<RAY_ADDRESS>:8080`) and the Docker autodiscovery snippet (`http://%%host%%:8080`). | [spec](../ray/assets/configuration/spec.yaml), [check](../ray/datadog_checks/ray/check.py), [readme](../ray/README.md), [upstream](https://docs.ray.io/en/latest/cluster/metrics.html) |
| Scylla (`scylla`) | `openmetrics_endpoint` | openmetrics-port-scan — Scylla exposes Prometheus metrics on the Scylla Prometheus exporter at the well-known port 9180 with path `/metrics`. | [spec](../scylla/assets/configuration/spec.yaml), [check](../scylla/datadog_checks/scylla/scylla.py), [check](../scylla/datadog_checks/scylla/scylla_v2.py), [readme](../scylla/README.md), [upstream](https://opensource.docs.scylladb.com/stable/operating-scylla/monitoring/monitoring-stack.html) |
| Strimzi (`strimzi`) | — | openmetrics-port-scan — Strimzi is a Kubernetes Kafka operator. | [spec](../strimzi/assets/configuration/spec.yaml), [check](../strimzi/datadog_checks/strimzi/check.py), [upstream](https://strimzi.io/docs/operators/latest/deploying.html#assembly-metrics-str) |
| Teleport (`teleport`) | `teleport_url` | openmetrics-port-scan — Teleport's diagnostic HTTP endpoint serves Prometheus metrics on a known default port (3000) at /metrics, with a fixed health probe at /healthz. | [spec](../teleport/assets/configuration/spec.yaml), [check](../teleport/datadog_checks/teleport/check.py) |
| Temporal (`temporal`) | `openmetrics_endpoint` | openmetrics-port-scan — Temporal uses the standard OpenMetrics V2 instances template with a single required openmetrics_endpoint. | [spec](../temporal/assets/configuration/spec.yaml), [check](../temporal/datadog_checks/temporal/check.py) |
| Velero (`velero`) | `openmetrics_endpoint` | openmetrics-port-scan — Velero's check is a thin `OpenMetricsBaseCheckV2` that scrapes a single Prometheus endpoint. The spec uses the standard `instances/openmetrics` template with `openmetrics_endpoint` as the only required field. | [spec](../velero/assets/configuration/spec.yaml), [check](../velero/datadog_checks/velero/check.py), [readme](../velero/README.md), [other](../velero/datadog_checks/velero/config_models/validators.py), [upstream](https://velero.io/docs/main/troubleshooting/#velero-is-not-publishing-prometheus-metrics) |
| vLLM (`vllm`) | `openmetrics_endpoint` | openmetrics-port-scan — vLLM's OpenAI-compatible API server exposes Prometheus metrics on the same port as the API (default 8000) at `/metrics`. | [spec](../vllm/assets/configuration/spec.yaml), [check](../vllm/datadog_checks/vllm/check.py), [readme](../vllm/README.md), [upstream](https://docs.vllm.ai/en/stable/serving/metrics.html) |
| Weaviate (`weaviate`) | `openmetrics_endpoint` | openmetrics-port-scan — Weaviate exposes Prometheus-formatted metrics on a fixed `/metrics` path; the upstream-documented default port is 2112 (configurable via the `PROMETHEUS_MONITORING_PORT` env var). | [spec](../weaviate/assets/configuration/spec.yaml), [check](../weaviate/datadog_checks/weaviate/check.py), [readme](../weaviate/README.md), [upstream](https://weaviate.io/developers/weaviate/configuration/monitoring) |

### `generic-incluster-bearer-token` (10)

Same as openmetrics-scan but the Agent's pod ServiceAccount token is auto-injected for HTTPS+auth endpoints. Per-integration data is just port + path.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| EKS Fargate (`eks_fargate`) | — | other — EKS Fargate generates heartbeat and capacity metrics for pods running on Fargate virtual nodes. | [spec](../eks_fargate/assets/configuration/spec.yaml), [auto_conf](../eks_fargate/datadog_checks/eks_fargate/data/conf.yaml.default), [check](../eks_fargate/datadog_checks/eks_fargate/eks_fargate.py), [upstream](https://docs.aws.amazon.com/eks/latest/userguide/fargate.html) |
| Kubernetes API server metrics (`kube_apiserver_metrics`) | `prometheus_url` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheck and scrapes a single `/metrics` path on the kube-apiserver. | [spec](../kube_apiserver_metrics/assets/configuration/spec.yaml), [auto_conf](../kube_apiserver_metrics/datadog_checks/kube_apiserver_metrics/data/auto_conf.yaml), [check](../kube_apiserver_metrics/datadog_checks/kube_apiserver_metrics/kube_apiserver_metrics.py), [upstream](https://kubernetes.io/docs/concepts/cluster-administration/system-metrics/) |
| Kubernetes Controller Manager (`kube_controller_manager`) | `prometheus_url` | openmetrics-port-scan — kube-controller-manager exposes Prometheus metrics on a fixed `/metrics` path. The auto_conf already enumerates the canonical port pair (10257 secure / 10252 insecure) and prefers the first that responds. | [spec](../kube_controller_manager/assets/configuration/spec.yaml), [auto_conf](../kube_controller_manager/datadog_checks/kube_controller_manager/data/auto_conf.yaml), [check](../kube_controller_manager/datadog_checks/kube_controller_manager/kube_controller_manager.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kube-controller-manager/) |
| Kube metrics server (`kube_metrics_server`) | `prometheus_url` | openmetrics-port-scan — kube-metrics-server is a Kubernetes API extension server that exposes Prometheus metrics on a fixed path (/metrics) over HTTPS on port 443. | [spec](../kube_metrics_server/assets/configuration/spec.yaml), [check](../kube_metrics_server/datadog_checks/kube_metrics_server/kube_metrics_server.py) |
| Kube Scheduler (`kube_scheduler`) | `prometheus_url` | http-path-probe — kube-scheduler exposes Prometheus metrics on /metrics, but the port and scheme have changed between Kubernetes versions: legacy clusters expose unauthenticated http on :10251, while modern (1.13+) clusters expose authenticated https on :10259 and require a service-account bearer token. | [spec](../kube_scheduler/assets/configuration/spec.yaml), [check](../kube_scheduler/datadog_checks/kube_scheduler/kube_scheduler.py), [auto_conf](../kube_scheduler/datadog_checks/kube_scheduler/data/auto_conf.yaml) |
| Kubelet (`kubelet`) | — | other — The kubelet integration runs against the Agent's own host node. | [spec](../kubelet/assets/configuration/spec.yaml), [auto_conf](../kubelet/datadog_checks/kubelet/data/conf.yaml.default), [check](../kubelet/datadog_checks/kubelet/kubelet.py), [upstream](https://kubernetes.io/docs/reference/command-line-tools-reference/kubelet/) |
| Kubernetes State Core (`kubernetes_state_core`) | — | other — The Kubernetes State Core check is a Cluster-Agent built-in (Go-implemented, the `datadog_checks/kubernetes_state_core` directory ships only `conf.yaml.example` — no Python check). | [spec](../kubernetes_state_core/assets/configuration/spec.yaml), [readme](../kubernetes_state_core/README.md), [upstream](https://docs.datadoghq.com/integrations/kubernetes_state_core/) |
| KubeVirt API (`kubevirt_api`) | `kubevirt_api_metrics_endpoint` | openmetrics-port-scan — The KubeVirt API `virt-api` pod exposes Prometheus metrics over HTTPS on port 443 at `/metrics` (per README and tests). | [spec](../kubevirt_api/assets/configuration/spec.yaml), [check](../kubevirt_api/datadog_checks/kubevirt_api/check.py), [check](../kubevirt_api/datadog_checks/kubevirt_api/kube_client.py), [readme](../kubevirt_api/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |
| KubeVirt Controller (`kubevirt_controller`) | `kubevirt_controller_metrics_endpoint` | openmetrics-port-scan — The check inherits OpenMetricsBaseCheckV2 and points at the KubeVirt Controller's `/metrics` Prometheus endpoint, which it exposes on the pod at a fixed path (port 8443/443). | [spec](../kubevirt_controller/assets/configuration/spec.yaml), [check](../kubevirt_controller/datadog_checks/kubevirt_controller/check.py), [readme](../kubevirt_controller/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |
| KubeVirt Handler (`kubevirt_handler`) | `kubevirt_handler_metrics_endpoint` | openmetrics-port-scan — Mirrors kubevirt_controller: the check inherits OpenMetricsBaseCheckV2 and scrapes the KubeVirt Handler (`virt-handler`) DaemonSet pod's `/metrics` endpoint on a fixed path. | [spec](../kubevirt_handler/assets/configuration/spec.yaml), [check](../kubevirt_handler/datadog_checks/kubevirt_handler/check.py), [readme](../kubevirt_handler/README.md), [upstream](https://kubevirt.io/user-guide/operations/component_monitoring/) |

### `generic-windows-perf` (6)

Detect a PDH counter set on the local Windows host (e.g. `IIS`, `MSExchange*`). Per-integration data is the counter set name + counter list.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Active Directory (`active_directory`) | — | other — Active Directory inherits the Windows perf_counters template (same shape as iis). | [spec](../active_directory/assets/configuration/spec.yaml), [check](../active_directory/datadog_checks/active_directory/check.py) |
| ASP.NET (`aspdotnet`) | — | other — ASP.NET is collected via Windows Performance Counters (PDH) on the local host where the Agent runs. The spec uses `template: instances/perf_counters` and the check inherits `PerfCountersBaseCheckWithLegacySupport`. | [spec](../aspdotnet/assets/configuration/spec.yaml), [check](../aspdotnet/datadog_checks/aspdotnet/check.py), [check](../aspdotnet/datadog_checks/aspdotnet/aspdotnet.py), [readme](../aspdotnet/README.md) |
| .NET CLR (`dotnetclr`) | — | other — .NET CLR metrics are collected via Windows Performance Counters (PDH) on the local host where the Agent runs. The spec uses `template: instances/perf_counters` and the check inherits `PerfCountersBaseCheckWithLegacySupport`. | [spec](../dotnetclr/assets/configuration/spec.yaml), [check](../dotnetclr/datadog_checks/dotnetclr/check.py), [check](../dotnetclr/datadog_checks/dotnetclr/metrics.py) |
| Exchange Server (`exchange_server`) | — | other — Exchange Server inherits the Windows perf_counters template (PerfCountersBaseCheckWithLegacySupport). | [spec](../exchange_server/assets/configuration/spec.yaml), [check](../exchange_server/datadog_checks/exchange_server/check.py) |
| Hyper-V (`hyperv`) | — | other — Hyper-V is collected via Windows Performance Counters (PDH) on the local host where the Agent runs — `template: instances/perf_counters` plus a per-integration metric set defined in code. | [spec](../hyperv/assets/configuration/spec.yaml), [check](../hyperv/datadog_checks/hyperv/check.py), [check](../hyperv/datadog_checks/hyperv/hyperv.py), [readme](../hyperv/README.md), [upstream](https://docs.microsoft.com/en-us/windows-server/virtualization/hyper-v/hyper-v-on-windows-server) |
| IIS (`iis`) | — | other — IIS is collected via Windows Performance Counters (PDH) on the local host where the Agent runs — there's no remote host/port in the default mode. | [spec](../iis/assets/configuration/spec.yaml), [upstream](https://learn.microsoft.com/en-us/iis/web-hosting/web-server-for-shared-hosting/installing-internet-information-services-iis-7) |

### `generic-linux-procfs` (7)

Read host-local files under `/proc` or `/sys`. Per-integration data is just the file paths.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Btrfs (`btrfs`) | — | other — The Btrfs check has no required configuration fields and no network endpoint. | [spec](../btrfs/assets/configuration/spec.yaml), [check](../btrfs/datadog_checks/btrfs/btrfs.py), [readme](../btrfs/README.md) |
| Disk (`disk`) | — | other — The Disk check is a host-local system check with no required configuration fields and no remote target. | [spec](../disk/assets/configuration/spec.yaml), [check](../disk/datadog_checks/disk/disk.py), [readme](../disk/README.md) |
| Infiniband (`infiniband`) | — | other — The Infiniband check has no required configuration fields and no network endpoint. | [spec](../infiniband/assets/configuration/spec.yaml), [check](../infiniband/datadog_checks/infiniband/check.py), [readme](../infiniband/README.md) |
| Linux Proc Extras (`linux_proc_extras`) | — | other — The check reads files under the Agent host's `procfs_path` (defaults to `/proc`): `sys/fs/inode-nr`, `stat`, `sys/kernel/random/entropy_avail`, `<pid>/status`, and optionally `interrupts`. | [spec](../linux_proc_extras/assets/configuration/spec.yaml), [check](../linux_proc_extras/datadog_checks/linux_proc_extras/linux_proc_extras.py), [readme](../linux_proc_extras/README.md) |
| Network (`network`) | — | other — The Network check has no required configuration fields and no remote endpoint. | [spec](../network/assets/configuration/spec.yaml), [check](../network/datadog_checks/network/network.py), [check](../network/datadog_checks/network/check_linux.py), [readme](../network/README.md) |
| System Core (`system_core`) | — | other — The System Core check is a host-local CPU metric check with no required configuration fields and no remote target. | [spec](../system_core/assets/configuration/spec.yaml), [check](../system_core/datadog_checks/system_core/system_core.py), [readme](../system_core/README.md) |
| System Swap (`system_swap`) | — | other — The System Swap check is a host-local swap-usage metric check with no required configuration fields and no remote target. | [spec](../system_swap/assets/configuration/spec.yaml), [check](../system_swap/datadog_checks/system_swap/system_swap.py), [readme](../system_swap/README.md) |

## HTTP probe with integration-specific verification

_Fixed URL on a known port, but the discovery layer needs integration-specific verification code (more than just "is this Prometheus exposition format?") to confirm the target._

### `http-text-format` (4)

Fixed URL, integration-specific text/HTML format (e.g. apache mod_status, squid Cache Manager `key = value`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Apache (`apache`) | `apache_status_url` | http-path-probe — Apache's mod_status exposes a single canonical machine-readable URL: `/server-status?auto`. The endpoint and its `?auto` query parameter are universal across deployments — a single HTTP probe is enough. | [spec](../apache/assets/configuration/spec.yaml), [auto_conf](../apache/datadog_checks/apache/data/auto_conf.yaml), [upstream](https://httpd.apache.org/docs/2.4/mod/mod_status.html) |
| Kyoto Tycoon (`kyototycoon`) | `report_url` | http-path-probe — Kyoto Tycoon serves an HTTP RPC interface; the check issues a single GET against the canonical `/rpc/report` endpoint on the well-known port 1978 and parses its tab-separated response. No authentication is required. | [spec](../kyototycoon/assets/configuration/spec.yaml), [auto_conf](../kyototycoon/datadog_checks/kyototycoon/data/auto_conf.yaml), [check](../kyototycoon/datadog_checks/kyototycoon/kyototycoon.py), [readme](../kyototycoon/README.md) |
| Lighttpd (`lighttpd`) | `lighttpd_status_url` | http-path-probe — Lighttpd's `mod_status` exposes a single canonical status page at `/server-status` with the `?auto` (or `?format=plain` on Lighttpd 2) query string for machine-readable output. | [spec](../lighttpd/assets/configuration/spec.yaml), [check](../lighttpd/datadog_checks/lighttpd/lighttpd.py), [upstream](https://redmine.lighttpd.net/projects/1/wiki/Docs_ModStatus) |
| Squid (`squid`) | `name` | http-path-probe — Squid exposes its Cache Manager counters over HTTP at the single canonical path `/squid-internal-mgr/counters` on the proxy port (default 3128). The check just GETs that URL and parses the `key = value` body. | [spec](../squid/assets/configuration/spec.yaml), [check](../squid/datadog_checks/squid/squid.py), [readme](../squid/README.md), [upstream](https://wiki.squid-cache.org/Features/CacheManager) |

### `http-json-shape` (10)

Fixed URL, JSON shape verification with integration-specific keys (e.g. `version`+`cluster` for mesos master, `id`+`frameworks` for mesos slave).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Consul (`consul`) | `url` | http-path-probe — Consul's HTTP API listens on a single well-known port (8500 by default for the agent, 8501 for HTTPS) and the check just needs a base URL — it discovers everything else (`/v1/status/leader`, `/v1/agent/self`, `/v1/agent/metrics?format=prometheus`) from there. | [spec](../consul/assets/configuration/spec.yaml), [auto_conf](../consul/datadog_checks/consul/data/auto_conf.yaml), [check](../consul/datadog_checks/consul/consul.py), [upstream](https://developer.hashicorp.com/consul/api-docs/agent) |
| Fluentd (`fluentd`) | `monitor_agent_url` | http-path-probe — Fluentd's monitor_agent plugin exposes a single canonical JSON endpoint at `/api/plugins.json` on the conventional port 24220. | [spec](../fluentd/assets/configuration/spec.yaml), [check](../fluentd/datadog_checks/fluentd/fluentd.py), [upstream](https://docs.fluentd.org/input/monitor_agent) |
| HDFS Datanode (`hdfs_datanode`) | `hdfs_datanode_jmx_uri` | http-path-probe — The HDFS DataNode check fetches metrics from the JMX HTTP servlet at `/jmx` (no JMX-RMI). | [spec](../hdfs_datanode/assets/configuration/spec.yaml), [check](../hdfs_datanode/datadog_checks/hdfs_datanode/hdfs_datanode.py), [upstream](https://hadoop.apache.org/docs/r3.1.3/hadoop-project-dist/hadoop-hdfs/hdfs-default.xml) |
| HDFS Namenode (`hdfs_namenode`) | `hdfs_namenode_jmx_uri` | http-path-probe — Despite the name, the HDFS NameNode check does not use JMX/RMI: it issues an HTTP GET against the Hadoop JMX servlet at `/jmx` on the NameNode HTTP port (default 9870 in Hadoop 3, 50070 in Hadoop 2). | [spec](../hdfs_namenode/assets/configuration/spec.yaml), [check](../hdfs_namenode/datadog_checks/hdfs_namenode/hdfs_namenode.py), [readme](../hdfs_namenode/README.md), [upstream](https://hadoop.apache.org/docs/r3.1.3/hadoop-project-dist/hadoop-hdfs/hdfs-default.xml) |
| MapReduce (`mapreduce`) | `resourcemanager_uri`, `cluster_name` | http-path-probe — MapReduce is the YARN ResourceManager check's MapReduce-flavoured sibling: the check hits the same REST API root (`ws/v1/cluster`, `ws/v1/cluster/apps`, `ws/v1/mapreduce/jobs`) on the ResourceManager (default port 8088) and then follows the application master URLs. | [spec](../mapreduce/assets/configuration/spec.yaml), [check](../mapreduce/datadog_checks/mapreduce/mapreduce.py), [upstream](https://hadoop.apache.org/docs/current/hadoop-yarn/hadoop-yarn-site/ResourceManagerRest.html) |
| Mesos Master (`mesos_master`) | `url` | http-path-probe — Mesos Master exposes an unauthenticated HTTP REST API on the well-known port 5050. The check GETs /state (cluster topology, version) and /metrics/snapshot (counter snapshot) and parses JSON. | [spec](../mesos_master/assets/configuration/spec.yaml), [check](../mesos_master/datadog_checks/mesos_master/mesos_master.py), [readme](../mesos_master/README.md), [upstream](http://mesos.apache.org/documentation/latest/endpoints/master/state/) |
| Mesos Slave (`mesos_slave`) | `url` | http-path-probe — Mesos Slave (agent) exposes the same unauthenticated HTTP REST API shape as Mesos Master, on the well-known port 5051. | [spec](../mesos_slave/assets/configuration/spec.yaml), [check](../mesos_slave/datadog_checks/mesos_slave/mesos_slave.py), [readme](../mesos_slave/README.md), [upstream](http://mesos.apache.org/documentation/latest/endpoints/) |
| Riak (`riak`) | `url` | http-path-probe — Riak's HTTP REST API exposes a singular, well-known stats endpoint at `/stats` on the default HTTP port 8098. The shipped auto_conf.yaml templates `url: http://%%host%%:8098/stats` and the check simply GETs that URL and parses the JSON. | [spec](../riak/assets/configuration/spec.yaml), [auto_conf](../riak/datadog_checks/riak/data/auto_conf.yaml), [check](../riak/datadog_checks/riak/riak.py), [readme](../riak/README.md), [upstream](https://docs.riak.com/riak/kv/latest/developing/api/http/status.1.html) |
| Traffic Server (`traffic_server`) | `traffic_server_url` | http-path-probe — Apache Traffic Server exposes metrics through the `stats_over_http` plugin at a single canonical path `/_stats`, returning a JSON document with a `global` key. | [spec](../traffic_server/assets/configuration/spec.yaml), [check](../traffic_server/datadog_checks/traffic_server/check.py), [readme](../traffic_server/README.md), [upstream](https://docs.trafficserver.apache.org/en/latest/admin-guide/plugins/stats_over_http.en.html) |
| YARN (`yarn`) | `resourcemanager_uri` | http-path-probe — YARN's check hits the ResourceManager REST API at well-known paths under `/ws/v1/cluster` (metrics, apps, nodes, scheduler). | [spec](../yarn/assets/configuration/spec.yaml), [check](../yarn/datadog_checks/yarn/yarn.py), [upstream](https://hadoop.apache.org/docs/current/hadoop-yarn/hadoop-yarn-site/ResourceManagerRest.html) |

### `http-multi-path` (21)

Try several plausible paths or modes per integration (e.g. nginx stub_status / Plus API / VTS; rabbitmq Prometheus + management plugin).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Airflow (`airflow`) | `url` | http-path-probe — The `airflow` Agent check itself is an HTTP probe against the Airflow webserver (default port 8080). | [spec](../airflow/assets/configuration/spec.yaml), [check](../airflow/datadog_checks/airflow/airflow.py), [readme](../airflow/README.md), [upstream](https://airflow.apache.org/docs/apache-airflow/stable/stable-rest-api-ref.html) |
| CouchDB (`couch`) | `server` | http-path-probe — CouchDB exposes a REST API on the well-known port 5984. The existing auto_conf.yaml templates the server URL as http://%%host%%:5984. | [spec](../couch/assets/configuration/spec.yaml), [auto_conf](../couch/datadog_checks/couch/data/auto_conf.yaml), [check](../couch/datadog_checks/couch/couch.py) |
| Druid (`druid`) | `url` | http-path-probe — The Druid check takes a single required `url` (the base URL of a Druid process) and issues GETs against `/status/properties` and `/status/health`. | [spec](../druid/assets/configuration/spec.yaml), [check](../druid/datadog_checks/druid/druid.py), [readme](../druid/README.md), [upstream](https://druid.apache.org/docs/latest/operations/api-reference) |
| Elasticsearch (`elastic`) | `url` | http-path-probe — The check only requires a URL (default `http://localhost:9200`) — Elasticsearch's REST API is on a well-known port. | [spec](../elastic/assets/configuration/spec.yaml), [auto_conf](../elastic/datadog_checks/elastic/data/auto_conf.yaml), [upstream](https://www.elastic.co/guide/en/elasticsearch/reference/current/cluster.html) |
| Envoy (`envoy`) | — | http-path-probe — Envoy exposes its admin/stats interface on a listener whose port is operator-configured (commonly 9901, but the shipped example uses 80, and in service-mesh deployments it's typically 15000 for Istio sidecars or 8001/8081 in upstream samples). | [spec](../envoy/assets/configuration/spec.yaml), [check](../envoy/datadog_checks/envoy/check.py), [readme](../envoy/README.md), [upstream](https://www.envoyproxy.io/docs/envoy/latest/operations/admin) |
| GitLab (`gitlab`) | — | http-path-probe — GitLab exposes Prometheus metrics on the main app at `/-/metrics`, but the endpoint is IP-allowlisted by default (`monitoring.ip_whitelist` in gitlab.rb) — the Agent host must be allowed before the URL works. | [spec](../gitlab/assets/configuration/spec.yaml), [check](../gitlab/datadog_checks/gitlab/gitlab.py), [check](../gitlab/datadog_checks/gitlab/gitlab_v2.py), [upstream](https://docs.gitlab.com/ee/administration/monitoring/prometheus/gitlab_metrics.html) |
| GitLab Runner (`gitlab_runner`) | `gitlab_url`, `prometheus_endpoint`, `allowed_metrics` | openmetrics-port-scan — GitLab Runner's prometheus listener (default `:9252/metrics`) is unauthenticated and could be discovered with a generic OpenMetrics port-scan. | [spec](../gitlab_runner/assets/configuration/spec.yaml), [check](../gitlab_runner/datadog_checks/gitlab_runner/gitlab_runner.py), [upstream](https://docs.gitlab.com/runner/monitoring/) |
| HAProxy (`haproxy`) | — | http-path-probe — HAProxy supports two collection modes: a Prometheus exporter (HAProxy 2+ exposes `/metrics` on a configurable port, often 8404) which is unauthenticated and OpenMetrics-compatible, and the legacy stats page (`/admin?stats` or similar) which typically needs HTTP basic auth. | [spec](../haproxy/assets/configuration/spec.yaml), [upstream](https://www.haproxy.com/documentation/haproxy-configuration-tutorials/metrics-and-observability/prometheus/) |
| Impala (`impala`) | `service_type`, `openmetrics_endpoint` | http-path-probe — Impala's check is OpenMetricsBaseCheckV2 hitting `/metrics_prometheus` on each daemon's debug HTTP server. No credentials are needed for the Prometheus endpoint. | [spec](../impala/assets/configuration/spec.yaml), [check](../impala/datadog_checks/impala/check.py), [readme](../impala/README.md) |
| Istio (`istio`) | — | http-path-probe — Istio exposes Prometheus metrics from multiple distinct components: the proxy (sidecar) at :15020/stats/prometheus, the istiod control plane at :15014/metrics, plus ambient-mode ztunnel and waypoint (also :15020). | [spec](../istio/assets/configuration/spec.yaml), [check](../istio/datadog_checks/istio/check.py), [auto_conf](../istio/datadog_checks/istio/data/auto_conf.yaml), [readme](../istio/README.md) |
| Kubeflow (`kubeflow`) | `openmetrics_endpoint` | http-path-probe — KubeflowCheck inherits OpenMetricsBaseCheckV2 with a single `openmetrics_endpoint` field, but Kubeflow is a multi-component umbrella (pipelines / `ml-pipeline`, kserve, katib, plus profile/notebook/training controllers). | [spec](../kubeflow/assets/configuration/spec.yaml), [check](../kubeflow/datadog_checks/kubeflow/check.py), [readme](../kubeflow/README.md) |
| Marathon (`marathon`) | `url` | http-path-probe — The check GETs Marathon REST API paths (`/v2/apps`, `/v2/deployments`, `/v2/queue`) under a single user-supplied `url`. | [spec](../marathon/assets/configuration/spec.yaml), [check](../marathon/datadog_checks/marathon/marathon.py), [readme](../marathon/README.md) |
| NGINX (`nginx`) | `nginx_status_url` | http-path-probe — NGINX has at least three plausible status paths depending on the build: stub_status (open-source nginx), the NGINX Plus `/api` (R13+), and the vhost_traffic_status (`vts`) module's JSON endpoint. | [spec](../nginx/assets/configuration/spec.yaml), [upstream](https://nginx.org/en/docs/http/ngx_http_stub_status_module.html), [upstream](https://docs.nginx.com/nginx/admin-guide/monitoring/live-activity-monitoring/) |
| PHP-FPM (`php_fpm`) | `status_url` | http-path-probe — PHP-FPM exposes status/ping over an HTTP path that the user must configure on a FastCGI passthrough (typically nginx or Apache). | [spec](../php_fpm/assets/configuration/spec.yaml), [check](../php_fpm/datadog_checks/php_fpm/php_fpm.py), [readme](../php_fpm/README.md) |
| Prefect (`prefect`) | `prefect_url` | http-path-probe — Prefect's check polls the Prefect Server REST API rooted at `prefect_url` (e.g. http://<host>:4200/api), hitting endpoints like /version, /health, /ready, /work_pools/filter, /events/filter. | [spec](../prefect/assets/configuration/spec.yaml), [check](../prefect/datadog_checks/prefect/check.py), [readme](../prefect/README.md), [upstream](https://docs.prefect.io/latest/api-ref/server/) |
| RabbitMQ (`rabbitmq`) | — | http-path-probe — RabbitMQ supports two collection modes: the Prometheus plugin (port 15692, `/metrics`, no auth — easy generic case) and the Management plugin (port 15672, `/api/`, basic auth — typically `guest:guest` only on localhost; remote access needs a dedicated monitoring user). | [spec](../rabbitmq/assets/configuration/spec.yaml), [auto_conf](../rabbitmq/datadog_checks/rabbitmq/data/auto_conf.yaml), [upstream](https://www.rabbitmq.com/docs/prometheus) |
| Spark (`spark`) | `spark_url`, `cluster_name` | http-path-probe — The Spark check supports four mutually-exclusive deployment topologies — Spark Standalone master (port 8080), YARN ResourceManager (port 8088), Mesos master (port 5050), and Spark driver (port 4040) — selected by `spark_cluster_mode`. | [spec](../spark/assets/configuration/spec.yaml), [check](../spark/datadog_checks/spark/spark.py), [upstream](https://spark.apache.org/docs/latest/monitoring.html) |
| Supervisord (`supervisord`) | `name` | http-path-probe — Supervisord is monitored via its XML-RPC interface, reachable either through an inet HTTP server (default `http://localhost:9001/RPC2`) or through a UNIX domain socket whose path is deployment-specific. | [spec](../supervisord/assets/configuration/spec.yaml), [check](../supervisord/datadog_checks/supervisord/supervisord.py), [upstream](http://supervisord.org/configuration.html#inet-http-server-section-settings) |
| Tekton (`tekton`) | — | http-path-probe — Tekton uses OpenMetricsBaseCheckV2 but exposes two distinct Prometheus endpoints (Pipelines Controller on port 9090 and Triggers Controller on port 9000), each with its own metric namespace. | [spec](../tekton/assets/configuration/spec.yaml), [check](../tekton/datadog_checks/tekton/check.py) |
| TorchServe (`torchserve`) | — | http-path-probe — TorchServe exposes three distinct HTTP endpoints on three different conventional ports: the OpenMetrics scrape endpoint (`/metrics` on 8082), the Inference API (8080) and the Management API (8081). | [spec](../torchserve/assets/configuration/spec.yaml), [check](../torchserve/datadog_checks/torchserve/check.py), [readme](../torchserve/README.md), [upstream](https://pytorch.org/serve/metrics_api.html) |
| Traefik Mesh (`traefik_mesh`) | `openmetrics_endpoint` | http-path-probe — Traefik Mesh's primary collection path is OpenMetrics on the proxy's metrics entrypoint (default port 8082, path `/metrics`), which alone would be a generic openmetrics-port-scan case. | [spec](../traefik_mesh/assets/configuration/spec.yaml), [check](../traefik_mesh/datadog_checks/traefik_mesh/check.py), [readme](../traefik_mesh/README.md), [upstream](https://doc.traefik.io/traefik-mesh/observability/) |

## TCP probe with integration-specific protocol

_Open a TCP socket, exchange integration-specific bytes to confirm the target._

### `tcp-banner-server-greets` (1)

Server speaks first with an integration-specific reply (e.g. twemproxy emits its stats JSON on connect).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Twemproxy (nutcracker) (`twemproxy`) | `host`, `port` | tcp-banner-probe — Twemproxy (nutcracker) exposes a stats monitoring port that emits a single JSON document immediately upon TCP connect (no request needed). The check just opens a socket to host:port, drains the response, and parses it. | [spec](../twemproxy/assets/configuration/spec.yaml), [check](../twemproxy/datadog_checks/twemproxy/twemproxy.py), [readme](../twemproxy/README.md), [upstream](https://github.com/twitter/twemproxy#observability) |

### `tcp-protocol-handshake` (5)

Client sends fixed bytes, integration-specific reply (memcached `version`, redis `PING`/`+PONG`, zookeeper `ruok`/`imok`, gearmand admin protocol, statsd `health`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Gearman (`gearmand`) | — | tcp-banner-probe — Gearman speaks a plain-text admin protocol over TCP on the well-known port 4730. | [spec](../gearmand/assets/configuration/spec.yaml), [check](../gearmand/datadog_checks/gearmand/gearmand.py), [readme](../gearmand/README.md) |
| Memcached (`mcache`) | `url` | tcp-banner-probe — Memcached speaks a well-known text/binary protocol on port 11211; the check connects via bmemcached and issues stats commands. Required configuration is just url (host) and an optional port that defaults to 11211. | [spec](../mcache/assets/configuration/spec.yaml), [check](../mcache/datadog_checks/mcache/mcache.py), [auto_conf](../mcache/datadog_checks/mcache/data/auto_conf.yaml), [readme](../mcache/README.md) |
| Redis (`redisdb`) | `host`, `port` | tcp-banner-probe — Redis answers a banner over TCP and the default port 6379 is universal. A generic TCP/banner probe (PING / CLIENT INFO) is sufficient for the unauthenticated case. | [spec](../redisdb/assets/configuration/spec.yaml), [auto_conf](../redisdb/datadog_checks/redisdb/data/auto_conf.yaml) |
| StatsD (`statsd`) | — | tcp-banner-probe — Despite the name, this is NOT a DogStatsD-only integration. | [spec](../statsd/assets/configuration/spec.yaml), [check](../statsd/datadog_checks/statsd/statsd.py), [readme](../statsd/README.md) |
| ZooKeeper (`zk`) | `host` | tcp-banner-probe — The ZooKeeper check opens a TCP socket to host:port (default 2181) and sends the `ruok`, `stat`, and `mntr` four-letter words; `ruok` returns the literal `imok` and `stat` returns a multi-line response starting with `Zookeeper version: ...`. | [spec](../zk/assets/configuration/spec.yaml), [check](../zk/datadog_checks/zk/zk.py), [upstream](https://zookeeper.apache.org/doc/r3.8.0/zookeeperAdmin.html#sc_4lw) |

## Local detection (no network, no credentials)

_The integration runs against host-local state; discovery is "is this thing present on the Agent host?"._

### `local-cli-binary` (10)

Shell out to a local CLI binary (`varnishstat`, `ceph`, `gstatus`, `nodetool`, `lctl`, `slurm`, `lparstat`, `tibemsadmin`, `nfsiostat`, `postqueue`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Cassandra Nodetool (`cassandra_nodetool`) | `keyspaces` | other — The check shells out to a local `nodetool` binary (default /usr/bin/nodetool) and parses the output of `nodetool status -- <keyspace>`. There is no Agent-driven network probe; nodetool itself opens the JMX connection on port 7199. | [spec](../cassandra_nodetool/assets/configuration/spec.yaml), [check](../cassandra_nodetool/datadog_checks/cassandra_nodetool/cassandra_nodetool.py), [readme](../cassandra_nodetool/README.md) |
| Ceph (`ceph`) | — | other — The Ceph check shells out to the local `ceph` CLI (`/usr/bin/ceph --cluster ceph status -fjson`, etc.) via `get_subprocess_output`. | [spec](../ceph/assets/configuration/spec.yaml), [check](../ceph/datadog_checks/ceph/ceph.py), [upstream](https://docs.ceph.com/en/latest/mgr/prometheus/) |
| GlusterFS (`glusterfs`) | — | other — The GlusterFS check shells out to the local `gstatus` binary (shipped with the Agent at `<run_path>/embedded/sbin/gstatus`) via `subprocess.run`, parses the JSON output, and emits cluster/volume/brick metrics. | [spec](../glusterfs/assets/configuration/spec.yaml), [check](../glusterfs/datadog_checks/glusterfs/check.py), [readme](../glusterfs/README.md) |
| LPARStats (`lparstats`) | — | other — The LPARStats check shells out to the AIX `lparstat` CLI (`/usr/bin/lparstat`, optionally via `sudo`) and parses its tabular output for memory, hypervisor, and SPURR processor utilization metrics. | [spec](../lparstats/assets/configuration/spec.yaml), [check](../lparstats/datadog_checks/lparstats/lparstats.py), [readme](../lparstats/README.md) |
| Lustre (`lustre`) | — | other — The Lustre check shells out to local Lustre administrative binaries — `lctl` (default `/usr/sbin/lctl`), `lnetctl` (`/usr/sbin/lnetctl`), and `lfs` (`/usr/bin/lfs`) — to enumerate devices, parse params, collect LNET stats, and (on clients) read changelogs. | [spec](../lustre/assets/configuration/spec.yaml), [check](../lustre/datadog_checks/lustre/check.py), [readme](../lustre/README.md) |
| Nfsstat (`nfsstat`) | — | other — The Nfsstat check has no required configuration fields and no remote endpoint. | [spec](../nfsstat/assets/configuration/spec.yaml), [check](../nfsstat/datadog_checks/nfsstat/nfsstat.py), [readme](../nfsstat/README.md) |
| Postfix (`postfix`) | `directory` | other — Postfix is a host-local check: it counts files in /var/spool/postfix/<queue> directories or shells out to postqueue -p. There is no network endpoint to probe. | [spec](../postfix/assets/configuration/spec.yaml), [check](../postfix/datadog_checks/postfix/postfix.py) |
| Slurm (`slurm`) | — | other — The Slurm check shells out to local Slurm CLI binaries (sinfo, squeue, sacct, sdiag, sshare, scontrol, optionally seff) via subprocess and parses their delimited output. There is no remote endpoint, port, or service to probe. | [spec](../slurm/assets/configuration/spec.yaml), [check](../slurm/datadog_checks/slurm/check.py) |
| Tibco EMS (`tibco_ems`) | `username`, `password`, `script_path` | credentials-required — Despite the JMS framing, the check does not use JMXFetch: it shells out to the local `tibemsadmin` CLI (pattern L, local subprocess) and parses its output. | [spec](../tibco_ems/assets/configuration/spec.yaml), [check](../tibco_ems/datadog_checks/tibco_ems/tibco_ems.py) |
| Varnish (`varnish`) | `varnishstat` | other — The Varnish check does not talk to Varnish over the network — it shells out to the local `varnishstat` binary (and optionally `varnishadm`) and parses its text/XML/JSON output. | [spec](../varnish/assets/configuration/spec.yaml), [check](../varnish/datadog_checks/varnish/varnish.py), [upstream](https://varnish-cache.org/docs/trunk/reference/varnishstat.html) |

### `local-scm-enumeration` (1)

Enumerate the Windows Service Control Manager.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Windows Service (`windows_service`) | `services` | other — The check runs on the local Windows host and queries the Service Control Manager (win32service.OpenSCManager / EnumServicesStatusEx). There is no remote network probe. | [spec](../windows_service/assets/configuration/spec.yaml), [check](../windows_service/datadog_checks/windows_service/windows_service.py) |

### `cloud-task-metadata` (1)

Hit the link-local task metadata endpoint of the cloud platform (`ECS_CONTAINER_METADATA_URI_V4`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Amazon Fargate (`ecs_fargate`) | — | other — ECS Fargate metrics are collected by hitting the task-local ECS Container Metadata Endpoint, either v4 (URL provided in the `ECS_CONTAINER_METADATA_URI_V4` env var) or v2 (the link-local `http://169.254.170.2/v2`). | [spec](../ecs_fargate/assets/configuration/spec.yaml), [auto_conf](../ecs_fargate/datadog_checks/ecs_fargate/data/conf.yaml.default), [check](../ecs_fargate/datadog_checks/ecs_fargate/ecs_fargate.py), [upstream](https://docs.aws.amazon.com/AmazonECS/latest/userguide/task-metadata-endpoint-v4-fargate.html) |

### `local-config-file` (2)

Read a user-supplied local config or DB file (`duckdb` `.db` file, nagios `nagios.cfg`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| DuckDB (`duckdb`) | `db_name` | other — DuckDB is an embedded analytical database stored as a file on the local filesystem. The required `db_name` field is a filesystem path to a `.db` file (e.g. | [spec](../duckdb/assets/configuration/spec.yaml), [check](../duckdb/datadog_checks/duckdb/check.py) |
| Nagios (`nagios`) | `nagios_conf` | config-file-parse — The Nagios integration runs on the same host as a Nagios/Icinga v1 server and tails its log/perfdata files. The required `nagios_conf` points to the main config file (e.g. | [spec](../nagios/assets/configuration/spec.yaml), [check](../nagios/datadog_checks/nagios/nagios.py), [readme](../nagios/README.md) |

## Credentials required

_The check needs credentials that cannot be discovered from the wire. Sub-bucketed by what kind of credential._

### `creds-spec-mandated` (24)

Spec marks `username`/`password` (or equivalent) as required with no default. Typical for traditional databases.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Cacti (`cacti`) | `mysql_host`, `mysql_user`, `rrd_path` | credentials-required — The Cacti check connects to Cacti's MySQL backend (via pymysql) to enumerate hosts and RRD file metadata, then reads the on-disk RRD files (via rrdtool) for actual metric values. | [spec](../cacti/assets/configuration/spec.yaml), [check](../cacti/datadog_checks/cacti/cacti.py), [upstream](https://www.cacti.net/) |
| ClickHouse (`clickhouse`) | `server` | credentials-required — The check uses the `clickhouse-connect` Python client to authenticate to ClickHouse's HTTP interface (default port 8123) and runs SQL queries against `system.*` tables. | [spec](../clickhouse/assets/configuration/spec.yaml), [check](../clickhouse/datadog_checks/clickhouse/clickhouse.py), [readme](../clickhouse/README.md) |
| Couchbase (`couchbase`) | `server` | credentials-required — Couchbase exposes a REST API on port 8091 and the auto_conf template already covers host/port via `http://%%host%%:8091`. | [spec](../couchbase/assets/configuration/spec.yaml), [auto_conf](../couchbase/datadog_checks/couchbase/data/auto_conf.yaml), [check](../couchbase/datadog_checks/couchbase/couchbase.py), [upstream](https://docs.couchbase.com/server/current/rest-api/rest-intro.html) |
| DO Query Actions (`do_query_actions`) | `db_identifier`, `username`, `password`, `db_type`, `queries` | credentials-required — DO Query Actions is a generic SQL-execution framework (currently Postgres only) that runs user-defined queries on a user-nominated database on a user-defined schedule. | [spec](../do_query_actions/assets/configuration/spec.yaml), [check](../do_query_actions/datadog_checks/do_query_actions/check.py), [check](../do_query_actions/datadog_checks/do_query_actions/postgres_connection.py) |
| ESXi (`esxi`) | `host`, `username`, `password` | credentials-required — The ESXi check connects to the host's vSphere Web Services API via pyVmomi (`pyVim.connect.SmartConnect`) using `host`, `username`, and `password`, all marked required in the spec. | [spec](../esxi/assets/configuration/spec.yaml), [check](../esxi/datadog_checks/esxi/check.py), [readme](../esxi/README.md) |
| Harbor (`harbor`) | `url`, `username`, `password` | credentials-required — Harbor is a container registry with a REST API on port 80/443. | [spec](../harbor/assets/configuration/spec.yaml), [auto_conf](../harbor/datadog_checks/harbor/data/auto_conf.yaml), [check](../harbor/datadog_checks/harbor/harbor.py), [readme](../harbor/README.md), [upstream](https://goharbor.io/docs/latest/install-config/) |
| MarkLogic (`marklogic`) | `url`, `username`, `password` | credentials-required — MarkLogic is a NoSQL DB whose Management REST API on port 8002 (and the health view) is gated behind a user with the `manage-user` (or `manage-admin` for health checks) role. | [spec](../marklogic/assets/configuration/spec.yaml), [check](../marklogic/datadog_checks/marklogic/check.py), [upstream](https://docs.marklogic.com/REST/management) |
| MySQL (`mysql`) | `host`, `username`, `password` | credentials-required — MySQL needs a dedicated `datadog` user with explicit grants and a password to read performance schema and replication state. The credentials cannot be discovered from the wire — they must be provisioned in the database first. | [spec](../mysql/assets/configuration/spec.yaml) |
| OpenLDAP (`openldap`) | `url` | credentials-required — The check binds to slapd (`ldap3.Connection(...).bind()`) and reads the `cn=Monitor` backend. | [spec](../openldap/assets/configuration/spec.yaml), [check](../openldap/datadog_checks/openldap/openldap.py), [readme](../openldap/README.md), [upstream](https://www.openldap.org/doc/admin24/monitoringslapd.html) |
| PgBouncer (`pgbouncer`) | `host`, `username` | credentials-required — PgBouncer speaks the Postgres wire protocol on its admin port (default 6432) and exposes statistics via `SHOW` admin queries against the virtual `pgbouncer` database. | [spec](../pgbouncer/assets/configuration/spec.yaml), [check](../pgbouncer/datadog_checks/pgbouncer/pgbouncer.py), [upstream](https://www.pgbouncer.org/config.html) |
| Postgres (`postgres`) | `host`, `port`, `username`, `password` | credentials-required — Postgres requires a dedicated `datadog` user with explicit grants and a password to read the statistics views. The credentials cannot be discovered from the wire; they must be provisioned out-of-band. | [spec](../postgres/assets/configuration/spec.yaml) |
| ProxySQL (`proxysql`) | `host`, `port`, `username`, `password` | credentials-required — ProxySQL exposes its admin interface via the MySQL wire protocol on a separate admin port. The check connects with pymysql and queries stats_* tables in the admin schema, so a username and password (the ProxySQL admin user) are required. | [spec](../proxysql/assets/configuration/spec.yaml), [check](../proxysql/datadog_checks/proxysql/proxysql.py), [readme](../proxysql/README.md) |
| RethinkDB (`rethinkdb`) | `host`, `port` | credentials-required — The check connects to RethinkDB's driver port (default 28015) using the official `rethinkdb` Python driver and runs ReQL queries against the `rethinkdb` system database for cluster/server/table/job metrics. | [spec](../rethinkdb/assets/configuration/spec.yaml), [check](../rethinkdb/datadog_checks/rethinkdb/check.py), [check](../rethinkdb/datadog_checks/rethinkdb/config.py), [readme](../rethinkdb/README.md), [upstream](https://rethinkdb.com/docs/permissions-and-accounts/) |
| Silverstripe CMS (`silverstripe_cms`) | `SILVERSTRIPE_DATABASE_TYPE`, `SILVERSTRIPE_DATABASE_NAME`, `SILVERSTRIPE_DATABASE_SERVER_IP`, `SILVERSTRIPE_DATABASE_PORT`, `SILVERSTRIPE_DATABASE_USERNAME`, `SILVERSTRIPE_DATABASE_PASSWORD` | credentials-required — The Silverstripe CMS check connects directly to the backing database (MySQL or PostgreSQL) using DB credentials supplied by the user, then runs SQL queries against the Silverstripe schema to derive metrics. | [spec](../silverstripe_cms/assets/configuration/spec.yaml), [check](../silverstripe_cms/datadog_checks/silverstripe_cms/check.py) |
| SingleStore (`singlestore`) | `host`, `username`, `password` | credentials-required — SingleStore (formerly MemSQL) speaks the MySQL wire protocol (default port 3306). | [spec](../singlestore/assets/configuration/spec.yaml), [check](../singlestore/datadog_checks/singlestore/check.py), [other](../singlestore/datadog_checks/singlestore/queries.py), [readme](../singlestore/README.md) |
| SonarQube (`sonarqube`) | `web_endpoint` | credentials-required — SonarQube has two distinct collection modes: (1) a web API instance using `web_endpoint` (typically http://host:9000) plus basic auth, and (2) a JMX instance using JMXFetch with multiple JVMs (web, ce, search) on user-defined ports. | [spec](../sonarqube/assets/configuration/spec.yaml), [check](../sonarqube/datadog_checks/sonarqube/check.py), [readme](../sonarqube/README.md) |
| Sonatype Nexus (`sonatype_nexus`) | `username`, `password`, `server_url` | credentials-required — The Sonatype Nexus check authenticates against the Nexus Repository Manager REST API (/service/rest/v1/status/check and /service/metrics/data) using basic auth. The spec marks username, password, and server_url as required with no defaults. | [spec](../sonatype_nexus/assets/configuration/spec.yaml), [check](../sonatype_nexus/datadog_checks/sonatype_nexus/check.py) |
| SQL Server (`sqlserver`) | `host` | credentials-required — SQL Server requires either SQL authentication (username + password for a dedicated `datadog` user with `VIEW SERVER STATE` and other grants) or Windows authentication (the Agent's process identity must have those grants on the SQL Server instance). | [spec](../sqlserver/assets/configuration/spec.yaml), [upstream](https://learn.microsoft.com/en-us/sql/relational-databases/system-dynamic-management-views/system-dynamic-management-views) |
| SSH/SFTP (`ssh_check`) | `host`, `username` | credentials-required — The SSH check uses paramiko.SSHClient.connect with a required username and either password or private key, then verifies transport.is_authenticated(). | [spec](../ssh_check/assets/configuration/spec.yaml), [check](../ssh_check/datadog_checks/ssh_check/ssh_check.py), [readme](../ssh_check/README.md) |
| Teradata (`teradata`) | `server`, `database`, `username`, `password` | credentials-required — The Teradata check connects via the `teradatasql` driver (port 1025 by default) and runs SQL queries (DBC views, ResUsageSpma, AllSpaceV) under a dedicated Datadog database user. | [spec](../teradata/assets/configuration/spec.yaml), [check](../teradata/datadog_checks/teradata/check.py), [check](../teradata/datadog_checks/teradata/queries.py) |
| TokuMX (`tokumx`) | `server` | credentials-required — TokuMX is a deprecated MongoDB fork. The check uses a vendored pymongo client (`MongoClient`) and requires a full MongoDB URI in the form `mongodb://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>`. | [spec](../tokumx/assets/configuration/spec.yaml), [check](../tokumx/datadog_checks/tokumx/tokumx.py) |
| Vertica (`vertica`) | `server`, `port`, `username`, `password`, `db` | credentials-required — Vertica is a relational database. The check uses `vertica_python` to open a SQL connection on port 5433 and queries the `v_monitor` / `v_catalog` schemas. | [spec](../vertica/assets/configuration/spec.yaml), [check](../vertica/datadog_checks/vertica/vertica.py), [readme](../vertica/README.md) |
| VoltDB (`voltdb`) | `url`, `username`, `password` | credentials-required — The VoltDB check posts to the JSON HTTP client interface (default port 8080) and invokes system procedures such as @Statistics and @SystemInformation. | [spec](../voltdb/assets/configuration/spec.yaml), [check](../voltdb/datadog_checks/voltdb/check.py), [check](../voltdb/datadog_checks/voltdb/client.py) |
| vSphere (`vsphere`) | `host`, `username`, `password` | credentials-required — vSphere requires read-only vCenter credentials (host + username + password) to authenticate against the vSphere SOAP and REST APIs. There is no unauthenticated mode and no fixed endpoint that returns useful data without a valid session. | [spec](../vsphere/assets/configuration/spec.yaml), [check](../vsphere/datadog_checks/vsphere/vsphere.py) |

### `creds-jmx-rmi` (14)

JMX/RMI integrations (`is_jmx: true`, `template: instances/jmx`). Production deployments enable JMX authentication; ports are not standardized.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| ActiveMQ (`activemq`) | `host`, `port` | credentials-required — The activemq integration is a JMXFetch-backed check (spec inherits `instances/jmx`, `is_jmx: true`). | [spec](../activemq/assets/configuration/spec.yaml), [other](../activemq/datadog_checks/activemq/data/conf.yaml.example), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml), [upstream](https://activemq.apache.org/components/classic/documentation/jmx) |
| Cassandra (`cassandra`) | `host`, `port`, `user`, `password` | credentials-required — Cassandra is a JMXFetch-based integration: the spec inherits `template: instances/jmx` and sets `is_jmx: true`. | [spec](../cassandra/assets/configuration/spec.yaml), [upstream](https://cassandra.apache.org/doc/latest/cassandra/managing/operating/security.html) |
| Confluent Platform (`confluent_platform`) | `host`, `port` | credentials-required — Confluent Platform is a JMX integration (`is_jmx: true`, `template: instances/jmx`) that connects via the Java RMI registry to brokers, Connect, Schema Registry, REST Proxy, etc. | [spec](../confluent_platform/assets/configuration/spec.yaml), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml), [upstream](https://docs.confluent.io/platform/current/kafka/monitoring.html) |
| Hazelcast (`hazelcast`) | `host`, `port` | credentials-required — Hazelcast is primarily a JMXFetch integration: the spec inherits both `init_config/jmx` and `instances/jmx` (with default JMX port 1099), and the bundled Python check (`HazelcastCheck`) only adds a small HTTP poller for the optional Management Center health endpoint (`mc_health_check_endpoint`) plus a cluster-state mapping. | [spec](../hazelcast/assets/configuration/spec.yaml), [check](../hazelcast/datadog_checks/hazelcast/check.py), [readme](../hazelcast/README.md) |
| Hive (`hive`) | `host`, `port` | credentials-required — Hive is a JMXFetch-only integration: the spec inherits `template: instances/jmx` and there is no Python check class (the package only re-exports `__version__`). | [spec](../hive/assets/configuration/spec.yaml), [check](../hive/datadog_checks/hive/__init__.py), [readme](../hive/README.md) |
| HiveMQ (`hivemq`) | `host`, `port` | credentials-required — HiveMQ is monitored via JMXFetch: the spec inherits `template: instances/jmx` and `init_config/jmx`, so collection happens over JMX/RMI on the broker's monitoring port (default 9010). | [spec](../hivemq/assets/configuration/spec.yaml), [other](../hivemq/datadog_checks/hivemq/data/conf.yaml.example), [readme](../hivemq/README.md) |
| Hudi (`hudi`) | `host`, `port` | credentials-required — Hudi is a JMX integration: the spec only references `init_config/jmx` and `instances/jmx`, the integration ships a `metrics.yaml` MBean list, and there is no Python check (only an `__init__.py` re-exporting the version). | [spec](../hudi/assets/configuration/spec.yaml), [other](../hudi/datadog_checks/hudi/data/metrics.yaml), [other](../hudi/datadog_checks/hudi/data/conf.yaml.example) |
| Ignite (`ignite`) | `host`, `port` | credentials-required — Ignite is a JMXFetch-based integration: the spec inherits `init_config/jmx` and `instances/jmx`, with no Python check class — collection happens through JMXFetch using a shipped `metrics.yaml` MBean list. | [spec](../ignite/assets/configuration/spec.yaml), [other](../ignite/datadog_checks/ignite/data/metrics.yaml), [readme](../ignite/README.md) |
| JBoss/WildFly (`jboss_wildfly`) | `jmx_url` | credentials-required — JBoss/WildFly is a JMX integration handled by JMXFetch. The spec marks `jmx_url` as required (e.g. | [spec](../jboss_wildfly/assets/configuration/spec.yaml), [readme](../jboss_wildfly/README.md), [upstream](https://docs.wildfly.org/30/Admin_Guide.html#JMX) |
| Kafka (`kafka`) | `host`, `port` | credentials-required — The `kafka` integration is the JMX-based Kafka broker check (distinct from `kafka_consumer`). | [spec](../kafka/assets/configuration/spec.yaml), [readme](../kafka/README.md) |
| Presto (`presto`) | `host`, `port` | credentials-required — Presto is a JMXFetch-only integration: the spec inherits `template: instances/jmx`, the Python package has no check class (only `__version__`), and the shipped auto_conf.yaml sets `is_jmx: true` with `%%host%%` / `%%port%%` templating. | [spec](../presto/assets/configuration/spec.yaml), [auto_conf](../presto/datadog_checks/presto/data/auto_conf.yaml), [check](../presto/datadog_checks/presto/__init__.py), [readme](../presto/README.md) |
| Solr (`solr`) | `host`, `port` | credentials-required — Solr is collected via the JMXFetch helper (`template: instances/jmx`). Production Solr deployments enable JMX Remote with authentication and frequently TLS, requiring `user`, `password`, and key/trust-store paths. | [spec](../solr/assets/configuration/spec.yaml), [readme](../solr/README.md), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/jmx.yaml) |
| Tomcat (`tomcat`) | `host`, `port`, `user`, `password` | credentials-required — Tomcat is a JMX-based check that inherits JMXFetch (template: instances/jmx). | [spec](../tomcat/assets/configuration/spec.yaml), [auto_conf](../tomcat/datadog_checks/tomcat/data/auto_conf.yaml), [readme](../tomcat/README.md) |
| WebLogic (`weblogic`) | `host`, `port`, `user`, `password` | credentials-required — WebLogic is a JMXFetch-based integration. | [spec](../weblogic/assets/configuration/spec.yaml), [readme](../weblogic/README.md), [upstream](https://docs.oracle.com/en/middleware/standalone/weblogic-server/14.1.1.0/jmxcu/index.html) |

### `creds-api-token` (24)

Vendor REST API behind an API key, OAuth client credentials, or bearer token.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Amazon MSK (`amazon_msk`) | `cluster_arn` | credentials-required — The check requires a cluster ARN and uses boto3 (`kafka.list_nodes`) to enumerate broker endpoints, then scrapes the JMX/Node exporters per broker. | [spec](../amazon_msk/assets/configuration/spec.yaml), [check](../amazon_msk/datadog_checks/amazon_msk/check.py) |
| Ambari (`ambari`) | `url` | credentials-required — Ambari is the Hadoop cluster management REST API (typically on port 8080). The check walks the `/api/v1/clusters/...` tree to enumerate hosts, services, and components. | [spec](../ambari/assets/configuration/spec.yaml), [check](../ambari/datadog_checks/ambari/ambari.py), [readme](../ambari/README.md) |
| ArangoDB (`arangodb`) | `openmetrics_endpoint` | credentials-required — ArangoDB exposes Prometheus metrics on a fixed path (`/_admin/metrics/v2`) on its HTTP coordinator port (default 8529). | [spec](../arangodb/assets/configuration/spec.yaml), [check](../arangodb/datadog_checks/arangodb/check.py), [other](../arangodb/tests/docker/arangodb3/arangod.conf), [readme](../arangodb/README.md) |
| Avi Vantage (`avi_vantage`) | `avi_controller_url` | credentials-required — Avi Vantage is a load balancer whose Controller exposes Prometheus metrics under `/api/analytics/prometheus-metrics/<entity>`. | [spec](../avi_vantage/assets/configuration/spec.yaml), [check](../avi_vantage/datadog_checks/avi_vantage/check.py) |
| Cisco ACI (`cisco_aci`) | `aci_url`, `username`, `pwd` | credentials-required — The Cisco ACI check polls the APIC controller's REST API. | [spec](../cisco_aci/assets/configuration/spec.yaml), [check](../cisco_aci/datadog_checks/cisco_aci/cisco.py), [readme](../cisco_aci/README.md) |
| Citrix Hypervisor (`citrix_hypervisor`) | `url` | credentials-required — Citrix Hypervisor (formerly XenServer) exposes its RRD metrics over an HTTP endpoint (`/host_rrd`, `/rrd_updates`) protected by HTTP Basic auth against the XAPI user database. | [spec](../citrix_hypervisor/assets/configuration/spec.yaml), [check](../citrix_hypervisor/datadog_checks/citrix_hypervisor/check.py) |
| Cloud Foundry API (`cloud_foundry_api`) | `api_url`, `client_id`, `client_secret` | credentials-required — The Cloud Foundry API check authenticates to UAA with a `client_id` / `client_secret` (OAuth2 client credentials grant) and uses the resulting bearer token to query the Cloud Controller v2/v3 APIs. | [spec](../cloud_foundry_api/assets/configuration/spec.yaml), [check](../cloud_foundry_api/datadog_checks/cloud_foundry_api/cloud_foundry_api.py) |
| Cloudera (`cloudera`) | `api_url`, `workload_username`, `workload_password` | credentials-required — The Cloudera integration scrapes the Cloudera Manager REST API (e.g. `https://cluster1.cloudera.site/cluster_1/cdp-proxy-api/cm-api/v48`). | [spec](../cloudera/assets/configuration/spec.yaml), [check](../cloudera/datadog_checks/cloudera/check.py) |
| Control-M (`control_m`) | `control_m_api_endpoint` | credentials-required — Control-M is BMC's enterprise job scheduler. The check talks to the Control-M Automation API at a deployment-specific URL (e.g. | [spec](../control_m/assets/configuration/spec.yaml), [check](../control_m/datadog_checks/control_m/check.py) |
| Fly.io (`fly_io`) | `org_slug` | credentials-required — Fly.io is a hosted platform, not a co-located service. | [spec](../fly_io/assets/configuration/spec.yaml), [check](../fly_io/datadog_checks/fly_io/check.py), [readme](../fly_io/README.md) |
| IBM WAS (`ibm_was`) | `servlet_url` | credentials-required — Despite the JMX/WebSphere reputation, this check is an HTTP scraper of WebSphere's PerfServlet (`requests.get(servlet_url)` parsing PMI XML), not JMXFetch. | [spec](../ibm_was/assets/configuration/spec.yaml), [check](../ibm_was/datadog_checks/ibm_was/ibm_was.py), [readme](../ibm_was/README.md) |
| NiFi (`nifi`) | `api_url` | credentials-required — The NiFi integration polls the NiFi REST API (default `https://localhost:8443/nifi-api`) for system diagnostics, flow status, process group, connection, processor, bulletin and cluster summary endpoints. | [spec](../nifi/assets/configuration/spec.yaml), [check](../nifi/datadog_checks/nifi/check.py), [check](../nifi/datadog_checks/nifi/api.py), [readme](../nifi/README.md) |
| Nutanix (`nutanix`) | `pc_ip`, `pc_username`, `pc_password` | credentials-required — The Nutanix integration talks to a Prism Central HTTPS API (default port 9440) using basic auth. | [spec](../nutanix/assets/configuration/spec.yaml), [check](../nutanix/datadog_checks/nutanix/check.py), [readme](../nutanix/README.md) |
| Octopus Deploy (`octopus_deploy`) | `octopus_endpoint`, `api_key` | credentials-required — Octopus Deploy's REST API requires an `X-Octopus-ApiKey` header (or equivalent `auth_token`) for every request. The README explicitly directs users to create an API key in the Octopus Server before configuring the check. | [spec](../octopus_deploy/assets/configuration/spec.yaml), [check](../octopus_deploy/datadog_checks/octopus_deploy/check.py), [readme](../octopus_deploy/README.md), [upstream](https://octopus.com/docs/octopus-rest-api/getting-started#authentication) |
| OpenStack (`openstack`) | `keystone_server_url`, `name`, `user` | credentials-required — The legacy OpenStack check authenticates against Keystone Identity API v3 to obtain a scoped/unscoped token, then walks Nova / Neutron service catalogs. | [spec](../openstack/assets/configuration/spec.yaml), [check](../openstack/datadog_checks/openstack/openstack.py) |
| OpenStack Controller (`openstack_controller`) | `keystone_server_url`, `user` | credentials-required — The OpenStack Controller check authenticates against Keystone (Identity API v3) using a username/password/domain triple (or an openstack-sdk clouds.yaml file referenced via openstack_config_file_path / openstack_cloud_name). | [spec](../openstack_controller/assets/configuration/spec.yaml), [check](../openstack_controller/datadog_checks/openstack_controller/openstack_controller.py), [readme](../openstack_controller/README.md) |
| PowerDNS Recursor (`powerdns_recursor`) | `host`, `port`, `api_key` | credentials-required — The PowerDNS Recursor check hits the recursor's webserver statistics API (`/api/v1/servers/localhost/statistics` on v4, `/servers/localhost/statistics` on v3). | [spec](../powerdns_recursor/assets/configuration/spec.yaml), [check](../powerdns_recursor/datadog_checks/powerdns_recursor/powerdns_recursor.py), [readme](../powerdns_recursor/README.md), [upstream](https://doc.powerdns.com/recursor/http-api/index.html) |
| Proxmox (`proxmox`) | `proxmox_server`, `headers` | credentials-required — The Proxmox check queries the Proxmox VE REST API at `proxmox_server` (default port 8006, path /api2/json). | [spec](../proxmox/assets/configuration/spec.yaml), [check](../proxmox/datadog_checks/proxmox/check.py), [readme](../proxmox/README.md), [upstream](https://pve.proxmox.com/pve-docs/api-viewer/) |
| RiakCS (`riakcs`) | `access_id`, `access_secret` | credentials-required — RiakCS is the S3-compatible Riak fork. The check uses boto3 to issue an S3 GetObject against the special `riak-cs` bucket, which requires an `access_id` and `access_secret` pair from the user's RiakCS deployment. | [spec](../riakcs/assets/configuration/spec.yaml), [check](../riakcs/datadog_checks/riakcs/riakcs.py) |
| Silk (`silk`) | `host_address`, `username`, `password` | credentials-required — The Silk check builds a base URL `<host_address>/api/v2/` and issues authenticated HTTPS GETs to the Silk Cloud Data Platform REST API (`/system/state`, `/system/servers`, etc.). | [spec](../silk/assets/configuration/spec.yaml), [check](../silk/datadog_checks/silk/check.py), [check](../silk/datadog_checks/silk/constants.py) |
| SNMP (`snmp`) | `ip_address` | credentials-required — SNMP needs at minimum the device IP and a credential: a community string (v1/v2c) or a v3 user with auth/priv keys. | [spec](../snmp/assets/configuration/spec.yaml), [upstream](https://datatracker.ietf.org/doc/html/rfc3411) |
| Supabase (`supabase`) | — | credentials-required — Supabase exposes two Prometheus-compatible endpoints. | [spec](../supabase/assets/configuration/spec.yaml), [check](../supabase/datadog_checks/supabase/check.py), [readme](../supabase/README.md), [upstream](https://supabase.com/docs/guides/monitoring-troubleshooting/metrics#accessing-the-metrics-endpoint) |
| TeamCity (`teamcity`) | `server` | credentials-required — TeamCity is a JetBrains CI server reached over HTTP at a per-deployment hostname (e.g. `http://teamcity.<ACCOUNT_NAME>.com`). | [spec](../teamcity/assets/configuration/spec.yaml), [check](../teamcity/datadog_checks/teamcity/check.py), [readme](../teamcity/README.md) |
| Prisma Cloud Compute (Twistlock) (`twistlock`) | `url`, `username`, `password` | credentials-required — The Twistlock check targets the Prisma Cloud Compute Console API and authenticates with basic auth using a Prisma Cloud API access key ID (username) and secret key (password). | [spec](../twistlock/assets/configuration/spec.yaml), [check](../twistlock/datadog_checks/twistlock/config.py), [check](../twistlock/datadog_checks/twistlock/twistlock.py), [readme](../twistlock/README.md) |

### `creds-proprietary-client` (9)

Needs a proprietary client library installed on the Agent host (Oracle Instant Client, IBM `ibm_db`, `pymqi`, `hdbcli`, FoundationDB cluster file, …) plus credentials.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| FoundationDB (`foundationdb`) | — | credentials-required — FoundationDB clients connect through a binary cluster file (`fdb.cluster`) which contains a description, an ID, and the coordinator IP/port list. | [spec](../foundationdb/assets/configuration/spec.yaml), [check](../foundationdb/datadog_checks/foundationdb/check.py) |
| IBM ACE (`ibm_ace`) | `mq_server`, `mq_port`, `channel`, `queue_manager` | credentials-required — IBM App Connect Enterprise is collected by subscribing to IBM MQ pub/sub topics over MQI (via `pymqi`). | [spec](../ibm_ace/assets/configuration/spec.yaml), [check](../ibm_ace/datadog_checks/ibm_ace/check.py), [check](../ibm_ace/datadog_checks/ibm_ace/subscription.py) |
| IBM Db2 (`ibm_db2`) | `db`, `username`, `password` | credentials-required — IBM Db2 connects via the `ibm_db` driver and the spec marks `db` (database name), `username`, and `password` as required. | [spec](../ibm_db2/assets/configuration/spec.yaml), [check](../ibm_db2/datadog_checks/ibm_db2/ibm_db2.py), [readme](../ibm_db2/README.md) |
| IBM i ⚠ (`ibm_i`) | `username`, `password`, `driver` | credentials-required — IBM i (formerly AS/400) is queried over ODBC using a proprietary driver (`iSeries Access ODBC Driver` or equivalent). The check spawns a Python subprocess that opens a pyodbc connection and runs SQL against `SYSIBMADM` views. | [spec](../ibm_i/assets/configuration/spec.yaml), [check](../ibm_i/datadog_checks/ibm_i/check.py), [upstream](https://www.ibm.com/docs/en/i/7.4?topic=services-odbc-driver) |
| IBM MQ (`ibm_mq`) | `channel`, `queue_manager` | credentials-required — IBM MQ requires the proprietary `pymqi` client library and connects to a queue manager over a named SVRCONN channel. | [spec](../ibm_mq/assets/configuration/spec.yaml), [check](../ibm_mq/datadog_checks/ibm_mq/ibm_mq.py), [upstream](https://www.ibm.com/docs/en/ibm-mq) |
| IBM Spectrum LSF (`ibm_spectrum_lsf`) | `cluster_name` | other — IBM Spectrum LSF is a HPC workload scheduler. | [spec](../ibm_spectrum_lsf/assets/configuration/spec.yaml), [check](../ibm_spectrum_lsf/datadog_checks/ibm_spectrum_lsf/check.py), [check](../ibm_spectrum_lsf/datadog_checks/ibm_spectrum_lsf/client.py), [readme](../ibm_spectrum_lsf/README.md) |
| MapR (`mapr`) | — | credentials-required — MapR is the Hadoop distribution. The check consumes metrics from a MapR Streams (Kafka-API) topic at `<stream_path>/<stream_id>:<hostname>` using the MapR-flavoured `confluent_kafka` library. | [spec](../mapr/assets/configuration/spec.yaml), [check](../mapr/datadog_checks/mapr/mapr.py) |
| Oracle Database ⚠ (`oracle`) | `server`, `service_name`, `username`, `password` | credentials-required — The Oracle Database integration requires a dedicated Datadog database user with explicit grants (`username` + `password` are both `required: true`) plus the `service_name` of the target instance. | [spec](../oracle/assets/configuration/spec.yaml), [readme](../oracle/README.md), [upstream](https://docs.datadoghq.com/database_monitoring/setup_oracle/) |
| SAP HANA (`sap_hana`) | `server`, `username`, `password` | credentials-required — The SAP HANA check connects to the HANA database over the SQL protocol via the proprietary `hdbcli` driver and runs SQL queries against monitoring views (`M_*`, `SYS_DATABASES.*`). | [spec](../sap_hana/assets/configuration/spec.yaml), [check](../sap_hana/datadog_checks/sap_hana/sap_hana.py), [readme](../sap_hana/README.md), [upstream](https://help.sap.com/docs/SAP_HANA_PLATFORM) |

### `creds-auth-optional-practical` (4)

Spec marks auth as optional but production deployments invariably need it (xpack-secured Elasticsearch, MongoDB with auth enabled, Vault token-gated metrics, etc.).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| ActiveMQ XML (`activemq_xml`) | `url` | credentials-required — Unlike its JMX-based sibling `activemq`, this integration scrapes ActiveMQ's web console: it appends `/admin/xml/queues.jsp`, `/admin/xml/topics.jsp`, `/admin/xml/subscribers.jsp` to the user-supplied base `url` and parses the XML with `ElementTree`. | [spec](../activemq_xml/assets/configuration/spec.yaml), [check](../activemq_xml/datadog_checks/activemq_xml/activemq_xml.py), [readme](../activemq_xml/README.md), [upstream](https://activemq.apache.org/web-console) |
| Kafka Consumer (`kafka_consumer`) | `kafka_connect_str` | other — The check connects to a Kafka broker bootstrap list (default port 9092 for plaintext, 9093 for SSL/SASL). Strictly per spec only `kafka_connect_str` is required, so unauthenticated localhost setups would discover via a Metadata API request. | [spec](../kafka_consumer/assets/configuration/spec.yaml), [upstream](https://kafka.apache.org/protocol) |
| MongoDB (`mongo`) | `hosts` | credentials-required — MongoDB's default port (27017) is universal and the wire protocol is detectable. Strictly per spec, only `hosts` is required, so an unauthenticated localhost dev instance could be discovered generically. | [spec](../mongo/assets/configuration/spec.yaml), [upstream](https://www.mongodb.com/docs/manual/tutorial/configure-x509-client-authentication/) |
| Vault (`vault`) | `api_url` | credentials-required — Vault exposes its API on a known port (8200) under /v1, and the agent can reach /sys/health unauthenticated for basic status checks. | [spec](../vault/assets/configuration/spec.yaml), [check](../vault/datadog_checks/vault/vault.py), [check](../vault/datadog_checks/vault/check.py) |

## No probe surface

_No reachable upstream service to probe at all. The integration is a logs sink, a DogStatsD listener, a generic configuration template, or a synthetic check._

### `logs-only` (38)

Vendor security tile or log-only integration. The Agent ingests logs; there is no metric check to schedule.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Arctic Wolf Aurora Endpoint Security (`arctic_wolf_aurora_endpoint_security`) | — | other — Arctic Wolf Aurora Endpoint Security is a logs-only marketplace tile. | [spec](../arctic_wolf_aurora_endpoint_security/assets/configuration/spec.yaml), [readme](../arctic_wolf_aurora_endpoint_security/README.md) |
| Barracuda SecureEdge (`barracuda_secure_edge`) | — | other — Barracuda SecureEdge is a marketplace tile that ships only a logs configuration: the spec defines a fixed set of file tail sources under /var/phion/logs/ (auth, firewall, vpn, ssh, etc.) and the datadog_checks/barracuda_secure_edge package contains no check.py - just __about__.py, __init__.py and the conf.yaml.example. | [spec](../barracuda_secure_edge/assets/configuration/spec.yaml), [readme](../barracuda_secure_edge/README.md) |
| BeyondTrust Password Safe (`beyondtrust_password_safe`) | — | other — BeyondTrust Password Safe is a marketplace tile that ships only a logs configuration - a TCP syslog receiver where the user must supply <PORT> for the appliance to forward syslog to. | [spec](../beyondtrust_password_safe/assets/configuration/spec.yaml), [readme](../beyondtrust_password_safe/README.md) |
| BeyondTrust Privileged Remote Access (`beyondtrust_privileged_remote_access`) | — | other — BeyondTrust Privileged Remote Access is a marketplace tile that ships only a logs configuration - a TCP syslog receiver with custom log_processing_rules where the user must supply <PORT> for the appliance to forward syslog to. | [spec](../beyondtrust_privileged_remote_access/assets/configuration/spec.yaml), [readme](../beyondtrust_privileged_remote_access/README.md) |
| Checkpoint Harmony Endpoint (`checkpoint_harmony_endpoint`) | — | other — Checkpoint Harmony Endpoint is a logs-only vendor security tile. | [spec](../checkpoint_harmony_endpoint/assets/configuration/spec.yaml), [readme](../checkpoint_harmony_endpoint/README.md) |
| Check Point Quantum Firewall (`checkpoint_quantum_firewall`) | — | other — Check Point Quantum Firewall is a logs-only vendor security tile. | [spec](../checkpoint_quantum_firewall/assets/configuration/spec.yaml), [readme](../checkpoint_quantum_firewall/README.md) |
| Cisco ASA (`cisco_asa`) | — | other — Cisco ASA is a logs-only vendor security tile. | [spec](../cisco_asa/assets/configuration/spec.yaml), [readme](../cisco_asa/README.md) |
| Cisco Secure Client (`cisco_secure_client`) | — | other — Cisco Secure Client (formerly AnyConnect) is a logs-only vendor security tile. | [spec](../cisco_secure_client/assets/configuration/spec.yaml), [readme](../cisco_secure_client/README.md) |
| Cisco Secure Firewall (`cisco_secure_firewall`) | — | other — Cisco Secure Firewall is a logs-only vendor security tile. The spec defines only a `logs` template (tcp/udp syslog) and there is no Agent check class under `datadog_checks/cisco_secure_firewall/` (no <name>.py implementation). | [spec](../cisco_secure_firewall/assets/configuration/spec.yaml), [readme](../cisco_secure_firewall/README.md) |
| Cisco Secure Web Appliance (`cisco_secure_web_appliance`) | — | other — Cisco Secure Web Appliance (CSWA) is a logs-only vendor security tile. The spec exposes only a `logs` template offering two collection modes (file tail of access_logs/l4tm_logs, or UDP syslog). | [spec](../cisco_secure_web_appliance/assets/configuration/spec.yaml), [readme](../cisco_secure_web_appliance/README.md) |
| Barracuda CloudGen Firewall (`cloudgen_firewall`) | — | other — Barracuda CloudGen Firewall is a logs-only vendor security tile. The spec defines only a `logs` template tailing a file (default example `/var/log/cloudgen_firewall.log`). | [spec](../cloudgen_firewall/assets/configuration/spec.yaml), [readme](../cloudgen_firewall/README.md) |
| Delinea Privilege Manager (`delinea_privilege_manager`) | — | other — Delinea Privilege Manager is a vendor security marketplace tile with no Agent metric check. | [spec](../delinea_privilege_manager/assets/configuration/spec.yaml), [readme](../delinea_privilege_manager/README.md) |
| Delinea Secret Server (`delinea_secret_server`) | — | other — Delinea Secret Server is a vendor security marketplace tile with no Agent metric check. | [spec](../delinea_secret_server/assets/configuration/spec.yaml), [readme](../delinea_secret_server/README.md) |
| ESET Protect (`eset_protect`) | — | other — ESET Protect is a logs-only integration. The spec defines no Agent metric check; the only options are a `logs` template (TCP/UDP listener with a user-chosen port, source eset-protect). | [spec](../eset_protect/assets/configuration/spec.yaml), [readme](../eset_protect/README.md) |
| Flink (`flink`) | — | other — The Flink integration ships no Agent check binary — datadog_checks/flink/ contains only __init__.py and a logs-only conf.yaml.example. | [spec](../flink/assets/configuration/spec.yaml), [readme](../flink/README.md), [other](../flink/datadog_checks/flink/data/conf.yaml.example) |
| Forescout (`forescout`) | — | other — Forescout is a logs-only vendor tile. | [spec](../forescout/assets/configuration/spec.yaml), [readme](../forescout/README.md) |
| iboss (`iboss`) | — | other — iboss is a logs-only vendor security tile (Secure Web Gateway / Zero Trust). | [spec](../iboss/assets/configuration/spec.yaml), [readme](../iboss/README.md), [other](../iboss/manifest.json) |
| Ivanti Connect Secure (`ivanti_connect_secure`) | — | other — Ivanti Connect Secure is a logs-only vendor security tile. | [spec](../ivanti_connect_secure/assets/configuration/spec.yaml), [readme](../ivanti_connect_secure/README.md) |
| journald (`journald`) | — | other — journald is a logs-only integration: there is no Agent metric check (no `journald.py`), only a logs configuration template. The Agent's logs subsystem reads the systemd journal locally via `/var/log/journal` or `/run/log/journal`. | [spec](../journald/assets/configuration/spec.yaml), [readme](../journald/README.md) |
| Juniper SRX Firewall (`juniper_srx_firewall`) | — | other — Juniper SRX Firewall is a logs-only vendor security tile. | [spec](../juniper_srx_firewall/assets/configuration/spec.yaml), [readme](../juniper_srx_firewall/README.md) |
| Keycloak (`keycloak`) | — | other — The Keycloak integration is logs-only: spec.yaml defines a single `logs` block (TCP/UDP syslog listener) and no `instances` block, and the package ships no Agent check class — there is no live endpoint for an auto-config probe to hit. | [spec](../keycloak/assets/configuration/spec.yaml), [readme](../keycloak/README.md) |
| Linux Audit Logs (`linux_audit_logs`) | — | other — Linux Audit Logs is a logs-only integration. | [spec](../linux_audit_logs/assets/configuration/spec.yaml), [readme](../linux_audit_logs/README.md), [other](../linux_audit_logs/datadog_checks/linux_audit_logs/__init__.py) |
| Mac Audit Logs (`mac_audit_logs`) | `MONITOR`, `AUDIT_LOGS_DIR_PATH` | other — Mac Audit Logs is a logs-only integration. The check runs locally on a macOS host with BSM auditing enabled and shells out to `auditreduce` / `praudit` (via sudo) to parse files in /var/audit. | [spec](../mac_audit_logs/assets/configuration/spec.yaml), [check](../mac_audit_logs/datadog_checks/mac_audit_logs/check.py), [readme](../mac_audit_logs/README.md) |
| Microsoft DNS (`microsoft_dns`) | — | other — Microsoft DNS is a logs-only integration (despite the hint suggesting a perf-counters shape). | [spec](../microsoft_dns/assets/configuration/spec.yaml), [readme](../microsoft_dns/README.md) |
| Microsoft Sysmon (`microsoft_sysmon`) | — | other — Microsoft Sysmon is a logs-only integration. | [spec](../microsoft_sysmon/assets/configuration/spec.yaml), [readme](../microsoft_sysmon/README.md) |
| OpenVPN (`openvpn`) | — | other — The OpenVPN integration is logs-only: there is no Agent metric check (the `datadog_checks/openvpn/` package contains only a version stub, no `check.py`). | [spec](../openvpn/assets/configuration/spec.yaml), [readme](../openvpn/README.md) |
| OSSEC Security (`ossec_security`) | — | other — OSSEC is a host-based intrusion detection system. The integration spec contains only a `logs` template (UDP listener at a user-supplied `<PORT>`) and ships no Agent metric check (no <name>.py beyond `__init__.py`). | [spec](../ossec_security/assets/configuration/spec.yaml), [readme](../ossec_security/README.md) |
| Palo Alto Panorama (`palo_alto_panorama`) | — | other — Palo Alto Panorama is a vendor security tile for centralized Palo Alto firewall management. The spec contains only a `logs` template (UDP/TCP listener at a user-supplied `<PORT>`) and there is no Agent metric check beyond `__init__.py`. | [spec](../palo_alto_panorama/assets/configuration/spec.yaml), [readme](../palo_alto_panorama/README.md) |
| Palo Alto Networks Firewall (`pan_firewall`) | — | other — pan_firewall is a vendor logs-only tile for Palo Alto Networks firewall syslog ingestion. The spec contains only a `logs` template (TCP listener on port 10518 by default) and ships no Agent metric check beyond `__init__.py`. | [spec](../pan_firewall/assets/configuration/spec.yaml), [readme](../pan_firewall/README.md) |
| PingFederate (`ping_federate`) | — | other — Despite the hint about Prometheus, the PingFederate tile in this repo is logs-only: the spec contains only a `logs` template (file tail of `<pf_install>/pingfederate/log/admin.log` and `audit.log`) and ships no Agent metric check beyond `__init__.py`. | [spec](../ping_federate/assets/configuration/spec.yaml), [readme](../ping_federate/README.md) |
| SonicWall Firewall (`sonicwall_firewall`) | — | other — SonicWall Firewall is a logs-only vendor tile. | [spec](../sonicwall_firewall/assets/configuration/spec.yaml), [readme](../sonicwall_firewall/README.md) |
| Suricata (`suricata`) | — | other — Suricata is a logs-only integration. The spec defines no Agent metric check — only a `template: logs` block pointing the Agent at `/var/log/suricata/eve.json`. | [spec](../suricata/assets/configuration/spec.yaml), [readme](../suricata/README.md) |
| Symantec Endpoint Protection (`symantec_endpoint_protection`) | — | other — Symantec Endpoint Protection is a vendor-tile logs-only integration. | [spec](../symantec_endpoint_protection/assets/configuration/spec.yaml), [readme](../symantec_endpoint_protection/README.md) |
| Tenable (Nessus) (`tenable`) | — | other — Tenable ships only a logs configuration that tails Nessus log files (backend.log, www_server.log) - there is no Agent check class, no metrics endpoint, and no network probe. | [spec](../tenable/assets/configuration/spec.yaml), [readme](../tenable/README.md) |
| WatchGuard Firebox (`watchguard_firebox`) | — | other — WatchGuard Firebox is a vendor tile with no Agent metric check: there is no `datadog_checks/<name>/<name>.py`, no `OpenMetricsBaseCheck` subclass, and the spec defines only a `logs` template that ingests UDP syslog on a user-chosen port. | [spec](../watchguard_firebox/assets/configuration/spec.yaml), [readme](../watchguard_firebox/README.md), [other](../watchguard_firebox/manifest.json) |
| Wazuh (`wazuh`) | — | other — Wazuh is a host intrusion detection system (HIDS). | [spec](../wazuh/assets/configuration/spec.yaml), [readme](../wazuh/README.md) |
| Zeek (`zeek`) | — | other — Zeek (formerly Bro) is a network IDS. The Datadog integration is logs-only: the spec defines a single `logs` block where the user picks one of two collection modes. | [spec](../zeek/assets/configuration/spec.yaml), [readme](../zeek/README.md) |
| Zscaler Private Access (`zscaler_private_access`) | — | other — Zscaler Private Access is a vendor SaaS security tile. | [spec](../zscaler_private_access/assets/configuration/spec.yaml), [readme](../zscaler_private_access/README.md) |

### `dogstatsd-only` (1)

Application instruments via DogStatsD; the Agent only listens — no probe.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Sidekiq (`sidekiq`) | — | other — Sidekiq is not an Agent metric check at all. The integration is wired through DogStatsD: the application emits StatsD metrics via the dogstatsd-ruby client and the Agent ingests them through its DogStatsD listener. | [spec](../sidekiq/assets/configuration/spec.yaml), [readme](../sidekiq/README.md) |

### `user-schema-template` (6)

The integration is a configuration framework: the user supplies the URL / counter list / metric mapping that defines what to collect.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Kafka Actions (`kafka_actions`) | `remote_config_id`, `kafka_connect_str` | other — Kafka Actions is not a discovery-style metric check. The README states explicitly that the integration is exclusively triggered by Remote Configuration and should never be scheduled manually. | [spec](../kafka_actions/assets/configuration/spec.yaml), [check](../kafka_actions/datadog_checks/kafka_actions/check.py), [readme](../kafka_actions/README.md) |
| OpenMetrics (`openmetrics`) | `openmetrics_endpoint`, `namespace`, `metrics` | other — The OpenMetrics integration is a generic, user-supplied scraper for arbitrary Prometheus / OpenMetrics endpoints. | [spec](../openmetrics/assets/configuration/spec.yaml), [check](../openmetrics/datadog_checks/openmetrics/openmetrics.py), [readme](../openmetrics/README.md) |
| PDH (`pdh_check`) | `countersetname`, `metrics` | other — pdh_check is the generic Windows Performance Counter (PDH) framework: the user must declare which `countersetname` to query and supply a list of `[<COUNTER_NAME>, <METRIC_NAME>, <METRIC_TYPE>]` mappings. | [spec](../pdh_check/assets/configuration/spec.yaml), [check](../pdh_check/datadog_checks/pdh_check/pdh_check.py), [other](../datadog_checks_dev/datadog_checks/dev/tooling/templates/configuration/instances/pdh_legacy.yaml) |
| Prometheus (`prometheus`) | `prometheus_url`, `namespace`, `metrics` | other — This is the user-supplied Generic Prometheus check (`GenericPrometheusCheck`). | [spec](../prometheus/assets/configuration/spec.yaml), [check](../prometheus/datadog_checks/prometheus/prometheus.py), [readme](../prometheus/README.md) |
| Windows Performance Counters (`windows_performance_counters`) | `metrics` | other — This is a generic, user-supplied template check — not a check tied to any specific upstream service. | [spec](../windows_performance_counters/assets/configuration/spec.yaml), [check](../windows_performance_counters/datadog_checks/windows_performance_counters/check.py), [readme](../windows_performance_counters/README.md) |
| WMI (`wmi_check`) | `class`, `metrics` | other — wmi_check is a generic, schema-driven WMI bridge: the user must declare which WMI `class` to query and which `[<WMI_PROPERTY>, <METRIC_NAME>, <METRIC_TYPE>]` mappings to emit. | [spec](../wmi_check/assets/configuration/spec.yaml), [check](../wmi_check/datadog_checks/wmi_check/wmi_check.py), [upstream](https://learn.microsoft.com/en-us/windows/win32/wmisdk/wmi-start-page) |

### `user-intent-synthetic` (6)

User nominates an arbitrary target to probe (`http_check`, `tcp_check`, `dns_check`, `tls`, `directory`, `win32_event_log`).

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Directory (`directory`) | `directory` | other — The Directory check is a generic filesystem-scanning template: the user MUST supply an absolute path in `directory`, and the check walks it with `os.walk`, calling `stat()` on matching files to emit gauges/histograms (sizes, mtimes, file/folder counts). | [spec](../directory/assets/configuration/spec.yaml), [check](../directory/datadog_checks/directory/directory.py), [readme](../directory/README.md) |
| DNS (`dns_check`) | `hostname` | other — The DNS check is a synthetic probe: the user nominates a `hostname` (required) to resolve via `dns.resolver.Resolver().query()` and the check measures resolution time and optionally validates the result against an expected `resolves_as` IP list. | [spec](../dns_check/assets/configuration/spec.yaml), [check](../dns_check/datadog_checks/dns_check/dns_check.py), [readme](../dns_check/README.md) |
| HTTP (`http_check`) | `name`, `url` | other — http_check is a synthetic / user-intent probe (pattern K). The check accepts an arbitrary user-nominated URL and issues an HTTP request, validating status codes, response body content, and optionally the TLS certificate. | [spec](../http_check/assets/configuration/spec.yaml), [check](../http_check/datadog_checks/http_check/http_check.py), [readme](../http_check/README.md) |
| TCP (`tcp_check`) | `name`, `host`, `port` | other — tcp_check is a synthetic user-intent probe (pattern K): the user nominates an arbitrary host and port to test TCP reachability. There is no upstream service running alongside the Agent to discover; the integration is the probe itself. | [spec](../tcp_check/assets/configuration/spec.yaml), [check](../tcp_check/datadog_checks/tcp_check/tcp_check.py) |
| TLS (`tls`) | `server` | other — TLS is a synthetic check: the user explicitly nominates a hostname/IP (or local cert file) they want to monitor. | [spec](../tls/assets/configuration/spec.yaml), [check](../tls/datadog_checks/tls/tls.py), [readme](../tls/README.md) |
| Event Viewer (`win32_event_log`) | `path` | other — This is the Windows Event Viewer integration, distinct from the `win_event_log` Datadog Agent (Go) check of the same family. | [spec](../win32_event_log/assets/configuration/spec.yaml), [check](../win32_event_log/datadog_checks/win32_event_log/check.py), [upstream](https://learn.microsoft.com/en-us/windows/win32/wes/windows-event-log) |

### `per-process-discovery` (5)

User picks which application to monitor by name; the check enumerates host processes.

| Integration | Required fields | Method / detail | References |
|---|---|---|---|
| Go Expvar (`go_expvar`) | `expvar_url` | other — go_expvar scrapes Go's built-in `/debug/vars` endpoint exposed by `net/http/pprof` or `expvar.Handler()` in any Go program that opted in. | [spec](../go_expvar/assets/configuration/spec.yaml), [check](../go_expvar/datadog_checks/go_expvar/go_expvar.py), [readme](../go_expvar/README.md) |
| Go-Metro (TCP RTT) (`go_metro`) | `interface`, `ips`, `hosts` | other — go-metro is a passive TCP RTT sniffer (a Go binary using libpcap) that ships only with the legacy 64-bit Agent v5 DEB/RPM packages and is not available in Agent v6+. | [spec](../go-metro/assets/configuration/spec.yaml), [readme](../go-metro/README.md) |
| GuardDog (`guarddog`) | `guarddog_path`, `package_ecosystem`, `dependency_file_path` | other — GuardDog is a Datadog-authored CLI scanner that audits dependency manifests for malicious packages. | [spec](../guarddog/assets/configuration/spec.yaml), [check](../guarddog/datadog_checks/guarddog/check.py), [readme](../guarddog/README.md) |
| Gunicorn (`gunicorn`) | `proc_name` | other — The Gunicorn check does not probe the network at all. It uses `psutil.process_iter` on the host where the Agent runs to find a master process whose cmdline matches `gunicorn: master [<proc_name>]`. | [spec](../gunicorn/assets/configuration/spec.yaml), [check](../gunicorn/datadog_checks/gunicorn/gunicorn.py), [readme](../gunicorn/README.md) |
| Process (`process`) | `name` | other — The Process check is the canonical generic process-monitoring template (patterns J + M). It does not probe the network at all: it iterates host processes via `psutil` and matches against user-supplied `search_string`, `pid`, or `pid_file`. | [spec](../process/assets/configuration/spec.yaml), [check](../process/datadog_checks/process/process.py), [readme](../process/README.md) |
