name: Keycloak
fleet_configurable: true
files:
- name: keycloak.yaml
  options:
  - template: logs
    example:
    - type: <tcp/udp>
      port: <PORT>
      service: keycloak
      source: keycloak
