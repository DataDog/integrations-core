[
    {
        "agent_version": "6.16.0",
        "integration": "External DNS",
        "check": "external_dns.prometheus.health",
        "statuses": [
            "ok",
            "critical"
        ],
        "groups": [
            "host",
            "endpoint"
        ],
        "name": "External DNS prometheus health",
        "description": "Returns `CRITICAL` if the check cannot access the metrics endpoint, otherwise returns `OK`."
    },
    {
        "agent_version": "7.78.0",
        "integration": "External DNS",
        "check": "external_dns.openmetrics.health",
        "statuses": [
            "ok",
            "critical"
        ],
        "groups": [
            "host",
            "endpoint"
        ],
        "name": "External DNS OpenMetrics endpoint health",
        "description": "Returns `CRITICAL` if the check cannot access the metrics endpoint, otherwise returns `OK`."
    }
]
