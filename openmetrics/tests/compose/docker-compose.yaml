services:
  openmetrics:
    image: prom/prometheus
    volumes:
     - ./config:/etc/prometheus
    ports:
     - 9090:9090
  zero-buckets:
    image: python:3.13-alpine
    command: python -m http.server 8999 --directory /srv
    volumes:
     - ./zero-buckets:/srv:ro
    ports:
     - 8999:8999
