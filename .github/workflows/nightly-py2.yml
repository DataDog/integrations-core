name: Nightly Python 2 tests

on:
  schedule:
  # 4 AM UTC
  - cron: "0 4 * * *"

jobs:
  test:
    uses: ./.github/workflows/test-all.yml
    with:
      repo: core

      # Options
      checkout_ref: agent-v6
      standard: true
      test-py2: true
      test-py3: false
    secrets: inherit
