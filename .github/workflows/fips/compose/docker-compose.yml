services:
  agent:
    build:
      context: agent
      dockerfile: Dockerfile
      args:
        - BASE_IMAGE=$AGENT_IMAGE
    pid: host
    environment:
      - DD_API_KEY=$DD_API_KEY
      - DD_SITE=datad0g.com
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /proc/:/host/proc/:ro
      - /sys/fs/cgroup:/host/sys/fs/cgroup:ro
    healthcheck:
      test: ["CMD", "agent", "run"]
      interval: 5s
      timeout: 2s
      retries: 3
  http-server:
    build:
      context: server/$PLATFORM
      dockerfile: Dockerfile
      args:
        - BASE_IMAGE=$SERVER_IMAGE
        - CIPHER=$CIPHER
    ports:
      - "8443:443"
    volumes:
      - ./server/ca.crt:${SSL_PATH}/certs/server.crt
      - ./server/ca.key:${SSL_PATH}/private/server.key
    command: ["start-server.sh", $CIPHER]
    healthcheck:
      test: ["CMD", "curl", "-f", "https://localhost:443"]
      interval: 30s
      timeout: 10s
      retries: 3
