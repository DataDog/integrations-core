name: Dummy workflow

on:
  pull_request:
    branches:
      - master

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo ${{ github.actor }}
            echo ${{ github.event.pull_request.number }}