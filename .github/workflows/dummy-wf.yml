name: Dummy workflow

run-name: "[${{ github.event.pull_request.head.sha }}] Dummy workflow"

on:
  pull_request:
    branches:
      - master

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo "Head branch: "
            echo "Ref: "
            echo ${{ github.ref_name }}
           