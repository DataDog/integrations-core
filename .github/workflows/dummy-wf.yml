name: Dummy workflow
run-name: Dummy workflow - ${{ github.event.pull_request.title }}


on:
  pull_request:
    branches:
      - main

jobs:
  dummy-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"