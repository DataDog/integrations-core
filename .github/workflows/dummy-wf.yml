name: Dummy workflow

run-name: "[${{ substring(github.event.pull_request.head.sha, 0, 7) }}] Dummy workflow"

on:
  pull_request:
    branches:
      - master

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo "Head branch: "
            echo "Ref: "
            echo ${{ github.ref_name }}
           