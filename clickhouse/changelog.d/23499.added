Add a Remote Query producer through the ClickHouse check's standard optional execution interface: execute a validated query exactly once through a dedicated streaming client with read-only server settings and send/receive timeouts derived from the remaining run budget, streaming the bounded server-rendered rows to its-agent-intake as one direct request per page. Producer runs also open native ddtrace producer spans at the producer's phase boundaries through the shared base tracing machinery, fail-open.