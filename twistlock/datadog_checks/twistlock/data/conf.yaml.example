init_config:

instances:
  ## @param endpoint - string - required
  ## The twistlock prometheus endpoint
  #
  - endpoint: 'http://localhost:8081/api/v1/metrics'

  ## @param username - string - required
  ## The twistlock prometheus endpoint
  #
    username: 'admin'

  ## @param password - string - required
  ## The twistlock prometheus endpoint
  #
    password: 'password'
