{
  "version": 2,
  "created_at": "2024-04-05",
  "last_updated_at": "2024-04-05",
  "title": "Cisco SD-WAN device has rebooted several times",
  "tags": [
    "integration:cisco-sdwan"
  ],
  "description": "This monitor alerts when a Cisco SD-WAN device has rebooted more than 5 times within the last 10 minutes.",
  "definition": {
	"name": "[Cisco SD-WAN] Device {{hostname.name}} ({{system_ip.name}}) rebooted more than 5 times in the last 10 minutes",
	"type": "query alert",
	"query": "sum(last_10m):sum:cisco_sdwan.reboot.count{*} by {device_namespace,hostname,system_ip} > 5",
	"message": "{{#is_alert}}\nSD-WAN Device {{hostname.name}} ({{system_ip.name}}) rebooted more than 5 times in the last 10 minutes.\n{{/is_alert}}\n\nTo know more about the status of your device, you can have more information from the [NDM page for the device {{device_namespace.name}}:{{system_ip.name}}](/infrastructure/devices/graph?inspectedDevice={{device_namespace.name}}%3A{{system_ip.name}}).",
	"tags": [],
	"options": {
		"thresholds": {
			"critical": 5
		},
		"notify_audit": false,
		"include_tags": true,
		"new_group_delay": 60
	}
  }
}
