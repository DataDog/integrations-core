provides:
  - id: rabbitmq-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: rabbitmq-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: rabbitmq-events
    always_on: true
    data_type: events
    direction: inbound
