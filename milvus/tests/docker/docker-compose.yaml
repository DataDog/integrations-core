services:

# Caddy will create host a site with the metrics to exposes
  caddy:
    image: caddy:2.7
    container_name: caddy
    ports:
      - "9091:9091"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ../fixtures/milvus_payload.txt:/metrics/metrics
