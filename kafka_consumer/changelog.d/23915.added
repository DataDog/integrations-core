Add consumer group rebalance detection (`kafka.consumer_group.rebalancing`) and membership-change counting (`kafka.consumer_group.membership_changes`) metrics (DSM only), plus partition assignor, group type, simple-group, and static-membership tags, when cluster monitoring is enabled.
