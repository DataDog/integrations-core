services:
  singlestore:
    image: ghcr.io/singlestore-labs/singlestoredb-dev:0.2.51
    ports:
      - 3306:3306
      - 8080:8080

    platform: linux/amd64
    environment:
      ROOT_PASSWORD: 'password'
      START_AFTER_INIT: 'Y'
