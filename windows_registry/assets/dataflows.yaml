provides:
  - id: windows-registry-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: windows-registry-logs
    always_on: false
    data_type: logs
    direction: inbound
