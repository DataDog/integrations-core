Rework schema collection onto the shared ``SchemaCollector``, which streams schema metadata with snapshot markers. MySQL 8.0 and later use a single-query strategy and older versions use a chunked strategy that works on every supported version.
