Collect MongoDB slow operation metrics for auto-discovered databases when DBM is enabled. The slow operation metrics are collected from
- `system.profile` collection when database profiler is enabled
- MongoDB slow query logs when database profiler is not enabled
