Fall back to the configured host when serverStatus hostname resolution fails, preventing a TypeError crash when the MongoDB check runs without reported_database_hostname and the server is unreachable via Private Link or proxy.
