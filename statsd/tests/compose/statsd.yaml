services:
  statsd:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - STATSD_VERSION=${STATSD_VERSION}
    image: statsd/statsd:${STATSD_VERSION}
    ports:
      - "8125:8125"
      - "8128:8126"
    volumes:
      - ${DD_LOG_1:-/dev/null}:/usr/src/app/statsd.log
