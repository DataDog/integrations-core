Route the OpenStack API and Keystone authentication requests through the shared HTTP client and catch the backend-agnostic ``HTTPStatusError``, ``HTTPTimeoutError``, and ``HTTPConnectionError`` so the check keeps working after the httpx migration.
