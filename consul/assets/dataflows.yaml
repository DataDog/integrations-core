provides:
  - id: consul-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: consul-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: consul-events
    always_on: true
    data_type: events
    direction: inbound
