provides:
  - id: docker-daemon-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: docker-daemon-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: docker-daemon-events
    always_on: true
    data_type: events
    direction: inbound
