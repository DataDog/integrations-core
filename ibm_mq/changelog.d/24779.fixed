Fix `queue_patterns` being ignored when combined with `queue_regex`: an operator-precedence bug forced a full `'*'` queue discovery sweep instead of scoping discovery to the patterns. Also stop polling and submitting channels twice when a wildcard `channels` entry (e.g. `YMQU.*`) is used together with channel auto-discovery; the de-duplication now matches `channels` entries as MQ generic-name patterns.
