[
    {
        "agent_version": "5.0.0",
        "integration":"redis",
        "check": "redis.can_connect",
        "statuses": ["ok", "critical"],
        "groups": ["host", "redis_host", "redis_port"],
        "name": "Can Connect",
        "description": "Returns `CRITICAL` if the Agent check is unable to connect to the monitored redis instance. Returns `OK` otherwise."
    },
    {
        "agent_version": "5.3.0",
        "integration":"redis",
        "check": "redis.replication.master_link_status",
        "statuses": ["ok", "critical"],
        "groups": ["host", "redis_host", "redis_port"],
        "name": "Master Link Status",
        "description": "Returns `CRITICAL` if this Redis instance is unable to connect to its master instance. Returns `OK` otherwise."
    }
]
