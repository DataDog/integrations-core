name: HyperV
files:
- name: hyperv.yaml
  options:
  - template: init_config
    options:
    - template: init_config/default
  - template: instances
    options:
    - template: instances/pdh
    - template: instances/default
