version: "3.5"

services:
  systemd2:
    privileged: true
    image: "centos/systemd:latest"
    volumes:
      - ${HOST_SOCKET_DIR}/systemd_run:/run
      - ${HOST_SOCKET_DIR}/run_dbus:/var/run/dbus
      - ./conf/system.conf:/etc/systemd/system.conf
