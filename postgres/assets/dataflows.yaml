provides:
  - id: postgres-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: postgres-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: postgres-events
    always_on: true
    data_type: events
    direction: inbound
