provides:
  - id: vault-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: vault-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: vault-events
    always_on: true
    data_type: events
    direction: inbound
