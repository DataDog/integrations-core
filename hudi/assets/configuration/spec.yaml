name: Hudi
files:
- name: hudi.yaml
  options:
  - template: init_config
    options:
    - template: init_config/jmx
  - template: instances
    options:
    - template: instances/jmx
  - template: logs
    example:
    - type: file
      path: /var/log/hudi.log
      source: hudi
