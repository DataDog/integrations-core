services:

# Caddy will create host a site with the metrics to exposes
  velero-caddy:
    image: caddy:2.7
    container_name: caddy
    ports:
      - "8085:8085"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ../fixtures/velero_payload.txt:/metrics/metrics
