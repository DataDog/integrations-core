version: "3"

services:
  iot-edge-mock:
    image: caddy:2.1.1-windowsservercore
    container_name: iot-edge-mock
    volumes:
      - ./metrics/:C:\usr\share\caddy\metrics\
    ports:
      - ${MOCK_SERVER_PORT}:80
