version: "3"

services:
  iot-edge-mock:
    image: nginx:alpine
    container_name: iot-edge-mock
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./metrics/:/www/data/metrics/
    ports:
      - ${MOCK_SERVER_PORT}:80
