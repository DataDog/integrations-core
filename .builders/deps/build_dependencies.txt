hatchling==1.21.1
hatch-requirements-txt
setuptools==77.0.3
wheel==0.38.4
setuptools-scm
setuptools-rust>=1.7.0
maturin
cffi>=1.12
cython==3.2.1
tomli>=2.0.1
# ddtrace's setup.py imports the cmake package to build its C++ extensions. Build isolation is
# off, so its build-system requires are never installed and this file is all it sees.
# Range matches ddtrace's own pyproject constraint.
cmake>=3.24.2,<3.28
