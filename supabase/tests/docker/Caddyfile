:8000 {
    handle_path /metrics {
        root * /usr/share/caddy
        rewrite * /supabase_metrics
        file_server
    }
}

:9000 {
    handle_path /metrics {
        root * /usr/share/caddy
        rewrite * /storage_api-metrics
        file_server
    }
}
