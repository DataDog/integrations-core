ARG VERTICA_VERSION="latest"
FROM opentext/vertica-k8s:${VERTICA_VERSION}

ADD ./custom-entrypoint.sh ${VERTICA_HOME_DIR}/custom-entrypoint.sh
RUN rm -rf "${VERTICA_OPT_DIR}/packages"

# Use exec form; assume script is executable
ENTRYPOINT ["bash","-lc","${VERTICA_HOME_DIR}/custom-entrypoint.sh"]