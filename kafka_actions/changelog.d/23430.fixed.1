Preload protobuf well-known types (e.g. `google/protobuf/timestamp.proto`) into the descriptor pool so schemas that reference them deserialize correctly, and surface deserialization failures as action errors so they're reported to the backend instead of silently embedded in the message value.