version: '3.5'

services:
  lighttpd:
    image: sebp/lighttpd:${LIGHTTPD_VERSION}-r0
    ports:
      - "9449:9449"
    volumes:
      - ./lighttpd.conf:/etc/lighttpd/lighttpd.conf
      - ./passwd:/etc/lighttpd/passwd
