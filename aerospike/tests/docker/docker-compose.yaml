version: '3'

services:
  aerospike:
    networks:
      - aerospike
    image: aerospike/aerospike-server:${AEROSPIKE_VERSION}
    ports:
      - "3000:3000"

  networks:
    aerospike:
      external: true
