name: {integration_name}
files:
- name: {integration_name}.yaml
  options:
  - template: logs
    example:
    - type: file
      path: /var/log/{integration_name}.log
      source: {check_name}
      service: <SERVICE>
