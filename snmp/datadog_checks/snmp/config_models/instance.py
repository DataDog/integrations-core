# (C) Datadog, Inc. 2023-present
# All rights reserved
# Licensed under a 3-clause BSD style license (see LICENSE)

# This file is autogenerated.
# To change this file you should edit assets/configuration/spec.yaml and then run the following commands:
#     ddev -x validate config -s <INTEGRATION_NAME>
#     ddev -x validate models -s <INTEGRATION_NAME>

from __future__ import annotations

from types import MappingProxyType
from typing import Any, Optional

from pydantic import BaseModel, ConfigDict, field_validator, model_validator

from datadog_checks.base.utils.functions import identity
from datadog_checks.base.utils.models import validation

from . import defaults, validators


class MetricPatterns(BaseModel):
    model_config = ConfigDict(
        arbitrary_types_allowed=True,
        frozen=True,
    )
    exclude: Optional[tuple[str, ...]] = None
    include: Optional[tuple[str, ...]] = None


class InstanceConfig(BaseModel):
    model_config = ConfigDict(
        validate_default=True,
        arbitrary_types_allowed=True,
        frozen=True,
    )
    authKey: Optional[str] = None
    authProtocol: Optional[str] = None
    bulk_threshold: Optional[int] = None
    collect_device_metadata: Optional[bool] = None
    community_string: Optional[str] = None
    context_engine_id: Optional[str] = None
    context_name: Optional[str] = None
    disable_generic_tags: Optional[bool] = None
    discovery_allowed_failures: Optional[int] = None
    discovery_interval: Optional[int] = None
    discovery_workers: Optional[int] = None
    empty_default_hostname: Optional[bool] = None
    enforce_mib_constraints: Optional[bool] = None
    ignored_ip_addresses: Optional[tuple[str, ...]] = None
    interface_configs: Optional[tuple[MappingProxyType[str, Any], ...]] = None
    ip_address: Optional[str] = None
    loader: Optional[str] = None
    metric_patterns: Optional[MetricPatterns] = None
    metric_tags: Optional[tuple[MappingProxyType[str, Any], ...]] = None
    metrics: Optional[tuple[MappingProxyType[str, Any], ...]] = None
    min_collection_interval: Optional[float] = None
    network_address: Optional[str] = None
    oid_batch_size: Optional[int] = None
    port: Optional[int] = None
    privKey: Optional[str] = None
    privProtocol: Optional[str] = None
    profile: Optional[str] = None
    refresh_oids_cache_interval: Optional[int] = None
    retries: Optional[int] = None
    service: Optional[str] = None
    snmp_version: Optional[int] = None
    tags: Optional[tuple[str, ...]] = None
    timeout: Optional[int] = None
    use_device_id_as_hostname: Optional[bool] = None
    use_global_metrics: Optional[bool] = None
    user: Optional[str] = None
    workers: Optional[int] = None

    @model_validator(mode='before')
    def _initial_validation(cls, values):
        return validation.core.initialize_config(getattr(validators, 'initialize_instance', identity)(values))

    @field_validator('*', mode='before')
    def _validate(cls, value, info):
        field = cls.model_fields[info.field_name]
        field_name = field.alias or info.field_name
        if field_name in info.context['configured_fields']:
            value = getattr(validators, f'instance_{info.field_name}', identity)(value, field=field)
        else:
            value = getattr(defaults, f'instance_{info.field_name}', lambda: value)()

        return validation.utils.make_immutable(value)

    @model_validator(mode='after')
    def _final_validation(cls, model):
        return validation.core.check_model(getattr(validators, 'check_instance', identity)(model))
