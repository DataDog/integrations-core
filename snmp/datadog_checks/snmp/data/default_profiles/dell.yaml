# Profile for Dell  devices

extends:
  - _base.yaml
  - _generic-ip.yaml
  - _generic-tcp.yaml
  - _generic-udp.yaml

metadata:
  device:
    fields:
      vendor:
        value: "dell"

sysobjectid: 1.3.6.1.4.1.674.*
