version: '3.5'

services:
  snmp:
    image: "florianveauxdd/snmp:florian-test"
    ports:
      - "1161:1161/udp"
    command: --args-from-file=/usr/snmpsim/data/args_list.txt
    volumes:
      - ${DATA_DIR}:/usr/snmpsim/data/
    container_name: dd-snmp
