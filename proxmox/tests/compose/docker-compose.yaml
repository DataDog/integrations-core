services:
  proxmox:
    image: makedie/proxmox_ve:8.3.0
    platform: linux/amd64
    container_name: pve
    hostname: pve
    privileged: true
    ports:
      - "8006:8006"
    tty: true
    stdin_open: true
