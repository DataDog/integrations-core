provides:
  - id: proxmox-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: proxmox-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: proxmox-events
    always_on: true
    data_type: events
    direction: inbound
