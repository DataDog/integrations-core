Fix `nutanix.vm.disk_capacity_bytes` to source allocation from VM config (`disks[].backingInfo.diskSizeBytes`); the v4 stats endpoint never returns `diskCapacityBytes` so the metric was effectively never emitted.