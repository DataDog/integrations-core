Pin `minimum_version = TLSv1_2` on the SSL context used to fetch intermediate certificates via AIA chasing, so the handshake at `RequestsWrapper.fetch_intermediate_certs` no longer falls back to SSLv3/TLSv1.0/TLSv1.1.
