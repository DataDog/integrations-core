Add ``get_peer_cert`` and the ``encoding``, ``elapsed``, ``cookies``, ``links``, ``url``, and ``history`` attributes to the agnostic HTTP response protocol. Add ``ignore_tls_warning``, ``persist_connections``, and ``should_bypass_proxy`` to the agnostic HTTP client protocol.
