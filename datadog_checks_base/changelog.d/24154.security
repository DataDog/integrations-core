Fetch AIA intermediate certificates with a fresh credential-free HTTP session over public http(s) endpoints only, trying TLS first then plain HTTP, without following redirects, and capping the response size.
