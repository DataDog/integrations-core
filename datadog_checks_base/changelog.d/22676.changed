The HTTP request wrapper now raises backend-agnostic exception types (``http_exceptions``) instead of ``requests``-specific ones. Checks that catch HTTP errors from ``self.http`` should catch the agnostic types.
