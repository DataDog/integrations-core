provides:
  - id: argocd-metrics
    always_on: true
    data_type: metrics
    direction: inbound
  - id: argocd-logs
    always_on: false
    data_type: logs
    direction: inbound
  - id: argocd-events
    always_on: false
    data_type: events
    direction: inbound
